# 商品规格（specs+skus）接口文档

> 规格不单独建，跟着商品一起提交。`POST /it/shop/item` 一个接口全搞定。

---

## 一、新增商品（含规格）`POST /it/shop/item`

### 单规格（不传 specs，兼容旧版）

```json
{
  "name": "小米14 Pro",
  "price": 318900,
  "stock": 50,
  "image": "https://xxx.jpg",
  "categoryId": 1,
  "brandId": 1
}
```

### 多规格（传 specs + skus）

```json
{
  "name": "纯棉T恤",
  "image": "https://xxx.jpg",
  "categoryId": 3,
  "brandId": 1,
  "specs": [
    { "specName": "颜色", "values": ["白色", "黑色"] },
    { "specName": "尺码", "values": ["S", "M", "L"] }
  ],
  "skus": [
    { "specData": {"颜色":"白色","尺码":"S"}, "price": 9900, "stock": 200 },
    { "specData": {"颜色":"白色","尺码":"M"}, "price": 9900, "stock": 200 },
    { "specData": {"颜色":"黑色","尺码":"M"}, "price": 9900, "stock": 200 },
    { "specData": {"颜色":"黑色","尺码":"L"}, "price": 9900, "stock": 200 }
  ]
}
```

> **规则**：传了 `specs` 就按多规格走。**多规格时不要传顶层 `price` / `stock`**，后端从 SKU 自动算。

### 响应

```json
{ "code": 200, "message": "success", "data": { "id": 3, "name": "纯棉T恤", "price": 9900, "stock": 800 } }
```

---

## 二、编辑商品（含更新规格）`PUT /it/shop/item/{itemId}`

同新增，传完整 `name` + `image` + `specs` + `skus`。后端软删旧 SKU 再写入新的。

### 响应

```json
{ "code": 200, "message": "success" }
```

---

## 三、商家商品列表 `GET /it/shop/item/page`

请求：`?page=1&size=10&status=&keyword=`

响应含 `specs` + `skus`，编辑时直接回显：

```json
{
  "code": 200,
  "data": {
    "list": [{
      "id": 3, "name": "纯棉T恤", "price": 9900, "stock": 800, "sold": 5,
      "specs": [
        { "specName": "颜色", "values": [{ "value": "白色", "stock": 400 }, { "value": "黑色", "stock": 400 }] },
        { "specName": "尺码", "values": [{ "value": "S", "stock": 200 }, { "value": "M", "stock": 400 }, { "value": "L", "stock": 200 }] }
      ],
      "skus": [{
        "id": 7, "itemId": 3, "skuName": "纯棉T恤 白色 S",
        "price": 9900, "stock": 200, "image": null,
        "specData": { "颜色": "白色", "尺码": "S" }
      }]
    }],
    "total": 15, "pages": 2
  }
}
```

---

## 四、商品详情 `GET /it/items/{id}`

响应格式同上，`mainImage` / `images` 字段不同：

```json
{
  "code": 200,
  "data": {
    "id": 3, "name": "纯棉T恤", "price": 9900,
    "mainImage": "https://xxx.jpg", "images": ["https://xxx.jpg"],
    "sold": 5, "brandName": "某品牌", "categoryName": "服饰",
    "specs": [ ... ],
    "skus": [ ... ]
  }
}
```

---

## 五、字段说明

### `specs[]`

| 字段 | 类型 | 说明 |
|------|------|------|
| `specName` | string | "颜色" / "尺码" |
| `values[].value` | string | 规格值 |
| `values[].stock` | int | 该规格值下所有 SKU 库存之和（列表/详情返回，新增时是 string[]） |

### `skus[]`

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | long | SKU ID |
| `itemId` | long | 商品 ID |
| `skuName` | string | 自动生成 "商品名 白色 S" |
| `price` | int | **分** |
| `stock` | int | 库存 |
| `image` | string | SKU 独立图，可 null |
| `specData` | object | `{"颜色":"白色","尺码":"S"}` |

### 全局约定

| 项 | 值 |
|------|-----|
| 响应格式 | `{ code: 200, data: {...} }` |
| 金额单位 | **分**，前端 `/100` |
| 分页数组字段 | `list` |
| 多规格判断 | `specs != null && specs.length > 0` |

---

## 六、前端取值示例

```js
// 编辑回显
const res = response.data
const item = res.list[0]   // 列表
// 或 res                    // 详情
const skuList = item.skus || []
const specGroups = item.specs || []

// SKU 匹配
const selected = { "颜色": "白色", "尺码": "S" }
const currentSku = skuList.find(sku =>
  Object.entries(selected).every(([k, v]) => sku.specData[k] === v)
)
const price = currentSku?.price ?? item.price
const stock = currentSku?.stock ?? 0

// 库存判断
const val = specGroups.find(g => g.specName === "颜色")?.values.find(v => v.value === "白色")
const hasStock = (val?.stock ?? 0) > 0
```
