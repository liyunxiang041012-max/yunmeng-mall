# 错误修复总结 - user_id 为空问题

## 🐛 错误信息

```
java.sql.SQLException: Field 'user_id' doesn't have a default value
SQL: INSERT INTO favorite (item_id) VALUES (?)
```

---

## 🔍 错误原因

### 1. 根本原因
`ym-item` 服务**缺少 `WebMvcConfig` 配置类**，导致 `UserInfoInterceptor` 拦截器未注册。

### 2. 直接原因
- 用户请求到达 Controller 时，`UserContext.getUserId()` 返回 `null`
- MyBatis Plus 的 `save()` 方法只插入了非 null 字段（只插入了 `item_id`）
- 数据库表 `user_id` 字段设置了 `NOT NULL` 约束且无默认值
- 导致 SQL 执行失败

### 3. 调用链路
```
前端请求 → 网关（JWT解析）→ ym-item 服务
                                    ↓
                            ❌ 缺少拦截器
                                    ↓
                            UserContext 为空
                                    ↓
                            Controller 获取 userId = null
                                    ↓
                            Service save(favorite) 只插入 item_id
                                    ↓
                            ❌ 数据库报错：user_id 不能为空
```

---

## ✅ 解决方案

### 修复 1：创建 WebMvcConfig 配置类（已完成）

**文件位置：** `ym-item/src/main/java/com/liyun/item/config/WebMvcConfig.java`

```java
package com.liyun.item.config;

import com.liyun.common.interceptor.UserInfoInterceptor;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.InterceptorRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class WebMvcConfig implements WebMvcConfigurer {

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(new UserInfoInterceptor()).addPathPatterns("/**");
    }
}
```

**作用：**
- 注册 `UserInfoInterceptor` 拦截器
- 拦截所有请求（`/**`）
- 自动从请求头获取 `user-id` 并设置到 `UserContext`

---

### 修复 2：Service 层添加空值校验（已完成）

**修改文件：**
1. `FavoriteServiceImpl.java`
2. `ShopFollowServiceImpl.java`

**添加内容：**
```java
@Override
public boolean toggleFavorite(Long userId, Long itemId) {
    // 校验用户ID
    if (userId == null) {
        throw new RuntimeException("用户未登录");
    }
    
    // ... 后续逻辑
}
```

**作用：**
- 提前发现并提示用户未登录问题
- 避免 null 值传入数据库导致 SQL 错误
- 提供更友好的错误信息

---

## 🧪 验证步骤

### 1. 重新编译项目
```bash
mvn clean package -DskipTests
```

### 2. 重启 ym-item 服务
```bash
java -jar ym-item/target/ym-item-1.0.0.jar
```

### 3. 测试接口

**方式一：通过网关测试（推荐）**
```bash
# 1. 先登录获取 token
POST http://localhost:8080/us/user/login
{
  "username": "test",
  "password": "123456"
}

# 2. 使用 token 调用收藏接口
POST http://localhost:8080/it/favorites/toggle/1
Authorization: Bearer <token>
```

**方式二：直接测试 ym-item 服务（开发调试用）**
```bash
# 手动在请求头添加 user-id（模拟网关）
POST http://localhost:8082/it/favorites/toggle/1
user-id: 1
```

---

## 📊 修复效果对比

### 修复前
```json
{
  "timestamp": "2026-05-28T12:00:00",
  "status": 500,
  "error": "Internal Server Error",
  "message": "Field 'user_id' doesn't have a default value"
}
```

### 修复后
```json
{
  "code": 200,
  "data": true,
  "msg": "ok"
}
```

---

## 🔧 相关文件清单

### 新增文件
- ✅ `ym-item/src/main/java/com/liyun/item/config/WebMvcConfig.java`

### 修改文件
- ✅ `ym-item/src/main/java/com/liyun/item/service/impl/FavoriteServiceImpl.java`
- ✅ `ym-item/src/main/java/com/liyun/item/service/impl/ShopFollowServiceImpl.java`

---

## 💡 经验总结

### 1. 微服务架构注意事项
- **每个微服务都需要配置拦截器**，不能只在一个服务配置
- 网关负责认证，但下游服务需要配置拦截器来接收用户信息
- `UserInfoInterceptor` 从请求头获取用户信息（网关注入）

### 2. 数据库设计建议
- `NOT NULL` 字段最好设置默认值
- 或者在代码层做好空值校验

### 3. 错误处理最佳实践
- 提前校验参数，避免数据库报错
- 提供友好的错误信息
- 使用全局异常处理器统一处理

---

## 📖 扩展阅读

- **拦截器原理**：`UserInfoInterceptor` 在每次请求前自动执行
- **ThreadLocal**：`UserContext` 使用 ThreadLocal 保证线程安全
- **网关转发**：网关解析 JWT 后，将用户信息放入请求头转发给下游

---

## ⚠️ 重要提示

1. **重启服务**：修改配置后必须重启 `ym-item` 服务
2. **通过网关访问**：前端请求必须通过网关（8080端口），不要直接访问 ym-item（8082端口）
3. **Token 有效期**：确保 JWT Token 未过期
4. **权限问题**：某些接口可能需要特定角色权限

---

**修复完成时间：2026-05-28**  
**问题类型：配置缺失 + 参数校验不足**  
**影响范围：收藏、关注相关接口**
