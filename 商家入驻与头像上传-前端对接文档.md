# 云梦商城 - 商家入驻 & 头像上传 & 店铺关注 接口文档

> 网关地址: `http://localhost:8080` | 更新时间: 2026-06-18

---

## 鉴权说明

请求头携带 Token：
```
Authorization: Bearer <token>
```

统一响应格式：
```json
{ "code": 200, "message": "success", "data": {} }
```

---

## 接口清单

| 序号 | 接口 | 方法 | 权限 |
|------|------|------|------|
| 1 | `/us/user-profile/avatar` | POST | 登录 |
| 2 | `/it/shop/status` | GET | 商家 |
| 3 | `/it/shop/upload/avatar` | POST | 商家 |
| 4 | `/it/shop/entry` | POST | 商家 |
| 5 | `/it/shop/follow/my` | GET | 登录 |
| 6 | `/it/shop/follow/toggle/{shopId}` | POST | 登录 |
| 7 | `/it/shop/follow/check/{shopId}` | GET | 登录 |
| 8 | `/it/shop/follow/fans-count/{shopId}` | GET | 无 |

---

## 1. 用户头像上传

```
POST /us/user-profile/avatar
Content-Type: multipart/form-data
```

**参数**: `file` (File, 必填) — jpg/png/gif/webp，≤5MB

**响应**:
```json
{
  "code": 200,
  "data": "https://yunmengmall.oss-cn-hangzhou.aliyuncs.com/user-header/2026/06/18/uuid.jpg"
}
```

**JS 示例**:
```js
async function uploadUserAvatar(file) {
  const fd = new FormData(); fd.append('file', file);
  const res = await axios.post('/us/user-profile/avatar', fd, {
    headers: { 'Content-Type': 'multipart/form-data' }
  });
  return res.data.data; // 头像URL
}
```

---

## 2. 商家店铺状态查询

```
GET /it/shop/status
权限: 仅商家 (role=1)
```

**响应**:

未设置店铺:
```json
{ "code": 200, "data": { "hasShop": false } }
```

已设置店铺:
```json
{ "code": 200, "data": { "hasShop": true, "shopId": 1, "shopName": "云梦小店" } }
```

**前端判断**:
```js
const { hasShop } = (await axios.get('/it/shop/status')).data.data;
if (hasShop) { goToDashboard(); } else { goToEntryPage(); }
```

---

## 3. 商家店铺头像上传

```
POST /it/shop/upload/avatar
Content-Type: multipart/form-data
权限: 仅商家 (role=1)
```

**参数**: `file` (File, 必填) — jpg/png/gif/webp，≤5MB

**响应**:
```json
{
  "code": 200,
  "data": "https://yunmengmall.oss-cn-hangzhou.aliyuncs.com/item/2026/06/18/uuid.jpg"
}
```

**JS 示例**:
```js
async function uploadShopAvatar(file) {
  const fd = new FormData(); fd.append('file', file);
  const res = await axios.post('/it/shop/upload/avatar', fd, {
    headers: { 'Content-Type': 'multipart/form-data' }
  });
  return res.data.data;
}
```

---

## 4. 商家入驻（设置店铺信息）

```
POST /it/shop/entry
Content-Type: application/json
权限: 仅商家 (role=1)
```

**请求体**:
```json
{
  "shopName": "云梦优选小店",
  "logo": "https://oss.xxx/item/xxx.jpg",
  "description": "专注优质商品"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| shopName | String | 是 | 店铺名称 |
| logo | String | 否 | 头像URL |
| description | String | 否 | 描述，≤512字 |

**响应**: `{ "code": 200, "data": null }`

**完整入驻流程**:
```js
// 1. 上传头像
const logoUrl = file ? await uploadShopAvatar(file) : '';
// 2. 提交入驻
await axios.post('/it/shop/entry', {
  shopName: form.shopName,
  logo: logoUrl,
  description: form.description
});
// → 成功，跳转商家后台
```

> 已入驻的店铺再次调用等同于更新店铺信息。

---

## 5. 店铺关注管理

### 5.1 我的关注列表（分页）

```
GET /it/shop/follow/my?page=1&size=20
```

**响应例**:
```json
{
  "code": 200,
  "data": {
    "total": 5,
    "pages": 1,
    "list": [{
      "id": 10, "shopId": 3, "shopName": "小店",
      "logo": "https://oss.xxx/item/xxx.jpg",
      "description": "...",
      "createTime": "2026-06-18 12:00:00"
    }]
  }
}
```

```js
const res = await axios.get('/it/shop/follow/my', { params: { page: 1, size: 20 } });
// 使用 res.data.data.list
```

### 5.2 切换关注

```
POST /it/shop/follow/toggle/{shopId}
```

**响应**: `{ "code": 200, "data": true }` — true=已关注, false=已取消

```js
const res = await axios.post(`/it/shop/follow/toggle/${shopId}`);
const followed = res.data.data; // true/false
```

### 5.3 检查关注状态

```
GET /it/shop/follow/check/{shopId}
```

**响应**: `{ "code": 200, "data": true }` — true=已关注

### 5.4 店铺粉丝数

```
GET /it/shop/follow/fans-count/{shopId}
```

**响应**: `{ "code": 200, "data": 128 }`

---

## 错误码

| code | 常见原因 |
|------|----------|
| 200 | 成功 |
| 401 | Token缺失或过期 |
| 500 | 服务异常（看message） |

**常见 message**:
| message | 原因 |
|---------|------|
| 仅商家可访问此接口 | 用户role≠1 |
| 上传文件不能为空 | 未选文件 |
| 文件大小不能超过5MB | 图片太大 |
| 只支持jpg/png/gif/webp | 格式不对 |
| 店铺名称不能为空 | shopName未填 |
| 请先登录 | 未传Token |
