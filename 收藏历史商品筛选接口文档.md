# 收藏 & 历史 & 商品筛选 — 后端接口实现文档

## ✅ 已实现的接口清单

### 1. 收藏相关接口

| 方法 | 端点 | 说明 | 状态 |
|------|------|------|------|
| POST | `/it/favorites/toggle/{itemId}` | 切换收藏状态 | ✅ 已实现 |
| GET | `/it/favorites/check/{itemId}` | 检查是否已收藏 | ✅ 已实现 |
| GET | `/it/favorites/my` | **获取我的收藏列表（分页）** | ✅ 新增 |

---

### 2. 浏览历史接口

| 方法 | 端点 | 说明 | 状态 |
|------|------|------|------|
| GET | `/it/history/my` | **获取我的浏览历史（分页）** | ✅ 新增 |
| DELETE | `/it/history/{id}` | **删除单条浏览记录** | ✅ 新增 |
| DELETE | `/it/history/clear` | **清空全部浏览记录** | ✅ 新增 |

---

### 3. 商品分页接口（已有，增强）

| 方法 | 端点 | 说明 | 状态 |
|------|------|------|------|
| GET | `/it/items/page` | 商品分页查询 | ✅ 已增强，新增 `shopId` 过滤 |

---

## 📊 接口详细说明

### 1. GET /it/favorites/my — 我的收藏列表

**请求参数：**
```
GET /it/favorites/my?page=1&size=10
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | int | 否 | 1 | 页码 |
| size | int | 否 | 10 | 每页条数 |

**返回值：** `Result<PageDTO<FavoriteItemVO>>`

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "total": 34,
    "pages": 4,
    "list": [
      {
        "id": 1,
        "itemId": 101,
        "name": "极简无线降噪耳机",
        "itemName": "极简无线降噪耳机",
        "mainImage": "/images/item/101/main.jpg",
        "image": "/images/item/101/thumb.jpg",
        "price": 8900,
        "salesCount": 1200,
        "createTime": "2026-05-20 14:30:00"
      }
    ]
  }
}
```

---

### 2. GET /it/history/my — 我的浏览历史

**请求参数：**
```
GET /it/history/my?page=1&size=10
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | int | 否 | 1 | 页码 |
| size | int | 否 | 10 | 每页条数 |

**返回值：** `Result<PageDTO<HistoryItemVO>>`

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "total": 210,
    "pages": 21,
    "list": [
      {
        "id": 1,
        "itemId": 201,
        "name": "轻量碳纤维背包",
        "itemName": "轻量碳纤维背包",
        "mainImage": "/images/item/201/main.jpg",
        "image": "/images/item/201/thumb.jpg",
        "price": 12900,
        "createTime": "2026-05-28 10:15:00",
        "viewTime": "2026-05-28 10:15:00"
      }
    ]
  }
}
```

---

### 3. DELETE /it/history/{id} — 删除单条浏览记录

**请求参数：**
```
DELETE /it/history/1
```

| 参数 | 位置 | 类型 | 说明 |
|------|------|------|------|
| id | Path | Long | 浏览记录 ID |

**返回值：** `Result<Void>`

```json
{
  "code": 200,
  "message": "ok"
}
```

---

### 4. DELETE /it/history/clear — 清空全部浏览记录

**请求参数：** 无

**返回值：** `Result<Void>`

```json
{
  "code": 200,
  "message": "ok"
}
```

---

### 5. GET /it/items/page — 商品分页（增强版）

**请求参数：**
```
GET /it/items/page?page=1&size=20&shopId=10&sort=sales
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | int | 否 | 1 | 页码 |
| size | int | 否 | 20 | 每页条数 |
| shopId | Long | **否** | - | **新增** — 店铺ID过滤 |
| sort | String | 否 | default | 排序：default/sales/price/newest |
| keyword | String | 否 | - | 关键词搜索 |
| categoryId | Long | 否 | - | 分类ID过滤 |
| brandId | Long | 否 | - | 品牌ID过滤 |
| minPrice | Long | 否 | - | 最低价格（分） |
| maxPrice | Long | 否 | - | 最高价格（分） |

**返回值：** `Result<PageDTO<ItemVO>>`

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "total": 56,
    "pages": 3,
    "list": [
      {
        "id": 301,
        "name": "AI智能降噪蓝牙耳机旗舰版",
        "mainImage": "/images/item/301/main.jpg",
        "image": "/images/item/301/thumb.jpg",
        "price": 59900,
        "originalPrice": 89900,
        "salesCount": 800,
        "shopId": 10,
        "shopName": "云梦数码旗舰店"
      }
    ]
  }
}
```

---

## 🗂️ 新增文件清单

### 数据库脚本
- ✅ `ym-item/src/main/resources/sql/history.sql` — 浏览历史表

### 实体类
- ✅ `History.java` — 浏览历史实体
- ✅ `FavoriteItemVO.java` — 收藏商品 VO
- ✅ `HistoryItemVO.java` — 浏览历史 VO

### Mapper
- ✅ `HistoryMapper.java` — 浏览历史 Mapper

### Service
- ✅ `IHistoryService.java` — 浏览历史 Service 接口
- ✅ `HistoryServiceImpl.java` — 浏览历史 Service 实现
- ✅ `IFavoriteService.java` — 收藏 Service 接口（已扩展）
- ✅ `FavoriteServiceImpl.java` — 收藏 Service 实现（已扩展）

### Controller
- ✅ `HistoryController.java` — 浏览历史 Controller
- ✅ `FavoriteController.java` — 收藏 Controller（已扩展）

### 其他修改
- ✅ `ItemPageQuery.java` — 新增 `shopId` 和 `sort` 字段
- ✅ `ItemServiceImpl.java` — 增加 `shopId` 过滤逻辑
- ✅ `ItemDetailServiceImpl.java` — 商品详情自动记录浏览历史

---

## 🚀 部署步骤

### 第一步：执行数据库脚本

```bash
mysql -u root -p ym_item < ym-item\src\main\resources\sql\history.sql
```

**脚本内容：**
- ✅ 创建 `history` 表（浏览历史）
- ✅ 包含必要的索引

---

### 第二步：重新编译项目

```bash
mvn clean package -DskipTests
```

---

### 第三步：重启服务

```bash
java -jar ym-item/target/ym-item-1.0.0.jar
```

---

## 🧪 接口测试示例

### 测试收藏列表

```bash
GET http://localhost:8080/it/favorites/my?page=1&size=10
Authorization: Bearer <token>
```

---

### 测试浏览历史

```bash
# 1. 先访问商品详情（自动记录历史）
GET http://localhost:8080/it/items/1
Authorization: Bearer <token>

# 2. 查看浏览历史
GET http://localhost:8080/it/history/my?page=1&size=10
Authorization: Bearer <token>

# 3. 删除单条记录
DELETE http://localhost:8080/it/history/1
Authorization: Bearer <token>

# 4. 清空全部历史
DELETE http://localhost:8080/it/history/clear
Authorization: Bearer <token>
```

---

### 测试商品按店铺筛选

```bash
GET http://localhost:8080/it/items/page?page=1&size=20&shopId=10&sort=sales
```

---

## 💡 业务逻辑说明

### 1. 收藏列表
- 分页查询用户的收藏记录
- 批量关联商品信息（避免 N+1 查询）
- 按收藏时间倒序排列

---

### 2. 浏览历史
- **自动记录**：访问商品详情时自动记录
- **去重处理**：同一用户重复浏览同一商品时，删除旧记录，只保留最新
- **未登录用户**：不记录浏览历史
- **按时间倒序**：最新浏览的排在前面

---

### 3. 商品店铺筛选
- 在原有分页接口基础上增加 `shopId` 过滤
- 支持与其他过滤条件组合使用
- 适用于店铺页面展示该店铺的所有商品

---

## ⚠️ 注意事项

1. **用户认证**：收藏和历史接口都需要用户登录（携带 JWT Token）
2. **数据库表**：必须先执行 `history.sql` 创建浏览历史表
3. **浏览历史自动记录**：在商品详情接口中自动触发，无需前端额外调用
4. **分页结构**：使用项目统一的 `PageDTO` 结构（total, pages, list）
5. **价格单位**：所有价格字段单位为**分（fen）**

---

## 📖 附录：数据库表结构

### history 表（浏览历史）

```sql
CREATE TABLE `history` (
  `id`          bigint NOT NULL AUTO_INCREMENT,
  `user_id`     bigint NOT NULL COMMENT '用户ID',
  `item_id`     bigint NOT NULL COMMENT '商品ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '浏览时间',
  PRIMARY KEY (`id`),
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_item_id` (`item_id`),
  INDEX `idx_user_create_time` (`user_id`, `create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

**文档更新时间：2026-05-28**  
**新增接口：5个**  
**增强接口：1个**  
**所有接口均已实现并测试通过 ✅**
