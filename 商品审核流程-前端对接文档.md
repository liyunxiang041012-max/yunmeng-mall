# 商品审核流程 - 前端对接文档

---

## 流程概述

```
商家新增商品 → status=0（待审核/下架）→ 管理员审核通过 → status=1（已上架）→ 用户可见
                                    → 管理员审核驳回 → status=0（下架/拒绝）
```

| 角色 | 能做什么 |
|------|----------|
| **商家** (role=1) | 新增、编辑、删除自己的商品。**不能自行上架** |
| **管理员** (role=2) | 查看所有商品、审核通过/驳回 |
| **普通用户** (role=0) | 浏览已上架商品（status=1） |

---

## 一、商家端接口

### 1.1 新增商品

```
POST /it/shop/item
Content-Type: application/json
```

**请求体：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | String | 是 | 商品名称 |
| `image` | String | 是 | 主图URL（先调上传接口获取） |
| `categoryId` | Long | 是 | 分类ID |
| `brandId` | Long | 否 | 品牌ID |
| `price` | Long | 是 | 价格，单位**分** |
| `stock` | Integer | 是 | 库存 |

**响应：** 返回完整 Item 对象，`status` 固定为 `0`（待审核）

```json
{ "code": 0, "data": { "id": 123, "status": 0, ... } }
```

### 1.2 上传商品图片

```
POST /it/shop/item/upload/image
Content-Type: multipart/form-data

参数: file（图片文件，≤5MB，jpg/png/gif/webp）
返回: { "code": 0, "data": "https://oss-url/xxx.jpg" }
```

### 1.3 编辑商品

```
PUT /it/shop/item/{itemId}
```

请求体同新增（只传需修改字段）。

### 1.4 删除商品

```
DELETE /it/shop/item/{itemId}
```

软删除（`deleted=1`），不可恢复。

### 1.5 商家端调用示例

```javascript
// 步骤1：上传图片
const fd = new FormData(); fd.append('file', file);
const { data: imgUrl } = await fetch('/it/shop/item/upload/image', { method: 'POST', body: fd }).then(r => r.json());

// 步骤2：新增商品（status 自动为 0）
const { data: item } = await fetch('/it/shop/item', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ name: 'xx', image: imgUrl, categoryId: 1, price: 9900, stock: 100 })
}).then(r => r.json());

// item.status === 0  → 前端提示 "已提交，等待管理员审核"
```

---

## 二、管理员审核接口

### 2.1 商品列表（含待审核）

```
GET /it/shop/admin/item/page?page=1&size=10&status=0&keyword=xxx
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 默认1 |
| `size` | int | 否 | 默认10 |
| `status` | int | 否 | 筛选：`0`=待审核，`1`=已上架，不传=全部 |
| `keyword` | String | 否 | 按商品名称模糊搜索 |

**响应：**

```json
{
  "code": 0,
  "data": {
    "total": 5,
    "pages": 1,
    "list": [
      {
        "id": 123,
        "shopId": 10,
        "name": "iPhone 15",
        "image": "https://...",
        "price": 599900,
        "stock": 100,
        "status": 0,
        "createTime": "2026-06-24T12:00:00"
      }
    ]
  }
}
```

### 2.2 审核通过（上架）

```
PUT /it/shop/admin/item/{itemId}/approve
```

将 `status` 设为 `1`，商品对用户可见。

```json
{ "code": 0, "data": null }
```

### 2.3 审核驳回（下架）

```
PUT /it/shop/admin/item/{itemId}/reject
```

将 `status` 设为 `0`，商品对用户不可见。

```json
{ "code": 0, "data": null }
```

### 2.4 管理员调用示例

```javascript
// 查询待审核商品
const { data } = await fetch('/it/shop/admin/item/page?status=0').then(r => r.json());
// data.list → 待审核列表

// 审核通过
await fetch(`/it/shop/admin/item/${itemId}/approve`, { method: 'PUT' });

// 审核驳回
await fetch(`/it/shop/admin/item/${itemId}/reject`, { method: 'PUT' });
```

---

## 三、用户端浏览接口

普通用户只能看到 **已上架（status=1）** 的商品。

### 3.1 商品分页查询

```
GET /it/items/page?pageNo=1&pageSize=10&categoryId=1&keyword=xxx&sortBy=price
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `pageNo` | int | 页码，默认1 |
| `pageSize` | int | 每页条数 |
| `categoryId` | Long | 分类筛选 |
| `brandId` | Long | 品牌筛选 |
| `keyword` | String | 关键词搜索 |
| `minPrice` | Long | 最低价（分） |
| `maxPrice` | Long | 最高价（分） |
| `sortBy` | String | `price`/`sold`，默认按创建时间 |

### 3.2 商品详情

```
GET /it/items/{itemId}
```

### 3.3 错误码说明

| code | 含义 |
|------|------|
| `0` | 成功 |
| `1` | 业务异常（如"仅管理员可访问"、"商品不存在"） |
| `401` | 未登录 |

---

## 四、完整生命周期

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  商家新增商品  │ ──→ │  status = 0  │ ──→ │ 管理员审核    │
│  POST /item   │     │  （待审核）   │     │              │
└──────────────┘     └──────────────┘     └──────┬───────┘
                                          ┌──────┴───────┐
                                          │              │
                                    审核通过          审核驳回
                                   status = 1       status = 0
                                   （上架可见）       （下架不可见）
                                          │
                                          ▼
                                   普通用户浏览
                                   GET /items/page
                                   （仅显示 status=1）
```

---

## 五、修改涉及文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `ym-item/.../controller/ItemAdminController.java` | **新建** | 管理员审核接口 |
| `ym-item/.../controller/MerchantItemController.java` | **修改** | 移除 `toggle-status` 端点 |
| `ym-item/.../service/IItemService.java` | **修改** | 新增 `approveItem`/`rejectItem`/`listAllItems` |
| `ym-item/.../service/impl/ItemServiceImpl.java` | **修改** | 实现审核逻辑 |
