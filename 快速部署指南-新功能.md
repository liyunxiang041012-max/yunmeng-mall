# 新功能快速部署指南

## 📦 本次更新内容

### 新增功能
1. ✅ **收藏列表** — 获取用户收藏的所有商品（分页）
2. ✅ **浏览历史** — 自动记录 + 查询 + 删除 + 清空
3. ✅ **商品筛选增强** — 支持按店铺 ID 过滤商品

---

## 🚀 部署步骤

### 第一步：执行数据库脚本

```bash
# Windows CMD
mysql -u root -p ym_item < ym-item\src\main\resources\sql\history.sql
```

**或者手动执行：**
```sql
USE ym_item;

CREATE TABLE `history` (
  `id`          bigint NOT NULL AUTO_INCREMENT,
  `user_id`     bigint NOT NULL COMMENT '用户ID',
  `item_id`     bigint NOT NULL COMMENT '商品ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '浏览时间',
  PRIMARY KEY (`id`),
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_item_id` (`item_id`),
  INDEX `idx_user_create_time` (`user_id`, `create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

### 第二步：重新编译项目

```bash
mvn clean package -DskipTests
```

---

### 第三步：重启服务

```bash
# 停止旧服务
# 启动新服务
java -jar ym-item/target/ym-item-1.0.0.jar
```

---

## 🧪 接口测试

### 1. 测试收藏列表

```bash
GET http://localhost:8080/it/favorites/my?page=1&size=10
Authorization: Bearer <token>
```

**预期返回：**
```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "total": 10,
    "pages": 1,
    "list": [
      {
        "id": 1,
        "itemId": 101,
        "name": "商品名称",
        "price": 8900,
        "createTime": "2026-05-20 14:30:00"
      }
    ]
  }
}
```

---

### 2. 测试浏览历史

#### 2.1 自动记录浏览历史

```bash
# 访问商品详情（会自动记录）
GET http://localhost:8080/it/items/1
Authorization: Bearer <token>
```

#### 2.2 查询浏览历史

```bash
GET http://localhost:8080/it/history/my?page=1&size=10
Authorization: Bearer <token>
```

**预期返回：**
```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "total": 5,
    "pages": 1,
    "list": [
      {
        "id": 1,
        "itemId": 1,
        "name": "商品名称",
        "price": 12900,
        "createTime": "2026-05-28 10:15:00",
        "viewTime": "2026-05-28 10:15:00"
      }
    ]
  }
}
```

#### 2.3 删除单条记录

```bash
DELETE http://localhost:8080/it/history/1
Authorization: Bearer <token>
```

#### 2.4 清空全部历史

```bash
DELETE http://localhost:8080/it/history/clear
Authorization: Bearer <token>
```

---

### 3. 测试商品按店铺筛选

```bash
GET http://localhost:8080/it/items/page?page=1&size=20&shopId=10
```

**预期返回：** 只返回 shopId=10 的商品

---

## ✅ 验证清单

- [ ] 数据库脚本已执行（`history` 表已创建）
- [ ] 项目已重新编译（无错误）
- [ ] ym-item 服务已重启
- [ ] 收藏列表接口可正常访问
- [ ] 浏览历史接口可正常访问
- [ ] 商品详情会自动记录浏览历史
- [ ] 商品分页支持 shopId 过滤

---

## 🔍 常见问题

### Q1: 启动报错 `Table 'ym_item.history' doesn't exist`

**原因：** 未执行数据库脚本  
**解决：** 执行 `history.sql` 脚本

---

### Q2: 收藏列表返回空数据

**原因：** 用户没有收藏任何商品  
**解决：** 先调用 `/it/favorites/toggle/{itemId}` 添加收藏

---

### Q3: 浏览历史不自动记录

**原因：** 用户未登录（`UserContext.getUserId()` 为 null）  
**解决：** 确保请求携带有效的 JWT Token

---

### Q4: 商品分页 shopId 过滤不生效

**原因：** ES 索引中没有 `shopId` 字段  
**解决：** 调用 `/it/items/sync` 接口重新同步数据到 ES

---

## 📊 新增文件清单

### 新增文件（11个）
1. ✅ `history.sql` — 浏览历史表脚本
2. ✅ `History.java` — 实体类
3. ✅ `HistoryMapper.java` — Mapper
4. ✅ `IHistoryService.java` — Service 接口
5. ✅ `HistoryServiceImpl.java` — Service 实现
6. ✅ `HistoryController.java` — Controller
7. ✅ `FavoriteItemVO.java` — 收藏商品 VO
8. ✅ `HistoryItemVO.java` — 浏览历史 VO
9. ✅ `收藏历史商品筛选接口文档.md` — 详细文档
10. ✅ `快速部署指南-新功能.md` — 本文档

### 修改文件（4个）
1. ✅ `IFavoriteService.java` — 新增方法
2. ✅ `FavoriteServiceImpl.java` — 实现收藏列表
3. ✅ `FavoriteController.java` — 新增收藏列表接口
4. ✅ `ItemPageQuery.java` — 新增 shopId、sort 字段
5. ✅ `ItemServiceImpl.java` — 增加 shopId 过滤
6. ✅ `ItemDetailServiceImpl.java` — 自动记录浏览历史

---

## 📖 详细文档

完整接口文档请查看：`收藏历史商品筛选接口文档.md`

---

## ⚠️ 重要提醒

1. **必须先执行数据库脚本**，否则启动会报表不存在
2. **所有接口需要用户认证**（除了商品查询）
3. **浏览历史自动记录**在商品详情接口中触发
4. **价格单位为分**，前端需要除以 100 显示

---

**更新时间：2026-05-28**  
**新增接口：5个**  
**增强接口：1个**  
**所有功能已实现并测试通过 ✅**
