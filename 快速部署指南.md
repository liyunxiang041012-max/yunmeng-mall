# 商品收藏 & 店铺功能 - 快速部署指南

## ✅ 已修复的问题

### 编译错误
- ✅ 修复了 `ShopServiceImpl.java` 中的变量作用域问题
- ✅ 修复了 `PageDTO` 方法调用问题（使用 `PageDTO.of()` 静态方法）
- ✅ 所有文件编译通过，无错误

---

## 📋 部署步骤

### 第一步：执行数据库脚本

```bash
# Windows PowerShell
mysql -u root -p ym_item < ym-item\src\main\resources\sql\favorite_shop_follow.sql

# 或者手动执行：
# 1. 登录 MySQL
mysql -u root -p

# 2. 切换到 ym_item 数据库
use ym_item;

# 3. 执行脚本文件
source E:/code/yunmeng-mall-back/ym-item/src/main/resources/sql/favorite_shop_follow.sql
```

**脚本内容：**
- ✅ 创建 `favorite` 表（商品收藏）
- ✅ 创建 `shop_follow` 表（店铺关注）
- ✅ 包含测试数据（已注释，可按需取消注释执行）

---

### 第二步：重新编译项目

```bash
cd E:\code\yunmeng-mall-back
mvn clean package -DskipTests
```

---

### 第三步：重启 ym-item 服务

```bash
# 进入 ym-item 目录
cd ym-item\target

# 启动服务
java -jar ym-item-1.0.0.jar
```

或者在 IDEA 中直接运行 `ItemApplication` 主类。

---

### 第四步：验证接口

#### 4.1 收藏功能测试

```bash
# 1. 切换收藏状态（需要登录token）
POST http://localhost:8082/it/favorites/toggle/1
Authorization: Bearer <your_token>

# 2. 检查收藏状态
GET http://localhost:8082/it/favorites/check/1
Authorization: Bearer <your_token>
```

#### 4.2 店铺功能测试

```bash
# 1. 获取店铺详情（不需要登录）
GET http://localhost:8082/it/shop/1

# 2. 获取店铺商品列表
GET http://localhost:8082/it/shop/1/items?sort=sales&page=1&size=20

# 3. 切换关注店铺（需要登录token）
POST http://localhost:8082/it/shop/follow/toggle/1
Authorization: Bearer <your_token>

# 4. 检查关注状态
GET http://localhost:8082/it/shop/follow/check/1
Authorization: Bearer <your_token>
```

#### 4.3 商品详情测试

```bash
# 获取商品详情（验证 shopId 字段）
GET http://localhost:8082/it/items/1

# 返回结果应包含 shopId 字段
{
  "code": 200,
  "data": {
    "id": 1,
    "shopId": 1,  // ← 新增字段
    "name": "商品名称",
    ...
  }
}
```

---

## 📊 新增表结构

### favorite 表（商品收藏）

```sql
CREATE TABLE `favorite` (
  `id`          bigint NOT NULL AUTO_INCREMENT,
  `user_id`     bigint NOT NULL COMMENT '用户ID',
  `item_id`     bigint NOT NULL COMMENT '商品ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_item` (`user_id`, `item_id`),
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_item_id` (`item_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### shop_follow 表（店铺关注）

```sql
CREATE TABLE `shop_follow` (
  `id`          bigint NOT NULL AUTO_INCREMENT,
  `user_id`     bigint NOT NULL COMMENT '用户ID',
  `shop_id`     bigint NOT NULL COMMENT '店铺ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_shop` (`user_id`, `shop_id`),
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_shop_id` (`shop_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 🔍 常见问题

### Q1: 启动时报 `Table 'ym_item.favorite' doesn't exist`

**原因：** 未执行数据库脚本  
**解决：** 执行 `favorite_shop_follow.sql` 脚本

```bash
mysql -u root -p ym_item < ym-item\src\main\resources\sql\favorite_shop_follow.sql
```

---

### Q2: 调用收藏/关注接口报 `用户未登录` 或 `Field 'user_id' doesn't have a default value`

**原因：** `ym-item` 服务缺少 `WebMvcConfig` 配置类，导致用户认证拦截器未注册  
**解决：** 已自动创建 `WebMvcConfig.java`，重启服务即可

**注意：**
- 收藏和关注接口需要用户认证
- 请求必须经过网关（携带 JWT Token）
- 网关会将用户信息放入请求头，拦截器会自动解析

---

### Q2: 调用接口返回 401 Unauthorized

**原因：** 需要用户认证的接口未携带 Token  
**解决：** 在请求头中添加 `Authorization: Bearer <token>`

---

### Q3: 店铺详情返回空数据

**原因：** 店铺不存在或没有商品  
**解决：** 
1. 检查店铺是否存在：`SELECT * FROM shop WHERE id = ?`
2. 检查店铺是否有商品：`SELECT * FROM item WHERE shop_id = ? AND status = 1`

---

### Q4: PageDTO 返回的 list 为空

**原因：** 查询条件过严（商品必须是上架状态且未删除）  
**解决：** 确认商品 `status = 1` 且 `deleted = 0`

---

## 📝 接口汇总

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/it/favorites/toggle/{itemId}` | ✅ | 切换收藏状态 |
| GET | `/it/favorites/check/{itemId}` | ✅ | 检查收藏状态 |
| GET | `/it/shop/{shopId}` | ❌ | 获取店铺详情 |
| GET | `/it/shop/{shopId}/items` | ❌ | 获取店铺商品列表 |
| POST | `/it/shop/follow/toggle/{shopId}` | ✅ | 切换关注店铺 |
| GET | `/it/shop/follow/check/{shopId}` | ✅ | 检查关注状态 |
| GET | `/it/items/{id}` | ❌ | 获取商品详情（含 shopId） |

---

## 🎯 下一步

1. **执行数据库脚本** ← 最重要！
2. 重新编译项目
3. 重启服务
4. 使用 Postman 或前端页面测试接口
5. 如有问题，查看日志或检查数据库数据

---

## 📖 详细文档

完整文档请查看：`商品收藏店铺功能说明.md`

---

**最后更新：2026-05-28**
