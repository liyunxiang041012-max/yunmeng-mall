# 商家入驻 & 状态检查 接口文档

> 网关: `http://localhost:8080` | 更新时间: 2026-06-18

---

## 完整入驻流程

```
商家注册 → 商家登录 → 检查入驻状态 → 上传头像 → 提交入驻信息 → 进入管理后台
```

| 步骤 | 接口 | 说明 |
|------|------|------|
| 1 | `POST /us/user/shop/register` | 商家注册账号 |
| 2 | `POST /us/user/shop/login` | 商家登录，拿 token |
| 3 | `GET /it/shop/status` | 检查是否已入驻 |
| 4 | `POST /it/shop/upload/avatar` | 上传店铺头像 |
| 5 | `POST /it/shop/entry` | 提交入驻信息 |

---

## 1. 检查入驻状态

商家登录后，前端首先调用此接口判断跳转入驻页还是管理后台。

```
GET /it/shop/status
Authorization: Bearer <token>
```

**响应 - 未入驻**:
```json
{
  "code": 200,
  "data": {
    "hasShop": false
  }
}
```

**响应 - 已入驻**:
```json
{
  "code": 200,
  "data": {
    "hasShop": true,
    "shopId": 1,
    "shopName": "云梦优选"
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| hasShop | Boolean | false→跳转入驻页 / true→进入管理后台 |
| shopId | Long | 店铺ID（已入驻时返回） |
| shopName | String | 店铺名称（已入驻时返回） |

**前端逻辑**:
```js
const res = await axios.get('/it/shop/status');
if (res.data.data.hasShop) {
  // 已入驻 → 进入管理后台
  router.push('/shop/dashboard');
} else {
  // 未入驻 → 跳转入驻页
  router.push('/shop/entry');
}
```

---

## 2. 上传店铺头像

```
POST /it/shop/upload/avatar
Content-Type: multipart/form-data
Authorization: Bearer <token>

file: 图片文件
```

**响应**:
```json
{
  "code": 200,
  "data": "https://oss.xxx/item/2026/06/18/uuid.jpg"
}
```

返回的是 OSS 图片 URL，下一步入驻时把这个 URL 填到 `logo` 字段。

---

## 3. 提交入驻信息

```
POST /it/shop/entry
Content-Type: application/json
Authorization: Bearer <token>

{
  "shopName": "云梦优选",
  "logo": "https://oss.xxx/item/2026/06/18/uuid.jpg",
  "description": "专注好物，品质生活"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| shopName | String | **是** | 店铺名称 |
| logo | String | 否 | 店铺头像URL（调上传接口拿到） |
| description | String | 否 | 店铺描述，最长512字 |

**响应 - 新入驻**:
```json
{
  "code": 200,
  "data": {
    "shopId": 1,
    "userId": 10,
    "shopName": "云梦优选",
    "logo": "https://oss.xxx/item/xxx.jpg",
    "description": "专注好物，品质生活",
    "status": 1,
    "createTime": "2026-06-18 15:30:00",
    "isNew": true
  }
}
```

**响应 - 更新已有店铺**:
```json
{
  "code": 200,
  "data": {
    "shopId": 1,
    "userId": 10,
    "shopName": "云梦优选新名字",
    "logo": "https://oss.xxx/item/new.jpg",
    "description": "更新后的描述",
    "status": 1,
    "createTime": "2026-06-18 12:00:00",
    "isNew": false
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| shopId | Long | 店铺ID |
| userId | Long | 用户ID |
| shopName | String | 店铺名称 |
| logo | String | 店铺头像URL |
| description | String | 店铺描述 |
| status | Integer | 1=营业中 |
| createTime | String | 创建时间 |
| **isNew** | Boolean | **true=新入驻 / false=更新已有** |

> `isNew` 可以用于前端展示不同提示，比如"入驻成功！" vs "信息已更新"。

---

## 完整前端调用示例

```js
// 1. 登录
const loginRes = await axios.post('/us/user/shop/login', {
  account: '13800138000',
  password: '123456'
});
const token = loginRes.data.data.token;

// 设置全局 header
axios.defaults.headers.common['Authorization'] = `Bearer ${token}`;

// 2. 检查入驻状态
const statusRes = await axios.get('/it/shop/status');
if (statusRes.data.data.hasShop) {
  // 已入驻，直接进入后台
  window.location.href = '/shop/dashboard';
  return;
}

// 3. 上传头像
const formData = new FormData();
formData.append('file', fileInput.files[0]);
const uploadRes = await axios.post('/it/shop/upload/avatar', formData);
const logoUrl = uploadRes.data.data;

// 4. 提交入驻
const entryRes = await axios.post('/it/shop/entry', {
  shopName: '我的小店',
  logo: logoUrl,
  description: '这是一家好店'
});

if (entryRes.data.code === 200) {
  const { isNew, shopId } = entryRes.data.data;
  alert(isNew ? '入驻成功！' : '店铺信息已更新');
  window.location.href = '/shop/dashboard';
}
```

---

## 错误码

| code | 说明 |
|------|------|
| 200 | 成功 |
| 401 | 未登录或token过期 |
| 其它 | 仅商家可访问（role不是1） |
