# 商家后台 - 店铺详情 & 仪表盘 & 订单管理 接口文档

> 网关: `http://localhost:8080` | 更新时间: 2026-06-18 | 需商家登录 token

---

## 接口一览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/shop/{shopId}` | 按ID查店铺详情 |
| GET | `/it/shop/stats` | 当前商家仪表盘数据 |
| GET | `/it/shop/orders` | 当前店铺订单列表（分页） |

---

## 1. 店铺详情

```
GET /it/shop/4
Authorization: Bearer <token>
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 4,
    "userId": 10,
    "shopName": "云梦优选",
    "logo": "https://oss.xxx/item/xxx.jpg",
    "description": "专注好物",
    "status": 1,
    "deleted": 0,
    "createTime": "2026-06-18 12:00:00",
    "updateTime": "2026-06-18 15:30:00"
  }
}
```

| 字段 | 说明 |
|------|------|
| status | 1=营业中 0=已关闭 |

---

## 2. 仪表盘数据

```
GET /it/shop/stats
Authorization: Bearer <token>
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "shopId": 4,
    "shopName": "云梦优选",
    "logo": "https://oss.xxx/xxx.jpg",
    "status": 1,
    "totalProducts": 15,
    "activeProducts": 10
  }
}
```

| 字段 | 说明 |
|------|------|
| totalProducts | 全部商品数（含下架） |
| activeProducts | 上架中商品数 |

---

## 3. 店铺订单列表

```
GET /it/shop/orders?page=1&size=10
Authorization: Bearer <token>
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | int | 否 | 页码，默认1 |
| size | int | 否 | 每页条数，默认10 |

**响应**:
```json
{
  "code": 200,
  "data": {
    "total": 25,
    "pages": 3,
    "list": [
      {
        "id": "1718726400000123456",
        "userId": 100,
        "shopId": 4,
        "totalAmount": 19900,
        "payAmount": 0,
        "discountAmount": 0,
        "status": "PENDING_PAYMENT",
        "createTime": "2026-06-18 10:00:00",
        "updateTime": "2026-06-18 10:00:00",
        "payTime": null,
        "expireTime": "2026-06-18 10:30:00"
      }
    ]
  }
}
```

| 字段 | 说明 |
|------|------|
| id | 订单号 |
| totalAmount | 总金额（分） |
| status | PENDING_PAYMENT=待付款 / PAID=已付款 / CANCELLED=已取消 / COMPLETED=已完成 |
| expireTime | 订单过期时间，前端用于倒计时 |

> price 单位为**分**，前端展示时除以 100。

---

## 前端调用示例

```js
// 店铺详情
const { data: shop } = await axios.get('/it/shop/4');

// 仪表盘
const { data: stats } = await axios.get('/it/shop/stats');

// 订单列表
const { data: orders } = await axios.get('/it/shop/orders', {
  params: { page: 1, size: 10 }
});

// 分页判断
if (orders.data.pages > page) {
  // 还有下一页
}
```
