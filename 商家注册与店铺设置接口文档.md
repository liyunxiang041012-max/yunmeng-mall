# 商家注册与店铺设置 — 前后端对接文档

## 一、流程概述

商家入驻分两阶段：

```
阶段1：注册账号（和普通用户注册一样简单）
  POST /it/shop/register  →  只创建用户(role=1)，不创建Shop

阶段2：首次登录后设置店铺信息
  登录 → 判断 needSetup=true → 跳转店铺设置页
  POST /it/shop/setup    →  创建Shop + ShopRating
```

---

## 二、阶段一：商家注册

### 接口

```
POST /it/shop/register
```

> 网关白名单，无需登录。

### 请求体

```json
{
  "shopName": "我的小店",
  "phone": "13800138000",
  "code": "123456",
  "password": "123456qq",
  "nickname": "张三"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|:--:|------|
| phone | String | 是 | 手机号 |
| code | String | 是 | 短信验证码（调用 `/us/user/sendCode` 获取） |
| password | String | 是 | 登录密码 |
| nickname | String | 是 | 昵称 |
| shopName | String | 否 | 店铺名称（阶段二可重填，注册时可不传） |

### 返回值

```json
{ "code": 200, "data": null }
```

### 后端处理逻辑（三种情况）

| 手机号状态 | role | 后端行为 |
|-----------|:----:|---------|
| 新用户 | — | 新建 User(role=1) + Profile，返回 userId |
| 普通用户 | 0 | 升级 role→1，更新 Profile，不重复插入 |
| 已是商家 | 1 | 报错"该手机号已注册为商家，请直接登录" |

> 注册成功后引导用户去登录页登录。

---

## 三、发送验证码

```
POST /us/user/sendCode
```

```json
{ "phone": "13800138000" }
```

> 验证码 Redis key: `sms:code:phone:{手机号}`，有效期 2 分钟。目前测试环境打印在 ym-user 控制台日志中。

---

## 四、阶段二：商家登录与店铺设置

### 4.1 商家登录（专用接口）

```
POST /us/user/shop/login
```

> 网关白名单，无需登录。只有 role=1 的商家才能登录，普通用户会报错。

```json
// 请求
{ "account": "13800138000", "password": "123456qq" }

// 返回
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOi...",
    "role": 1,
    "needSetup": true,
    "avatar": "https://...",
    "nickname": "张三"
  }
}

// 非商家账号尝试登录 →
{ "code": 500, "msg": "该账号不是商家，请使用商家账号登录" }
```

| 字段 | 类型 | 说明 |
|------|------|------|
| token | String | JWT，后续请求 Header: `Authorization: Bearer <token>` |
| role | Integer | 固定为 1（商家） |
| **needSetup** | **Boolean** | **首次登录为 true，前端需判断此字段** |
| avatar | String | 头像URL |
| nickname | String | 昵称 |

### 4.2 查询店铺状态

```
GET /it/shop/status
```

需登录。

```json
// 还没设置店铺 → 返回
{
  "code": 200,
  "data": { "hasShop": false }
}

// 已设置店铺 → 返回
{
  "code": 200,
  "data": { "hasShop": true, "shopId": 10, "shopName": "我的小店" }
}
```

### 4.3 设置店铺信息

```
POST /it/shop/setup
```

需登录。

```json
// 请求
{
  "shopName": "我的数码小店",
  "logo": "https://xxx.com/logo.png",
  "description": "正品保证，品质优选"
}

// 返回
{ "code": 200, "data": null }
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|:--:|------|
| shopName | String | **是** | 店铺名称 |
| logo | String | 否 | 店铺 logo URL |
| description | String | 否 | 店铺描述，最长 512 字 |

> 如果之前已有 Shop 记录，此接口会更新而非报错（幂等）。

---

## 五、前端对接建议流程

```javascript
// 1. 注册
await axios.post('/it/shop/register', {
  phone, code, password, nickname
})
// 成功后 → 跳转商家登录页

// 2. 商家登录（专用接口）
const loginRes = await axios.post('/us/user/shop/login', { account, password })
const { token, role, needSetup } = loginRes.data

// 存储 token
localStorage.setItem('token', token)

// 3. 判断是否需要设置店铺
if (needSetup) {
  const statusRes = await axios.get('/it/shop/status')
  if (!statusRes.data.hasShop) {
    // 跳转店铺设置页
    router.push('/shop/setup')
    return
  }
}

// 4. 正常进入商家后台
router.push('/shop/dashboard')

// 5. 店铺设置页提交
await axios.post('/it/shop/setup', {
  shopName, logo, description
})
// 设置完成 → 跳转商家后台
router.push('/shop/dashboard')
```

---

## 六、完整时序图

```
前端                           网关                        后端服务
 │                              │                            │
 │── POST /it/shop/register ──→│── 白名单放行 ──────────────→│ ym-item 注册用户(role=1)
 │←── { code:200 } ────────────│←───────────────────────────│
 │                              │                            │
 │── POST /us/user/shop/login─→│── 白名单放行 ──────────────→│ ym-user 商家登录
 │←── { token, role:1,         │←───────────────────────────│   校验 role=1，非商家报错
 │       needSetup:true }       │                            │
 │                              │                            │
 │── GET /it/shop/status ─────→│── JWT校验 → user-id ──────→│ ym-item 查 Shop
 │←── { hasShop:false } ───────│←───────────────────────────│
 │                              │                            │
 │   [跳转店铺设置页]            │                            │
 │                              │                            │
 │── POST /it/shop/setup ─────→│── JWT校验 → user-id ──────→│ ym-item 创建 Shop
 │←── { code:200 } ────────────│←───────────────────────────│
 │                              │                            │
 │   [进入商家后台]              │                            │
```
