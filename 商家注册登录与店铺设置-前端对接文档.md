# 商家注册、登录与店铺设置 — 前端对接文档

---

## 一、完整流程

```
商家注册 → 商家登录 → 设置店铺 → 进入商家后台
```

---

## 二、接口列表

### 2.1 发送验证码

```
POST /us/user/sendCode
```

白名单，无需登录。

```json
{ "phone": "13800138000" }
// 返回
{ "code": 200 }
```

---

### 2.2 商家注册

```
POST /it/shop/register
```

白名单，无需登录。

```json
{
  "phone": "13800138000",
  "code": "123456",
  "password": "123456qq",
  "nickname": "张三"
}
// 返回
{ "code": 200 }
```

> 后端处理：如果手机号已有普通账号会自动升级为商家，不会重复创建。

---

### 2.3 商家登录

```
POST /us/user/shop/login
```

白名单，无需登录。**只有 role=1 的账号才能登录，普通用户会报错。**

```json
// 请求
{ "account": "13800138000", "password": "123456qq" }

// 成功返回
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOi...",
    "role": 1,
    "needSetup": true,
    "nickname": "张三",
    "avatar": "https://..."
  }
}

// 普通用户尝试 →
{ "code": 500, "msg": "该账号不是商家，请使用商家账号登录" }
```

| 字段 | 类型 | 说明 |
|------|------|------|
| token | String | JWT，存 localStorage，后续请求 Header 带上 |
| role | Integer | 固定为 1 |
| **needSetup** | Boolean | **首次登录为 true，需跳转店铺设置页** |
| nickname | String | 昵称 |
| avatar | String | 头像 |

---

### 2.4 查询店铺状态

```
GET /it/shop/status
```

需登录（Header: `Authorization: Bearer <token>`）。

```json
// 未设置店铺
{ "code": 200, "data": { "hasShop": false } }

// 已设置店铺
{ "code": 200, "data": { "hasShop": true, "shopId": 10, "shopName": "我的小店" } }
```

---

### 2.5 设置店铺信息

```
POST /it/shop/setup
```

需登录。

```json
// 请求
{
  "shopName": "我的数码小店",
  "logo": "https://xxx.com/logo.png",
  "description": "正品保证，品质优选"
}

// 返回
{ "code": 200 }
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|:--:|------|
| shopName | String | 是 | 店铺名称 |
| logo | String | 否 | 店铺 logo URL |
| description | String | 否 | 店铺描述 |

---

## 三、白名单接口（无需 Token）

| 接口 | 说明 |
|------|------|
| `POST /us/user/sendCode` | 发送验证码 |
| `POST /us/user/shop/login` | 商家登录 |
| `POST /it/shop/register` | 商家注册 |

其他接口必须带 `Authorization: Bearer <token>`，否则返回 401。

---

## 四、前端代码示例

```javascript
// ===== 注册 =====
await axios.post('/it/shop/register', {
  phone, code, password, nickname
})
// 成功 → 跳转商家登录页

// ===== 商家登录 =====
const { data } = await axios.post('/us/user/shop/login', {
  account: phone,
  password
})
localStorage.setItem('token', data.token)

// ===== 判断是否需要设置店铺 =====
if (data.needSetup) {
  const res = await axios.get('/it/shop/status', {
    headers: { Authorization: `Bearer ${data.token}` }
  })
  if (!res.data.hasShop) {
    router.push('/shop/setup')  // 跳转店铺设置页
    return
  }
}
router.push('/shop/dashboard')  // 已有店铺，直接进后台

// ===== 设置店铺提交 =====
await axios.post('/it/shop/setup', {
  shopName, logo, description
}, {
  headers: { Authorization: `Bearer ${token}` }
})
router.push('/shop/dashboard')
```
