# 商家商品管理接口文档

---

## 1. 新增商品

### 接口详情

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /it/shop/item` |
| **网关路由** | `/it/**` → `ym-item`（StripPrefix=1） |
| **后端路径** | `POST /shop/item` |
| **Content-Type** | `application/json` |
| **鉴权** | 需要登录，仅商家（role=1）可访问 |

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `name` | String | 是 | 商品名称 |
| `image` | String | 是 | 商品主图 URL（先调上传接口拿到 OSS URL） |
| `categoryId` | Long | 是 | 分类ID |
| `brandId` | Long | 否 | 品牌ID |
| `price` | Long | 是 | 价格，单位**分**（如 9900 表示 99.00 元） |
| `stock` | Integer | 是 | 库存数量 |

### 请求示例

```json
{
  "name": "iPhone 15",
  "image": "https://xxx.oss-cn-hangzhou.aliyuncs.com/item/2026/06/24/abc.jpg",
  "categoryId": 1,
  "brandId": 1,
  "price": 599900,
  "stock": 100
}
```

### 成功响应

```json
{
  "code": 0,
  "data": {
    "id": 123,
    "shopId": 10,
    "name": "iPhone 15",
    "image": "https://xxx.oss-cn-hangzhou.aliyuncs.com/item/2026/06/24/abc.jpg",
    "categoryId": 1,
    "brandId": 1,
    "price": 599900,
    "stock": 100,
    "sold": 0,
    "status": 0,
    "deleted": 0,
    "createTime": "2026-06-24T12:00:00",
    "updateTime": "2026-06-24T12:00:00"
  }
}
```

### 响应字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | Long | 商品ID（自增） |
| `shopId` | Long | 所属店铺ID（后端自动从当前登录商家获取） |
| `name` | String | 商品名称 |
| `image` | String | 主图 OSS URL |
| `price` | Long | 价格，单位**分** |
| `stock` | Integer | 库存 |
| `sold` | Integer | 销量 |
| `status` | Integer | 0=下架，1=上架（**新增后默认下架**） |
| `deleted` | Integer | 0=正常，1=已删除 |
| `createTime` | String | 创建时间 |
| `updateTime` | String | 更新时间 |

### 失败响应

```json
// 未登录
{ "code": 401, "msg": "请先登录" }

// 未开设店铺
{ "code": 1, "msg": "您还未开设店铺" }
```

### 前端调用示例

```javascript
// 步骤1：先上传图片拿到 OSS URL
const formData = new FormData();
formData.append('file', file);
const uploadRes = await fetch('/it/shop/item/upload/image', {
  method: 'POST',
  body: formData
});
const { data: imageUrl } = await uploadRes.json();

// 步骤2：创建商品
const res = await fetch('/it/shop/item', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    name: '商品名称',
    image: imageUrl,
    categoryId: 1,
    brandId: 1,
    price: 9900,    // 分，99.00元
    stock: 100
  })
});
const { data: item } = await res.json();
// item.id 即为新商品ID
```

### 后端逻辑

- `shopId` 由后端根据当前登录用户的店铺自动赋值，**前端无需传**
- `status` 默认设为 `0`（下架），需通过上架接口手动上架
- `deleted` 默认 `0`
- `createTime` / `updateTime` 自动生成

---

## 2. 编辑商品

| 项目 | 内容 |
|------|------|
| **接口路径** | `PUT /it/shop/item/{itemId}` |
| **请求体** | 同新增（只传需修改的字段即可） |
| **鉴权** | 仅商家，且只能修改自己店铺的商品 |

---

## 3. 删除商品（软删除）

| 项目 | 内容 |
|------|------|
| **接口路径** | `DELETE /it/shop/item/{itemId}` |
| **说明** | 软删除，`deleted` 置为 1 |
| **鉴权** | 仅商家，只能删除自己店铺的商品 |

---

## 4. 上架/下架商品

| 项目 | 内容 |
|------|------|
| **接口路径** | `PUT /it/shop/item/toggle-status/{itemId}` |
| **说明** | 切换上架/下架状态 |
| **鉴权** | 仅商家，只能操作自己店铺的商品 |

---

## 5. 分页查询商品列表

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /it/shop/item/page` |
| **参数** | `page`(默认1), `size`(默认10), `status`(可选), `keyword`(可选) |
| **鉴权** | 仅商家，只返回自己店铺的商品 |

---

## 6. 查看单个商品

| 项目 | 内容 |
|------|------|
| **接口路径** | `GET /it/shop/item/{itemId}` |
| **鉴权** | 仅商家，只能查看自己店铺的商品 |

---

## 完整调用流程

```
1. POST /it/shop/item/upload/image   → 上传图片拿到 OSS URL
2. POST /it/shop/item                → 传入 URL 创建商品（默认下架）
3. PUT  /it/shop/item/toggle-status/{id} → 上架商品
```
