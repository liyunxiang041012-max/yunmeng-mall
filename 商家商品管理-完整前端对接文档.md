# 商家商品管理 - 完整前端对接文档

---

## 一、状态定义

| status | 含义 | 谁可见 |
|--------|------|--------|
| `2` | 审核中 | 仅管理员、商家自己 |
| `1` | 已上架 | 所有用户 |
| `0` | 已下架 | 管理员、商家自己 |

---

## 二、状态流转

```
商家新增商品 → status=2（审核中）
    │
    ├── 管理员 approve  → status=1（已上架，用户可见）
    │       │
    │       └── 商家/管理员下架 → status=0
    │
    └── 管理员 reject   → status=0（已下架）
```

---

## 三、商家端接口（需登录，role=1）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/it/shop/item/upload/image` | 上传商品图片到 OSS |
| POST | `/it/shop/item` | 新增商品（status 自动=2） |
| PUT | `/it/shop/item/{id}` | 编辑商品 |
| DELETE | `/it/shop/item/{id}` | 删除商品（软删除） |
| GET | `/it/shop/item/page` | 分页查询我的商品 |

### 3.1 新增商品

```
POST /it/shop/item
Content-Type: application/json

{
  "name": "商品名称",
  "image": "https://oss-url/xxx.jpg",
  "categoryId": 1,
  "brandId": 1,
  "price": 9900,
  "stock": 100
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | String | 是 | 商品名称 |
| image | String | 是 | 主图 URL（先调上传接口） |
| categoryId | Long | 是 | 分类 ID |
| brandId | Long | 否 | 品牌 ID |
| price | Long | 是 | 价格，单位**分** |
| stock | Integer | 是 | 库存 |

**响应：** 返回完整 Item，`status` 固定为 `2`（审核中）

```json
{ "code": 200, "data": { "id": 123, "status": 2, ... } }
```

### 3.2 上传图片

```
POST /it/shop/item/upload/image
Content-Type: multipart/form-data

file: 图片文件（≤5MB，jpg/png/gif/webp）
```

```json
{ "code": 200, "data": "https://oss-url/xxx.jpg" }
```

---

## 四、管理员审核接口（需登录，role=2）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/shop/admin/item/page` | 全部商品列表 |
| PUT | `/it/shop/admin/item/{id}/approve` | 审核通过 → status=1 |
| PUT | `/it/shop/admin/item/{id}/reject` | 审核驳回 → status=0 |

### 4.1 商品列表

```
GET /it/shop/admin/item/page?page=1&size=10&status=2&keyword=xxx
```

| 参数 | 说明 |
|------|------|
| status | `2`=审核中, `1`=已上架, `0`=已下架, 不传=全部 |

### 4.2 审核通过 / 驳回

```
PUT /it/shop/admin/item/{id}/approve    → status=1
PUT /it/shop/admin/item/{id}/reject     → status=0
```

---

## 五、用户端浏览接口（无需登录）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/items/page` | 商品分页（仅 status=1） |
| GET | `/it/items/{id}` | 商品详情 |
| GET | `/it/categories/top` | 一级分类 |
| GET | `/it/categories/children?parentId=1` | 子分类 |
| GET | `/it/brands/list` | 品牌列表 |

### 5.1 商品分页

```
GET /it/items/page?pageNo=1&pageSize=10&categoryId=1&keyword=xxx&sortBy=price
```

### 5.2 分类级联

```
1. GET /it/categories/top              → 一级分类列表
2. GET /it/categories/children?parentId=1  → 二级分类
3. 有三级继续调 /children?parentId=2
```

### 5.3 品牌列表

```
GET /it/brands/list
```

---

## 六、前端 `admin.js` 汇总

```javascript
// 商家商品管理
POST   /it/shop/item/upload/image         // 上传图片
POST   /it/shop/item                      // 新增商品
PUT    /it/shop/item/{id}                 // 编辑
DELETE /it/shop/item/{id}                 // 删除
GET    /it/shop/item/page                 // 我的商品列表

// 管理员审核
GET    /it/shop/admin/item/page           // 全部商品
PUT    /it/shop/admin/item/{id}/approve   // 审核通过
PUT    /it/shop/admin/item/{id}/reject    // 审核驳回

// 用户浏览（公开）
GET    /it/items/page                     // 商品分页
GET    /it/items/{id}                     // 商品详情
GET    /it/categories/top                 // 一级分类
GET    /it/categories/children?parentId=1 // 子分类
GET    /it/brands/list                    // 品牌列表
```

---

## 七、错误码

| code | 含义 |
|------|------|
| 200 | 成功 |
| 400 | 参数校验失败 |
| 401 | 未登录 |
| 500 | 业务异常 |
