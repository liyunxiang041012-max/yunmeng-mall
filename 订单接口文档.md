# 订单模块接口文档

> 基础路径（经网关）：`/py`
> 
> 完整前缀示例：`http://{host}:8080/py/order/create`
> 
> ⚠️ 金额单位均为 **分**，前端展示需除以 100

## 订单状态枚举

| code | 说明   |
|------|--------|
| 0    | 待付款 |
| 1    | 已支付 |
| 2    | 已发货 |
| 3    | 已完成 |
| 4    | 已取消 |
| 5    | 已退款 |

---

## 1. 创建订单

**POST** `/order/create`

**请求头：**
- `Authorization: Bearer {token}`

**请求体：**
```json
{
  "items": [
    { "skuId": 1, "quantity": 2 },
    { "skuId": 3, "quantity": 1 }
  ],
  "couponId": null
}
```

| 字段              | 类型           | 必填 | 说明                     |
|-------------------|----------------|------|--------------------------|
| items             | Array          | 是   | 订单商品列表             |
| items[].skuId     | Long           | 是   | 商品SKU ID               |
| items[].quantity  | Integer        | 是   | 购买数量（>0）           |
| couponId          | Long           | 否   | 优惠券ID，没有传null     |

**响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": "1748329200123456"
}
```

---

## 2. 查询订单列表

**GET** `/order/list`

**请求头：**
- `Authorization: Bearer {token}`

**响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": [
    {
      "id": "1748329200123456",
      "userId": 1,
      "shopId": 1,
      "totalAmount": 9900,
      "payAmount": 9900,
      "couponId": null,
      "discountAmount": 0,
      "status": 0,
      "createTime": "2026-05-27T10:00:00",
      "updateTime": "2026-05-27T10:00:00",
      "payTime": null
    }
  ]
}
```

| 字段            | 类型          | 说明             |
|-----------------|---------------|------------------|
| id              | String        | 订单号           |
| userId          | Long          | 用户ID           |
| shopId          | Long          | 店铺ID           |
| totalAmount     | Long          | 总金额（分）     |
| payAmount       | Long          | 实付金额（分）   |
| couponId        | Long          | 优惠券ID         |
| discountAmount  | Long          | 优惠金额（分）   |
| status          | Integer       | 订单状态码       |
| createTime      | LocalDateTime | 创建时间         |
| updateTime      | LocalDateTime | 更新时间         |
| payTime         | LocalDateTime | 付款时间         |

---

## 3. 查询订单详情

**GET** `/order/detail/{orderId}`

**请求头：**
- `Authorization: Bearer {token}`

**路径参数：**

| 参数    | 类型   | 说明   |
|---------|--------|--------|
| orderId | String | 订单号 |

**响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "id": "1748329200123456",
    "userId": 1,
    "shopId": 1,
    "totalAmount": 9900,
    "payAmount": 9900,
    "couponId": null,
    "discountAmount": 0,
    "status": 0,
    "createTime": "2026-05-27T10:00:00",
    "updateTime": "2026-05-27T10:00:00",
    "payTime": null
  }
}
```

---

## 4. 取消订单

**PUT** `/order/cancel/{orderId}`

**请求头：**
- `Authorization: Bearer {token}`

**路径参数：**

| 参数    | 类型   | 说明   |
|---------|--------|--------|
| orderId | String | 订单号 |

**响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

## 5. 更新订单状态

**PUT** `/order/status`

**请求头：**
- `Authorization: Bearer {token}`

**请求体：**
```json
{
  "orderId": "1748329200123456",
  "status": 1
}
```

| 字段    | 类型    | 必填 | 说明         |
|---------|---------|------|--------------|
| orderId | String  | 是   | 订单号       |
| status  | Integer | 是   | 目标状态码   |

**响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

## 6. 查询订单明细（商品列表）

**GET** `/order-item/list/{orderId}`

**请求头：**
- `Authorization: Bearer {token}`

**路径参数：**

| 参数    | 类型   | 说明   |
|---------|--------|--------|
| orderId | String | 订单号 |

**响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": [
    {
      "id": 1,
      "orderId": "1748329200123456",
      "userId": 1,
      "skuId": 1,
      "spuId": 1,
      "shopId": 1,
      "name": "商品名称",
      "image": "图片URL",
      "price": 3300,
      "quantity": 3
    }
  ]
}
```

| 字段     | 类型    | 说明           |
|----------|---------|----------------|
| id       | Long    | 明细ID         |
| orderId  | String  | 订单号         |
| userId   | Long    | 用户ID         |
| skuId    | Long    | SKU ID         |
| spuId    | Long    | SPU ID         |
| shopId   | Long    | 店铺ID         |
| name     | String  | 商品名称（快照）|
| image    | String  | 商品图片（快照）|
| price    | Long    | 单价（分）     |
| quantity | Integer | 数量           |

---

> **备注：** userId 从 Token 中自动获取，前端无需传递。

---

# 支付模块接口文档

> 基础路径（经网关）：`/py`
> 
> ⚠️ 金额单位均为 **分**

## 支付状态枚举

| code | 说明   |
|------|--------|
| 0    | 待支付 |
| 1    | 已支付 |
| 2    | 已失败 |
| 3    | 已退款 |
| 4    | 已取消 |

---

## 7. 创建支付单

**POST** `/pay/create`

**请求头：**
- `Authorization: Bearer {token}`

**请求体：**
```json
{
  "orderId": "1748329200123456",
  "payChannel": "WECHAT",
  "amount": 9900,
  "addressId": 1,
  "note": "请尽快发货",
  "couponId": null
}
```

| 字段         | 类型    | 必填 | 说明                                  |
|--------------|---------|------|---------------------------------------|
| orderId      | String  | 是   | 订单号                                |
| payChannel   | String  | 是   | 支付渠道：`WECHAT` / `ALIPAY` / `BANK_CARD` |
| amount       | Long    | 是   | 支付金额（分）                        |
| addressId    | Long    | 是   | 收货地址 ID                           |
| note         | String  | 否   | 订单备注                              |
| couponId     | Long    | 否   | 优惠券 ID                             |

**响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "id": 1,
    "userId": 1,
    "orderId": "1748329200123456",
    "payNo": "PAY1748329200123456",
    "payChannel": "WECHAT",
    "amount": 9900,
    "status": 0,
    "payTime": null,
    "createTime": "2026-05-27T10:00:00",
    "payUrl": "https://pay.yunmengmall.com/pay?payNo=PAY1748329200123456"
  }
}
```

---

## 8. 查询支付记录列表

**GET** `/pay/list`

**请求头：**
- `Authorization: Bearer {token}`

**响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": [
    {
      "id": 1,
      "payNo": "PAY1748329200123456",
      "orderId": "1748329200123456",
      "amount": 9900,
      "payChannel": "WECHAT",
      "status": 0,
      "payTime": null,
      "createTime": "2026-05-27T10:00:00"
    }
  ]
}
```

---

## 9. 查询支付详情

**GET** `/pay/detail/{payId}`

**请求头：**
- `Authorization: Bearer {token}`

**路径参数：**

| 参数  | 类型 | 说明       |
|-------|------|------------|
| payId | Long | 支付记录 ID |

**响应：** 同创建支付单的 data 结构

---

## 10. 取消支付

**PUT** `/pay/cancel/{payId}`

**请求头：**
- `Authorization: Bearer {token}`

**路径参数：**

| 参数  | 类型 | 说明       |
|-------|------|------------|
| payId | Long | 支付记录 ID |

**响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

> **备注：**
> 1. userId 从 Token 中自动获取
> 2. 同一订单重复创建支付单会返回已存在的待支付记录（幂等性）
> 3. 支付单号格式：`PAY + 时间戳 + 6位随机数`

---

## 11. 支付成功回调（模拟）

**POST** `/pay/callback/{payNo}`

**路径参数：**

| 参数  | 类型   | 说明     |
|-------|--------|----------|
| payNo | String | 支付单号 |

**说明：** 模拟第三方支付（微信/支付宝）成功回调，调用后会自动：
1. 更新支付记录状态为 `已支付`
2. 更新关联订单状态为 `已支付`
3. 记录支付时间

**响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

---

## 完整支付流程

```
1. 前端创建订单  →  POST /order/create  →  返回 orderId
2. 前端创建支付  →  POST /pay/create    →  返回 payNo、payUrl
3. 前端跳转支付  →  用户完成支付
4. 支付成功回调  →  POST /pay/callback/{payNo}
5. 后端自动更新  →  支付状态=已支付、订单状态=已支付
6. 前端跳转成功  →  查询订单详情确认
```
