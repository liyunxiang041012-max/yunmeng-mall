# 订单超时取消 & 前端倒计时对接说明

## 机制说明

```
下单成功 → 后端返回 { orderId, expireTime }
                ↓
    前端计算倒计时: expireTime - Date.now()
                ↓
  后端 XXL-JOB 每分钟扫描超时订单 → status 改为 CANCELLED
                ↓
  前端轮询订单列表时发现 status=CANCELLED → 显示"已取消"
```

---

## 1. 创建订单接口变更

### 请求
```
POST /py/order/create
Content-Type: application/json

{ "items": [{ "skuId": 1001, "quantity": 2 }] }
```

### 响应（new! 之前只返回 orderId 字符串）
```json
{
  "code": 200,
  "data": {
    "orderId": "1718726400000123456",
    "expireTime": "2026-06-18 12:30:00"
  }
}
```

**expireTime = createTime + 30分钟（绝对时间）**

---

## 2. 前端倒计时实现

### 核心逻辑

```js
// 下单后拿到的数据
const { orderId, expireTime } = res.data.data;

// 计算剩余秒数
function getRemainingSeconds(expireTime) {
  const expire = new Date(expireTime).getTime();
  const now = Date.now();
  return Math.max(0, Math.floor((expire - now) / 1000));
}

// 格式化倒计时显示: MM:SS
function formatCountdown(seconds) {
  const m = Math.floor(seconds / 60);
  const s = seconds % 60;
  return `${String(m).padStart(2, '0')}:${String(s).padStart(2, '0')}`;
}
```

### Vue3 完整示例

```vue
<template>
  <div class="order-page">
    <div v-if="countdown > 0" class="countdown">
      ⏱ 请在 {{ formatCountdown(countdown) }} 内完成支付
    </div>
    <div v-else class="expired">订单已超时</div>
  </div>
</template>

<script setup>
import { ref, onMounted, onUnmounted } from 'vue';

const expireTime = ref('');  // 从下单接口获取
const countdown = ref(0);
let timer = null;

function getRemainingSeconds(expire) {
  return Math.max(0, Math.floor((new Date(expire).getTime() - Date.now()) / 1000));
}

function formatCountdown(s) {
  const m = Math.floor(s / 60), sec = s % 60;
  return `${String(m).padStart(2, '0')}:${String(sec).padStart(2, '0')}`;
}

onMounted(() => {
  countdown.value = getRemainingSeconds(expireTime.value);
  timer = setInterval(() => {
    countdown.value = getRemainingSeconds(expireTime.value);
    if (countdown.value <= 0) {
      clearInterval(timer);
      // 倒计时归零：刷新订单状态
      refreshOrderStatus();
    }
  }, 1000);
});

onUnmounted(() => clearInterval(timer));
</script>
```

---

## 3. 订单列表中的倒计时

### 响应中已有 expireTime 字段
```json
{
  "code": 200,
  "data": [{
    "id": "1718726400000123456",
    "status": 0,           // 0=待付款
    "totalAmount": 9900,
    "expireTime": "2026-06-18 12:30:00",   // ← 直接用这个
    "createTime": "2026-06-18 12:00:00"
  }]
}
```

### 列表渲染
```js
// 订单列表中判断是否需要倒计时
computed: {
  showCountdown(order) {
    // 只有 PENDING_PAYMENT 状态才显示倒计时
    if (order.status !== 0) return false;
    const remaining = getRemainingSeconds(order.expireTime);
    return remaining > 0;
  },
  countdownText(order) {
    const s = getRemainingSeconds(order.expireTime);
    if (s <= 0) return '已超时';
    const m = Math.floor(s / 60);
    const sec = s % 60;
    return `${m}:${String(sec).padStart(2, '0')} 后取消`;
  }
}
```

---

## 4. 后端配置（提醒运维）

### 数据库执行（一次性）
```sql
ALTER TABLE `order` ADD COLUMN `expire_time` DATETIME NULL COMMENT '订单过期时间' AFTER `pay_time`;
ALTER TABLE `order` ADD INDEX `idx_status_create` (`status`, `create_time`);
```

### XXL-JOB 调度中心配置
| 配置项 | 值 |
|--------|-----|
| 任务名称 | orderTimeoutCancel |
| Cron | `0 * * * * ?`（每分钟执行） |
| 执行器 | ym-pay-executor |
| 路由策略 | 第一个 |

---

## 5. 总结

| 关键点 | 说明 |
|--------|------|
| 下单返回 | `{ orderId, expireTime }` |
| expireTime | 绝对时间，前端直接 `new Date(expireTime)` |
| 倒计时公式 | `expireTime - Date.now()`，归零=超时 |
| 超时取消 | 后端 XXL-JOB 每分钟扫 `status=0 AND createTime<30分钟前` |
| 前端刷新 | 倒计时归零后重新请求订单列表，状态会变成 CANCELLED |
