# 管理端后端接口 - 前端对接文档

> 状态：P0 接口全部完成  
> 日期：2026-06-25

---

## 一、接口总览

| # | 接口 | 方法 | 路径 | 状态 |
|---|------|------|------|------|
| 1 | 概览仪表盘 | GET | `/us/user/admin/overview` | ✅ 真实数据 |
| 2 | 收入趋势 | GET | `/us/user/admin/revenue?period=7` | ✅ 真实数据 |
| 3 | 分类管理 CRUD | — | `/it/categories/admin/*` | ✅ 已有 |
| 4 | 管理员上下架 | PUT | `/it/shop/admin/item/toggle-status/{id}` | ✅ 已有 |
| 5 | 用户管理 | — | `/us/user/admin/*` | ✅ 已有 |
| 6 | 优惠券管理 | — | `/pm/coupons/*` | ✅ 已有 |

---

## 二、概览仪表盘 `GET /us/user/admin/overview`

**权限**：role = 2（管理员），Header 需带 token

**响应示例**：

```json
{
  "code": 200,
  "data": {
    "stats": {
      "gmv":        { "value": "¥12,850", "trend": 0, "delta": "—" },
      "orders":     { "value": "52",       "trend": 0, "delta": "3" },
      "users":      { "value": "25",       "trend": 0, "delta": "23" },
      "conversion": { "value": "—",        "trend": 0, "delta": "—" }
    },
    "userPortrait": {
      "gender": { "male": 60, "female": 40 },
      "ages": [
        { "label": "18-24", "percent": 28, "color": "#D9A53C" },
        { "label": "25-34", "percent": 42, "color": "#1A1712" },
        { "label": "35-44", "percent": 18, "color": "#8C6308" },
        { "label": "45-54", "percent": 8,  "color": "#9B9484" },
        { "label": "55+",   "percent": 4,  "color": "#C0B8A8" }
      ],
      "regions": [
        { "name": "广东", "percent": 35 },
        { "name": "浙江", "percent": 25 }
      ]
    },
    "consumeLevels": [
      { "label": "高消费",   "percent": 55, "color": "#D9A53C" },
      { "label": "中等消费", "percent": 25, "color": "#1A1712" },
      { "label": "低消费",   "percent": 20, "color": "#8C6308" }
    ],
    "recentOrders": [
      {
        "orderNo": "1782361443514405315",
        "product": "华为Mate60 Pro",
        "amount": "6999.0",
        "status": "已付款",
        "statusClass": "paid",
        "time": "12:24"
      }
    ],
    "topProducts": [
      { "name": "—", "sales": "—", "revenue": "—", "color": "#FDE8C8" }
    ]
  }
}
```

**字段说明**：

| 字段 | 来源 | 说明 |
|------|------|------|
| `stats.gmv.value` | 订单表实付金额汇总 | `¥` + 格式化金额，单位为元 |
| `stats.orders.value` | 订单表计数 | 有效订单总数（已付款/已发货/已完成） |
| `stats.orders.delta` | 今日订单数 | 今日新增订单数 |
| `stats.users.value` | 用户表计数 | 注册用户总数 |
| `stats.users.delta` | 启用用户数 | status=1 的用户数 |
| `stats.conversion` | — | 暂未实现，返回 `"—"` |
| `userPortrait.gender` | user_profile 表 gender 字段 | 百分比 |
| `userPortrait.ages` | — | 暂为静态数据 |
| `userPortrait.regions` | user_profile 表 region 字段 | TOP5 省份 |
| `consumeLevels` | — | 暂为静态数据 |
| `recentOrders` | 订单表最近5条 | `statusClass` 枚举：`paid/shipped/pending/done/cancel` |
| `topProducts` | — | 暂未实现，返回占位 |

> `stats.gmv.value` 格式：金额 >= 10000 显示 `¥X.X万`，否则 `¥X,XXX`

---

## 三、收入趋势 `GET /us/user/admin/revenue`

**权限**：role = 2

**请求**：`GET /us/user/admin/revenue?period=7`

| 参数 | 类型 | 说明 |
|------|------|------|
| period | int | 7=7天, 30=30天, 90=季度(按月汇总约12个点) |

**响应**：

```json
{
  "code": 200,
  "data": {
    "values": [4200, 3800, 5100, 4600, 6800, 7200, 12850]
  }
}
```

> `values` 为 int 数组，单位**元**。前端折线图直接用。

---

## 四、分类管理 CRUD

接口前缀 `/it/categories/admin`，均已实现：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/categories/admin/all` | 获取全部未删除分类（平铺列表） |
| POST | `/it/categories/admin` | 新增分类 `{ name, parentId }` |
| PUT | `/it/categories/admin/{id}` | 编辑分类 `{ name, parentId }` |
| DELETE | `/it/categories/admin/{id}` | 级联软删除 |

**平铺列表响应**：

```json
{
  "code": 200,
  "data": [
    { "id": 1, "name": "数码电子", "parentId": 0, "level": 1, "sort": 0, "status": 1 },
    { "id": 2, "name": "手机", "parentId": 1, "level": 2, "sort": 0, "status": 1 }
  ]
}
```

> 前端根据 `parentId` 自行构建树形结构。

---

## 五、管理员商品上下架

```
PUT /it/shop/admin/item/toggle-status/{id}
权限: role = 2
无请求体

逻辑: status=0→1（上架） / status=1→0（下架）
仅对 auditStatus=1（审核通过）的商品生效
```

---

## 六、需重启的服务

| 服务 | 原因 |
|------|------|
| **ym-pay** | 新增了 `/order/admin-*` 三个端点 |
| **ym-user** | 新增了 `@EnableFeignClients` + loadbalancer 依赖，Feign 调 ym-pay |

---

## 七、调用链路

```
前端 AdminDashboard
  │
  ├─ GET /us/user/admin/overview  →  ym-user AdminController
  │     └─ Feign → ym-pay /order/admin-stats     (GMV/订单数)
  │     └─ Feign → ym-pay /order/admin-reAcent    (最近订单)
  │     └─ 本地DB → user / user_profile           (用户统计/性别/地域)
  │
  ├─ GET /us/user/admin/revenue   →  ym-user AdminController
  │     └─ Feign → ym-pay /order/admin-revenue   (收入趋势)
  │
  ├─ /it/categories/admin/*       →  ym-item CategoryAdminController
  └─ /it/shop/admin/item/toggle-status/{id} → ym-item ItemAdminController
```
