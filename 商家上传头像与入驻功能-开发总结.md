# 商家上传头像与入驻功能 - 开发总结

## 功能概述

本次开发完成了两个核心接口：
1. **店铺头像上传接口** - 支持商家上传店铺logo/头像
2. **商家入驻接口** - 商家填写店铺名称、头像URL和描述完成入驻

---

## 新增文件清单

### 1. 工具类
- `ym-item/src/main/java/com/liyun/item/utils/FileUploadUtils.java`
  - 文件上传工具类
  - 支持图片格式验证（jpg, png, gif, webp）
  - 文件大小限制（最大5MB）
  - 自动生成唯一文件名

### 2. DTO类
- `ym-item/src/main/java/com/liyun/item/domain/dto/ShopEntryDTO.java`
  - 商家入驻请求参数
  - 包含：shopName（必填）、logo、description

### 3. 文档
- `商家上传头像与入驻接口-前端对接文档.md`
  - 详细的接口说明
  - 前端调用示例（Vue3 + Axios, React + Fetch）
  - 完整流程示例
  - 常见问题解答

- `商家上传头像与入驻接口-测试指南.md`
  - Postman/Apifox 测试配置
  - cURL 测试命令
  - 常见错误说明

---

## 修改文件清单

### 1. Controller层
**文件**: `ym-item/src/main/java/com/liyun/item/controller/ShopController.java`

**新增接口**:
```java
// 上传店铺头像
@PostMapping("/upload/avatar")
public Result<String> uploadAvatar(@RequestParam("file") MultipartFile file)

// 商家入驻
@PostMapping("/entry")
public Result entry(@Valid @RequestBody ShopEntryDTO dto)
```

### 2. Service接口
**文件**: `ym-item/src/main/java/com/liyun/item/service/IShopService.java`

**新增方法**:
```java
String uploadShopAvatar(MultipartFile file);
void entryShop(Long userId, ShopEntryDTO dto);
```

### 3. Service实现
**文件**: `ym-item/src/main/java/com/liyun/item/service/impl/ShopServiceImpl.java`

**新增实现**:
- `uploadShopAvatar()` - 调用文件上传工具类
- `entryShop()` - 处理商家入驻逻辑（新建或更新店铺）
- `saveShopAndRatingForEntry()` - 保存店铺和评分记录

### 4. Feign客户端
**文件**: `ym-api/src/main/java/com/liyun/api/client/ShopFeign.java`

**新增接口**（供内部服务调用参考）:
```java
@PostMapping(value = "/shop/upload/avatar", consumes = MediaType.MULTIPART_FORM_DATA_VALUE)
String uploadShopAvatar(@RequestPart("file") MultipartFile file);
```

### 5. 配置文件
**文件**: `ym-item/src/main/resources/application.yaml`

**新增配置**:
```yaml
spring:
  servlet:
    multipart:
      max-file-size: 5MB        # 单个文件最大大小
      max-request-size: 10MB    # 整个请求最大大小
```

---

## 接口详细说明

### 接口1: 上传店铺头像

**基本信息**:
- **路径**: `POST /it/shop/upload/avatar`
- **Content-Type**: `multipart/form-data`
- **权限**: 仅商家可访问 (role=1)

**请求参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | File | 是 | 图片文件 |

**响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": "/api/uploads/shop/1718726400000/a1b2c3d4e5f6.jpg"
}
```

**特性**:
- ✅ 文件格式验证（jpg, png, gif, webp）
- ✅ 文件大小限制（5MB）
- ✅ 自动生成唯一文件名（UUID + 时间戳）
- ✅ 按日期分目录存储

---

### 接口2: 商家入驻

**基本信息**:
- **路径**: `POST /it/shop/entry`
- **Content-Type**: `application/json`
- **权限**: 仅商家可访问 (role=1)

**请求参数**:
```json
{
  "shopName": "云梦优选小店",      // 必填
  "logo": "/api/uploads/...",      // 可选
  "description": "店铺描述"         // 可选，最多512字
}
```

**响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

**特性**:
- ✅ 自动校验店铺名称不能为空
- ✅ 支持新建店铺和更新已有店铺
- ✅ 自动创建店铺评分记录
- ✅ 事务保证数据一致性

---

## 业务流程

### 完整入驻流程

```
1. 商家登录 (/us/user/shop/login)
   ↓
2. 检查 needSetup 字段
   ↓ (如果需要入驻)
3. 上传店铺头像 (/it/shop/upload/avatar)
   ↓
4. 获取头像URL
   ↓
5. 提交入驻信息 (/it/shop/entry)
   {
     shopName: "店铺名称",
     logo: "头像URL",
     description: "店铺描述"
   }
   ↓
6. 入驻成功，跳转到商家后台
```

---

## 技术要点

### 1. 文件上传
- 使用 Spring Boot 内置的 MultipartFile
- 本地文件系统存储（生产环境建议接入OSS）
- UUID 生成唯一文件名避免冲突
- 按时间戳分目录便于管理

### 2. 权限控制
- 通过 `checkShopRole()` 方法校验用户角色
- 确保只有 role=1 的商家可以访问
- 从 UserContext 获取当前用户ID

### 3. 数据一致性
- 使用 `@Transactional` 保证事务
- 同时创建 Shop 和 ShopRating 记录
- 失败时自动回滚

### 4. 错误处理
- 统一的异常消息返回
- 友好的错误提示（文件格式、大小等）
- 日志记录关键操作

---

## 注意事项

### 开发注意事项
1. **文件存储路径**: 当前使用本地文件系统，生产环境应接入阿里云OSS/腾讯云COS等
2. **CDN加速**: 图片URL应配置CDN域名提升访问速度
3. **图片压缩**: 建议前端上传前进行图片压缩
4. **Token管理**: 所有接口都需要携带有效的JWT Token

### 前端对接注意事项
1. **表单验证**: 前端应先验证必填字段再提交
2. **上传进度**: 大文件上传建议显示进度条
3. **图片预览**: 上传成功后可以预览头像效果
4. **错误处理**: 统一处理网络错误和业务错误

### 安全注意事项
1. **文件类型白名单**: 严格限制可上传的文件类型
2. **文件大小限制**: 防止恶意上传大文件
3. **文件名随机化**: 防止文件名注入攻击
4. **路径遍历防护**: 使用固定目录存储

---

## 测试建议

### 单元测试
- [ ] 测试文件上传（正常文件、超大文件、不支持格式）
- [ ] 测试入驻流程（新建店铺、更新店铺）
- [ ] 测试权限校验（非商家访问）

### 集成测试
- [ ] 完整的入驻流程测试
- [ ] 并发上传测试
- [ ] 异常场景测试

### 性能测试
- [ ] 大文件上传性能
- [ ] 并发上传压力测试

---

## 后续优化建议

### 短期优化
1. 接入阿里云OSS或腾讯云COS存储图片
2. 添加图片压缩功能（后端或前端）
3. 添加图片裁剪功能
4. 支持批量上传图片

### 长期优化
1. 接入CDN加速图片访问
2. 添加图片审核机制（敏感内容检测）
3. 支持多种尺寸的图片生成（缩略图、中图、大图）
4. 添加图片水印功能

---

## 相关文档

- [前端对接文档](./商家上传头像与入驻接口-前端对接文档.md)
- [测试指南](./商家上传头像与入驻接口-测试指南.md)
- [API接口文档](./API接口文档.md)

---

**开发完成时间**: 2026-06-18  
**开发人员**: AI Assistant  
**文档版本**: v1.0
