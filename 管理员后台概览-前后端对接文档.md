# 管理员后台概览接口 — 前后端对接文档

> 接口：`GET /us/user/admin/overview`
> 更新时间：2026-06-25

---

## 一、后端改动清单（共5个文件）

| 文件 | 改动内容 |
|------|---------|
| `ym-api/.../client/OrderFeign.java` | ① `getAdminRecentOrders()` 返回类型 `List`→`Map`（修复Feign反序列化bug）<br>② 新增 `getAdminTopProducts()` |
| `ym-pay/.../service/IOrderService.java` | 新增 `getAdminTopProducts()` 接口声明 |
| `ym-pay/.../service/impl/OrderServiceImpl.java` | ① `getAdminRecentOrders()` 补充 `user`(买家昵称) 字段<br>② 实现 `getAdminTopProducts()`：按order_item聚合销量/销售额TOP5 |
| `ym-pay/.../controller/OrderController.java` | 新增 `GET /order/admin-top-products` 端点 |
| `ym-user/.../service/impl/AdminServiceImpl.java` | ① 年龄段从 `user_profile.birthday` 真实计算<br>② `topProducts` 调Feign取真实数据<br>③ `consumeLevels` 根据人均GMV动态计算<br>④ 性别兜底值从58/42改为0/0 |

---

## 二、完整返回结构

```json
{
  "code": 200,
  "data": {
    "stats": {
      "gmv":       { "value": "¥0",        "trend": 0, "delta": "—" },
      "orders":    { "value": "0",         "trend": 0, "delta": "0" },
      "users":     { "value": "11",        "trend": 0, "delta": "11" },
      "conversion":{ "value": "—",         "trend": 0, "delta": "—" }
    },

    "userPortrait": {
      "gender": {
        "male": 65,
        "female": 35
      },
      "ages": [
        { "label": "18-24", "percent": 25, "color": "#D9A53C" },
        { "label": "25-34", "percent": 40, "color": "#1A1712" },
        { "label": "35-44", "percent": 20, "color": "#8C6308" },
        { "label": "45-54", "percent": 10, "color": "#9B9484" },
        { "label": "55+",   "percent": 5,  "color": "#C0B8A8" }
      ],
      "regions": [
        { "name": "广东", "percent": 35 },
        { "name": "北京", "percent": 25 }
      ]
    },

    "consumeLevels": [
      { "label": "高消费",   "percent": 10, "color": "#D9A53C" },
      { "label": "中等消费", "percent": 30, "color": "#1A1712" },
      { "label": "低消费",   "percent": 60, "color": "#8C6308" }
    ],

    "recentOrders": [
      {
        "orderNo":     "1782361443514405315",
        "user":        "张三",
        "product":     "华为Mate60 Pro",
        "amount":      "6999.0",
        "status":      "已付款",
        "statusClass": "paid",
        "time":        "12:24:00"
      }
    ],

    "topProducts": [
      {
        "name":    "华为Mate60 Pro",
        "sales":   "5",
        "revenue": "¥34995.00",
        "color":   "#FDE8C8"
      },
      {
        "name":    "iPhone 15",
        "sales":   "3",
        "revenue": "¥20997.00",
        "color":   "#DBEAFE"
      }
    ]
  }
}
```

---

## 三、字段说明

### stats（统计卡片）

| 字段 | 类型 | 说明 |
|------|------|------|
| `gmv.value` | String | 格式化金额，如 `¥0` / `¥1,234` / `¥1.2万` |
| `gmv.trend` | Number | 趋势百分比（暂为0） |
| `gmv.delta` | String | 变化量文本（暂为"—"） |
| `orders.value` | String | 总订单数（字符串，前端可转Number） |
| `orders.delta` | String | 今日订单数 |
| `users.value` | String | 总用户数 |
| `users.delta` | String | 活跃用户数（status=1） |
| `conversion` | Object | 转化率（暂为"—"占位） |

### userPortrait（用户画像）

| 字段 | 类型 | 说明 |
|------|------|------|
| `gender.male` | Number | 男性百分比（基于user_profile.gender统计） |
| `gender.female` | Number | 女性百分比 |
| `ages` | Array | **已改为真实数据**，基于 `user_profile.birthday` 计算年龄分段百分比 |
| `ages[].label` | String | 年龄段：`18-24` / `25-34` / `35-44` / `45-54` / `55+` |
| `ages[].percent` | Number | 该年龄段百分比 |
| `ages[].color` | String | 前端直接用 |
| `regions` | Array | 省份TOP5，基于 `user_profile.region` 拆分统计 |

### consumeLevels（消费水平）

| 字段 | 类型 | 说明 |
|------|------|------|
| `label` | String | `高消费` / `中等消费` / `低消费` |
| `percent` | Number | **已改为动态计算**：人均消费>¥500→55/25/20，>¥100→30/40/30，否则→10/30/60 |
| `color` | String | 前端直接用 |

### recentOrders（最近订单）

| 字段 | 类型 | 说明 |
|------|------|------|
| `orderNo` | String | 订单号（雪花ID） |
| `user` | String | **已补充**，买家昵称 |
| `product` | String | 第一个订单项的商品名 |
| `amount` | String | 实付金额(元)，字符串格式如 `"6999.0"` |
| `status` | String | 中文状态：待付款/已付款/已发货/已完成/已取消 |
| `statusClass` | String | CSS类名：`pending`/`paid`/`shipped`/`done`/`cancel` |
| `time` | String | 订单时间，格式 `HH:mm:ss` |

> **空数据兜底**：`[{"orderNo":"—","user":"—","product":"—","amount":"—","status":"—","statusClass":"pending","time":"—"}]`

### topProducts（热销商品）

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | String | 商品名 |
| `sales` | String | 累计销量（字符串） |
| `revenue` | String | 累计销售额(元)，`¥`前缀+两位小数 |
| `color` | String | 后端补上，前端直接用 |

> **空数据兜底**：5条占位 `{"name":"—","sales":"—","revenue":"—"}` + colors

---

## 四、网关路由确认 ✅

```
/us/**  →  lb://ym-user  →  StripPrefix=1
```

`/us/user/admin/overview` → 网关去前缀 `/us` → ym-user 收到 `/user/admin/overview` → AdminController.overview()。路由正确，无需改动。

---

## 五、收入趋势接口

```
GET /us/user/admin/revenue?period=7
```

返回 `{"values":[0,0,0,0,0,0,0]}` 是因为当前数据库无已支付订单。创建并支付一笔订单后自动显示真实金额（单位：元）。

---

## 六、需要重启的服务

| 服务 | 原因 |
|------|------|
| **ym-pay** | OrderServiceImpl新增字段+getAdminTopProducts() |
| **ym-user** | AdminServiceImpl新增computeAgeDistribution()+Feign调用 |

`ym-api` 只改了接口声明，不独立部署，随调用方生效。

---

## 七、之前遗留的bug修复

| Bug | 根因 | 修复方式 |
|-----|------|---------|
| `recentOrders`永远显示`"—"` | OrderFeign声明返回`List<Map>`，但Controller实际返回`Result<List<Map>>`包裹着`{code,data,msg}`，JSON根节点是Object不是Array，Feign反序列化`List`直接报ClassCastException，被catch吞掉 | 改为返回`Map<String,Object>`，由extractDataList从`data`字段提取List |
| `topProducts`永远显示`"—"` | 硬编码占位数据，根本没查订单表 | 新增getAdminTopProducts()查询order_item聚合 |
| `userPortrait.ages`永远显示固定值28/42/18/8/4 | 硬编码 | 从user_profile.birthday真实计算 |
| 性别无数据时显示58%/42% | 兜底值不合理 | 改为0%/0% |
| 消费水平永远显示55/25/20 | 硬编码 | 根据人均GMV动态估算 |
