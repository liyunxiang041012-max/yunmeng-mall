# 关注 & 收藏 — 前端对接文档

## 一、API 清单

> 所有接口需要登录，请求头携带 `Authorization: Bearer <token>`
> 基础地址：`http://192.168.150.101:8084`

---

### 1. 店铺关注

| 功能 | 方法 | 路径 |
|------|------|------|
| 关注/取消关注 | `POST` | `/shop/follow/toggle/{shopId}` |
| 检查关注状态 | `GET` | `/shop/follow/check/{shopId}` |
| **我的关注列表** | **`GET`** | **`/shop/follow/my`** |

#### 1.1 关注 / 取消关注

```
POST /shop/follow/toggle/{shopId}
```

**路径参数：** `shopId` — 店铺ID

**返回值：**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": true    // true=已关注，false=已取消关注
}
```

---

#### 1.2 检查关注状态

```
GET /shop/follow/check/{shopId}
```

**返回值：**
```json
{
  "code": 200,
  "data": true    // true=已关注，false=未关注
}
```

---

#### 1.3 我的关注列表 🆕

```
GET /shop/follow/my
```

**返回值：**
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "shopId": 1,
      "shopName": "云梦小店",
      "logo": "https://picsum.photos/200/300?random=1",
      "description": "专注好物推荐",
      "createTime": "2026-05-31 12:00:00"
    },
    {
      "id": 2,
      "shopId": 2,
      "shopName": "品质生活馆",
      "logo": "https://picsum.photos/200/300?random=2",
      "description": "精选优质商品",
      "createTime": "2026-05-30 10:30:00"
    }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | Long | 关注记录ID |
| `shopId` | Long | 店铺ID（可跳转店铺页） |
| `shopName` | String | 店铺名称 |
| `logo` | String | 店铺Logo URL |
| `description` | String | 店铺描述 |
| `createTime` | String | 关注时间 `yyyy-MM-dd HH:mm:ss` |

---

### 2. 商品收藏

| 功能 | 方法 | 路径 |
|------|------|------|
| 收藏/取消收藏 | `POST` | `/favorites/toggle/{itemId}` |
| 检查收藏状态 | `GET` | `/favorites/check/{itemId}` |
| **我的收藏列表** | **`GET`** | **`/favorites/my?page=1&size=10`** |

#### 2.1 收藏 / 取消收藏

```
POST /favorites/toggle/{itemId}
```

**路径参数：** `itemId` — 商品ID

**返回值：**
```json
{
  "code": 200,
  "data": true    // true=已收藏，false=已取消收藏
}
```

---

#### 2.2 检查收藏状态

```
GET /favorites/check/{itemId}
```

**返回值：**
```json
{
  "code": 200,
  "data": true
}
```

---

#### 2.3 我的收藏列表

```
GET /favorites/my?page=1&size=10
```

**请求参数：**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `page` | Integer | 否 | 1 | 页码 |
| `size` | Integer | 否 | 10 | 每页条数 |

**返回值：**
```json
{
  "code": 200,
  "data": {
    "total": 3,
    "pages": 1,
    "current": 1,
    "size": 10,
    "records": [
      {
        "id": 3,
        "itemId": 3,
        "name": "无线蓝牙耳机",
        "itemName": "无线蓝牙耳机",
        "mainImage": "https://example.com/img3.jpg",
        "image": "https://example.com/img3.jpg",
        "price": 19900,
        "salesCount": 520,
        "createTime": "2026-05-29 18:20:00"
      }
    ]
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | Long | 收藏记录ID |
| `itemId` | Long | 商品ID（可跳转商品详情页） |
| `name` / `itemName` | String | 商品名称 |
| `mainImage` / `image` | String | 商品主图URL |
| `price` | Long | 价格（**单位：分**，需 ÷100 显示） |
| `salesCount` | Integer | 销量 |
| `createTime` | String | 收藏时间 |

---

## 二、前端需要做的事

### 关注模块
- [ ] 店铺详情页：[关注] 按钮 → 调用 `POST /shop/follow/toggle/{shopId}`，根据返回值切换按钮状态
- [ ] 店铺详情页：[已关注] / [取消关注] 文案跟随 toggle 返回值变化
- [ ] 我的页面 → 我的关注：调用 `GET /shop/follow/my`，展示店铺列表
- [ ] 点击店铺可跳转到店铺详情页（`/shop/{shopId}`）

### 收藏模块
- [ ] 商品详情页：收藏按钮 → 调用 `POST /favorites/toggle/{itemId}`
- [ ] 商品详情页：进入时调用 `GET /favorites/check/{itemId}` 查询初始状态
- [ ] 我的页面 → 我的收藏：调用 `GET /favorites/my`，分页展示
- [ ] 价格显示时需要 `÷100` 转元
- [ ] 点击商品可跳转到商品详情页（`/item/{itemId}`）

---

## 三、测试数据 SQL

执行以下 SQL 插入测试数据（先确认用户ID=1、商品ID=1/2/3、店铺ID=1/2 存在）：

```sql
USE ym_item;

-- 收藏测试数据
INSERT INTO `favorite` (`user_id`, `item_id`) VALUES (1, 1);
INSERT INTO `favorite` (`user_id`, `item_id`) VALUES (1, 2);
INSERT INTO `favorite` (`user_id`, `item_id`) VALUES (1, 3);

-- 关注测试数据
INSERT INTO `shop_follow` (`user_id`, `shop_id`) VALUES (1, 1);
INSERT INTO `shop_follow` (`user_id`, `shop_id`) VALUES (1, 2);
```
