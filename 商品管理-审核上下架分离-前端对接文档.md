# 商品管理 - 前端对接文档（审核+上下架分离版）

---

## 字段说明

| 字段 | 含义 | 谁控制 |
|------|------|--------|
| `auditStatus` | **审核状态**: 0=待审核, 1=通过, 2=驳回 | 仅管理员 |
| `status` | **上下架**: 0=下架, 1=上架 | 商家（审核通过后可自由切换） |

---

## 完整流程图

```
商家新增商品 → auditStatus=0（待审核）, status=0（下架）
    │
    ├── 管理员 approve → auditStatus=1（审核通过）
    │       │
    │       ├── 商家上架 → status=1（用户可见）
    │       └── 商家下架 → status=0
    │
    └── 管理员 reject → auditStatus=2（审核驳回）
```

---

## 一、商家端（role=1）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/it/shop/item/upload/image` | 上传图片 |
| POST | `/it/shop/item` | 新增商品 |
| PUT | `/it/shop/item/{id}` | 编辑 |
| DELETE | `/it/shop/item/{id}` | 删除 |
| PUT | `/it/shop/item/toggle-status/{id}` | **上架/下架** |
| GET | `/it/shop/item/page` | 我的商品列表 |

### 新增商品

```
POST /it/shop/item
{ "name":"xx", "image":"url", "categoryId":1, "brandId":1, "price":9900, "stock":100 }
```

返回：`{ "id":123, "auditStatus":0, "status":0, ... }`

### 上架/下架

```
PUT /it/shop/item/toggle-status/{id}   → status 在 0↔1 之间切换
```

审核通过后商家随时可操作，不依赖管理员。

---

## 二、管理员审核（role=2）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/shop/admin/item/page?auditStatus=0` | 待审核列表 |
| GET | `/it/shop/admin/item/page` | 全部商品 |
| PUT | `/it/shop/admin/item/{id}/approve` | 审核通过 → auditStatus=1 |
| PUT | `/it/shop/admin/item/{id}/reject` | 审核驳回 → auditStatus=2 |

### 查待审核

```
GET /it/shop/admin/item/page?page=1&size=10
```

响应 data.list 中 `auditStatus=0` 即为待审核。可传 `?auditStatus=0` 筛选。

### 审核操作

```
PUT /it/shop/admin/item/{id}/approve   → auditStatus=1
PUT /it/shop/admin/item/{id}/reject    → auditStatus=2
```

---

## 三、用户端（无需登录）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/items/page` | 商品分页（仅 auditStatus=1 且 status=1） |
| GET | `/it/items/{id}` | 商品详情 |
| GET | `/it/categories/top` | 一级分类 |
| GET | `/it/categories/children?parentId=1` | 子分类 |
| GET | `/it/brands/list` | 品牌列表 |

---

## 四、admin.js 路由

```javascript
// 商家
POST   /it/shop/item/upload/image
POST   /it/shop/item
PUT    /it/shop/item/{id}
DELETE /it/shop/item/{id}
PUT    /it/shop/item/toggle-status/{id}
GET    /it/shop/item/page

// 管理员审核
GET    /it/shop/admin/item/page
PUT    /it/shop/admin/item/{id}/approve
PUT    /it/shop/admin/item/{id}/reject

// 用户浏览（公开）
GET    /it/items/page
GET    /it/items/{id}
GET    /it/categories/top
GET    /it/categories/children?parentId=1
GET    /it/brands/list
```

---

## 五、数据库

执行 `ym-item/src/main/resources/sql/alter_item_audit_status.sql`：

```sql
ALTER TABLE item ADD COLUMN audit_status TINYINT DEFAULT 0
  COMMENT '审核状态: 0=待审核, 1=审核通过, 2=审核驳回' AFTER status;
UPDATE item SET audit_status = 1;  -- 存量数据默认已通过
```

部署后需执行 `GET /it/items/sync` 刷新 ES 索引。
