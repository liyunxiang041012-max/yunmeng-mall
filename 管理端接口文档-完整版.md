# 云梦商城 — 管理端完整接口文档

> **网关地址**: `http://localhost:8080`
> **权限要求**: 管理员登录（role = 2），所有接口需在 Header 携带 `Authorization: Bearer <admin_token>`
> **响应格式**: 统一 `{ "code": 200, "message": "操作成功", "data": {...}, "timestamp": 1719200000000 }`

---

## 目录

1. [管理员登录](#1-管理员登录)
2. [仪表盘](#2-仪表盘)
3. [用户管理](#3-用户管理)
4. [系统通知](#4-系统通知)
5. [商品审核](#5-商品审核)
6. [商家管理](#6-商家管理)
7. [分类管理](#7-分类管理)
8. [秒杀活动管理](#8-秒杀活动管理)
9. [认证配置指引](#9-认证配置指引)

---

## 1. 管理员登录

### POST `/us/user/admin/login`

管理员通过账号密码登录，获取 JWT token。

**请求体**:

```json
{
  "account": "admin",
  "password": "123456"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| account | String | 是 | 用户名 或 手机号 |
| password | String | 是 | 密码（BCrypt 加密存储） |

**成功响应** (code=200):

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "role": 2,
    "needSetup": false
  }
}
```

| 字段 | 说明 |
|------|------|
| token | JWT 令牌，后续接口须放入 `Authorization: Bearer <token>` |
| role | 固定为 `2`（管理员） |
| needSetup | 管理员固定 `false` |

**错误响应**:

| code | message | 触发条件 |
|------|---------|----------|
| 1002 | 用户不存在 | 账号不存在 |
| 500 | 密码错误 | 密码不正确 |
| 500 | 该账号不是管理员 | role ≠ 2 |
| 500 | 该账号已被禁用 | status = 0 |

---

## 2. 仪表盘

### 2.1 GET `/us/user/admin/overview`

管理员后台首页概览数据，聚合 GMV、订单、用户画像、热销商品等信息。

**权限**: role = 2

**响应**:

```json
{
  "code": 200,
  "data": {
    "stats": {
      "gmv":        { "value": "¥12,850", "trend": 0, "delta": "—" },
      "orders":     { "value": "52",      "trend": 0, "delta": "3" },
      "users":      { "value": "25",      "trend": 0, "delta": "23" },
      "conversion": { "value": "—",       "trend": 0, "delta": "—" }
    },
    "userPortrait": {
      "gender": { "male": 60, "female": 40 },
      "ages": [
        { "label": "18-24", "percent": 28, "color": "#D9A53C" },
        { "label": "25-34", "percent": 42, "color": "#1A1712" },
        { "label": "35-44", "percent": 18, "color": "#8C6308" },
        { "label": "45-54", "percent": 8,  "color": "#9B9484" },
        { "label": "55+",   "percent": 4,  "color": "#C0B8A8" }
      ],
      "regions": [
        { "name": "广东", "percent": 35 },
        { "name": "北京", "percent": 25 }
      ]
    },
    "consumeLevels": [
      { "label": "高消费",   "percent": 10, "color": "#D9A53C" },
      { "label": "中等消费", "percent": 30, "color": "#1A1712" },
      { "label": "低消费",   "percent": 60, "color": "#8C6308" }
    ],
    "recentOrders": [
      {
        "orderNo":     "1782361443514405315",
        "user":        "张三",
        "product":     "华为Mate60 Pro",
        "amount":      "6999.0",
        "status":      "已付款",
        "statusClass": "paid",
        "time":        "12:24"
      }
    ],
    "topProducts": [
      { "name": "华为Mate60 Pro", "sales": "5", "revenue": "¥34995.00", "color": "#FDE8C8" },
      { "name": "iPhone 15",    "sales": "3", "revenue": "¥20997.00", "color": "#DBEAFE" }
    ]
  }
}
```

**字段说明**:

| 路径 | 类型 | 说明 |
|------|------|------|
| stats.gmv.value | String | `¥` + 格式化金额（≥1万显示 `¥X.X万`） |
| stats.orders.value | String | 有效订单总数 |
| stats.orders.delta | String | 今日订单数 |
| stats.users.value | String | 注册用户总数 |
| stats.users.delta | String | 启用用户数（status=1） |
| stats.conversion | Object | 暂为占位 `"—"` |
| userPortrait.gender | Object | 基于 user_profile.gender 统计 |
| userPortrait.ages | Array | 基于 user_profile.birthday 计算 5 个年龄段百分比 |
| userPortrait.regions | Array | 省份 TOP5 |
| consumeLevels | Array | 基于人均 GMV 动态估算（高/中/低消费） |
| recentOrders | Array | 最近 5 笔订单，statusClass: `pending`/`paid`/`shipped`/`done`/`cancel` |
| topProducts | Array | 销量 TOP5 商品（按 order_item 聚合） |

---

### 2.2 GET `/us/user/admin/revenue`

按天统计收入趋势，供折线图使用。

**权限**: role = 2

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| period | int | 否 | `7`=7天, `30`=30天, `90`=季度（按月汇总），默认 `7` |

**响应**:

```json
{
  "code": 200,
  "data": {
    "values": [4200, 3800, 5100, 4600, 6800, 7200, 12850]
  }
}
```

> `values` 为 `int[]`，单位**元**。

---

## 3. 用户管理

> 服务: ym-user | Controller: `AdminController` | 基础路径: `/user/admin` | 网关: `/us/user/admin`

### 3.1 GET `/us/user/admin/page`

分页查询用户列表（含头像）。

**权限**: role = 2

**请求参数**:

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | int | 否 | 1 | 页码 |
| size | int | 否 | 10 | 每页条数 |
| keyword | String | 否 | — | 搜索（用户名/手机号/昵称模糊） |
| role | int | 否 | — | 角色筛选: `0`=用户, `1`=商家, `2`=管理员 |
| status | int | 否 | — | 状态筛选: `0`=禁用, `1`=启用 |

**响应**:

```json
{
  "code": 200,
  "data": {
    "total": 25,
    "pages": 3,
    "list": [
      {
        "id": 1,
        "username": "admin",
        "phone": "13800000001",
        "nickname": "管理员",
        "role": 2,
        "status": 1,
        "avatar": "https://oss.xxx.com/avatar/1.jpg",
        "createTime": "2026-05-07T10:00:00",
        "updateTime": "2026-06-25T12:00:00"
      }
    ]
  }
}
```

### 3.2 GET `/us/user/admin/{userId}`

查询单个用户详情。

**权限**: role = 2

**路径参数**: `userId` — 用户 ID（雪花 Long 类型）

**响应**:

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "username": "admin",
    "phone": "13800000001",
    "nickname": "管理员",
    "role": 2,
    "status": 1,
    "avatar": "https://oss.xxx.com/avatar/1.jpg",
    "createTime": "2026-05-07T10:00:00",
    "updateTime": "2026-06-25T12:00:00",
    "gender": 1,
    "birthday": "1990-01-01",
    "region": "广东省广州市",
    "experience": 1200
  }
}
```

> 若用户不存在，返回 `{"code":500,"message":"用户不存在"}`。

### 3.3 PUT `/us/user/admin/{userId}/status`

启用或禁用用户。

**权限**: role = 2

**路径参数**: `userId` — 用户 ID

**请求体**:

```json
{
  "status": 0
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| status | int | 是 | `0`=禁用, `1`=启用 |

**响应**: `{"code":200,"message":"操作成功"}`

### 3.4 PUT `/us/user/admin/{userId}/role`

修改用户角色。

**权限**: role = 2

**路径参数**: `userId` — 用户 ID

**请求体**:

```json
{
  "role": 1
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| role | int | 是 | `0`=普通用户, `1`=商家, `2`=管理员 |

**响应**: `{"code":200,"message":"操作成功"}`

---

## 4. 系统通知

> 服务: ym-user | Controller: `SystemNoticeController` | 基础路径: `/user/admin/notice` | 网关: `/us/user/admin/notice`

### 4.1 POST `/us/user/admin/notice`

管理员向指定角色群体发送系统通知。

**权限**: role = 2

**请求体**:

```json
{
  "title": "系统维护通知",
  "content": "今晚22:00-24:00系统维护，届时服务不可用",
  "targetRole": -1
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | String | 是 | 通知标题，1-100 字符 |
| content | String | 是 | 通知详情 |
| targetRole | int | 否 | 目标角色: `-1`=全部用户（默认）, `0`=普通用户, `1`=商家, `2`=管理员 |

**响应**: `{"code":200,"message":"操作成功"}`

**错误响应**:

| message | 触发条件 |
|---------|----------|
| 标题不能为空 | title 为空 |
| 标题不超过100字 | title 超长 |
| 内容不能为空 | content 为空 |
| 仅管理员可访问此接口 | role ≠ 2 |

---

## 5. 商品审核

> 服务: ym-item | Controller: `ItemAdminController` | 基础路径: `/shop/admin/item` | 网关: `/it/shop/admin/item`

### 状态字段说明

| 字段 | 值 | 含义 |
|------|-----|------|
| auditStatus | `0` | 待审核 |
| auditStatus | `1` | 审核通过 |
| auditStatus | `2` | 审核驳回 |
| status | `0` | 已下架 |
| status | `1` | 已上架 |

> **审核与上下架独立**: 审核通过后可单独执行上下架操作。仅 `auditStatus=1` 的商品允许上下架。

### 5.1 GET `/it/shop/admin/item/page`

分页查询所有商品（含店铺名），支持 auditStatus、status、keyword 筛选。

**权限**: role = 2

**请求参数**:

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | int | 否 | 1 | 页码 |
| size | int | 否 | 10 | 每页条数 |
| auditStatus | int | 否 | — | 审核: `0`=待审核, `1`=通过, `2`=驳回 |
| status | int | 否 | — | 上下架: `0`=下架, `1`=上架 |
| keyword | String | 否 | — | 商品名模糊搜索 |

**常用组合**:

```
GET /it/shop/admin/item/page?auditStatus=0              → 待审核列表
GET /it/shop/admin/item/page?auditStatus=0&keyword=手机   → 待审核 + 搜索
GET /it/shop/admin/item/page?auditStatus=1&status=1      → 已通过 + 已上架
GET /it/shop/admin/item/page?page=1&size=20              → 全部商品，第 1 页 20 条
```

**响应**:

```json
{
  "code": 200,
  "data": {
    "total": 5,
    "pages": 1,
    "list": [
      {
        "id": 1,
        "shopId": 10,
        "name": "华为Mate60 Pro",
        "image": "https://oss.xxx.com/item/1.jpg",
        "price": 699900,
        "stock": 100,
        "status": 0,
        "auditStatus": 0,
        "createTime": "2026-06-24T12:00:00"
      }
    ]
  }
}
```

### 5.2 GET `/it/shop/admin/item/{itemId}`

查看单个商品详情。

**权限**: role = 2

**响应**: 返回完整 Item 实体。若不存在则 `{"code":500,"message":"商品不存在"}`。

### 5.3 PUT `/it/shop/admin/item/{itemId}/approve`

审核通过 → `auditStatus = 1`。

**权限**: role = 2  
**响应**: `{"code":200,"message":"操作成功"}`

### 5.4 PUT `/it/shop/admin/item/{itemId}/reject`

审核驳回 → `auditStatus = 2`。

**权限**: role = 2  
**响应**: `{"code":200,"message":"操作成功"}`

### 5.5 PUT `/it/shop/admin/item/toggle-status/{itemId}`

管理员上下架商品。

**权限**: role = 2  
**逻辑**: `status=0→1`（上架），`status=1→0`（下架）  
**限制**: 仅 `auditStatus=1`（审核通过）的商品可操作  
**响应**: `{"code":200,"message":"操作成功"}`

---

## 6. 商家管理

> 服务: ym-item | Controller: `ShopAdminController` | 基础路径: `/shop/admin` | 网关: `/it/shop/admin`

### 6.1 GET `/it/shop/admin/page`

分页查询商家列表。

**权限**: role = 2

**请求参数**:

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | int | 否 | 1 | 页码 |
| size | int | 否 | 10 | 每页条数 |
| keyword | String | 否 | — | 店铺名搜索 |
| status | int | 否 | — | 状态筛选: `0`=关闭, `1`=营业 |

**响应**:

```json
{
  "code": 200,
  "data": {
    "total": 8,
    "pages": 1,
    "list": [
      {
        "id": 10,
        "shopName": "华为官方旗舰店",
        "logo": "https://oss.xxx.com/shop/10/logo.png",
        "description": "华为官方授权店铺",
        "status": 1,
        "userId": 5,
        "createTime": "2026-06-20T10:00:00",
        "updateTime": "2026-06-25T12:00:00"
      }
    ]
  }
}
```

### 6.2 GET `/it/shop/admin/{shopId}`

查询单个商家详情。

**权限**: role = 2  
**响应**: 返回完整 Shop 实体。不存在则 `{"code":500,"message":"店铺不存在"}`。

### 6.3 PUT `/it/shop/admin/{shopId}`

编辑商家信息。

**权限**: role = 2

**请求体**:

```json
{
  "shopName": "新店名",
  "logo": "https://oss.xxx.com/shop/10/new-logo.png",
  "description": "更新的店铺描述"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| shopName | String | 否 | 店铺名称 |
| logo | String | 否 | Logo URL |
| description | String | 否 | 店铺描述 |

**响应**: `{"code":200,"message":"操作成功"}`

### 6.4 DELETE `/it/shop/admin/{shopId}`

删除商家（软删除）。

**权限**: role = 2  
**响应**: `{"code":200,"message":"操作成功"}`

### 6.5 PUT `/it/shop/admin/toggle-status/{shopId}`

开关商家营业状态。

**权限**: role = 2  
**逻辑**: 来回切换 `status` 值  
**响应**: 返回更新后的 Shop 对象。

```json
{
  "code": 200,
  "data": {
    "id": 10,
    "shopName": "华为官方旗舰店",
    "status": 0
  }
}
```

---

## 7. 分类管理

> 服务: ym-item | Controller: `CategoryAdminController` | 基础路径: `/categories/admin` | 网关: `/it/categories/admin`

### 7.1 GET `/it/categories/admin/all`

获取全部分类（平铺列表，不含已删除）。

**权限**: role = 2

**响应**:

```json
{
  "code": 200,
  "data": [
    { "id": 1, "name": "数码电子", "parentId": 0, "level": 1, "sort": 0, "status": 1 },
    { "id": 2, "name": "手机",     "parentId": 1, "level": 2, "sort": 0, "status": 1 },
    { "id": 3, "name": "电脑",     "parentId": 1, "level": 2, "sort": 0, "status": 1 },
    { "id": 4, "name": "服装",     "parentId": 0, "level": 1, "sort": 0, "status": 1 }
  ]
}
```

> `parentId = 0` 为一级分类。返回**平铺列表**，前端根据 `parentId` 自行构建树形结构。

### 7.2 POST `/it/categories/admin`

新增分类。

**权限**: role = 2

**请求体**:

```json
{
  "name": "图书",
  "parentId": 0
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | String | 是 | 分类名称，2-50 字符 |
| parentId | Long | 否 | 父分类 ID，默认 `0`（一级分类） |

**成功响应**:

```json
{
  "code": 200,
  "data": { "id": 5, "name": "图书", "parentId": 0, "level": 1, "sort": 0 }
}
```

**错误校验**:

| 错误信息 |
|----------|
| 分类名称不能为空 |
| 分类名称长度需在 2-50 字符之间 |
| 父分类不存在 |
| 该分类名称已存在 |

### 7.3 PUT `/it/categories/admin/{id}`

编辑分类。

**权限**: role = 2

**请求体**: 同新增 `{ "name": "新名称", "parentId": 0 }`

**额外校验**:
- 不能将分类的 parentId 设为自己
- 不能将分类移到其子孙分类下（防止循环引用）

### 7.4 DELETE `/it/categories/admin/{id}`

删除分类（级联软删除子分类）。

**权限**: role = 2  
**机制**: 软删除（`deleted = 1`），自动级联删除所有子孙分类。  
**响应**: `{"code":200,"message":"操作成功"}`

---

## 8. 秒杀活动管理

> 服务: ym-promotion | Controller: `FlashSaleAdminController` | 网关: `/pm/admin/flash-sales`

### 8.1 POST `/pm/admin/flash-sales`

创建秒杀活动。

**权限**: role = 2

**请求体**: `FlashSale` 对象（含活动时间、名称等），通过 `items` 参数绑定秒杀商品列表。

### 8.2 PUT `/pm/admin/flash-sales/{id}/start`

开始秒杀活动 → `status = 2`。

**权限**: role = 2  
**错误**: 活动不存在返回 `{"code":500,"message":"活动不存在"}`

### 8.3 PUT `/pm/admin/flash-sales/{id}/end`

结束秒杀活动 → `status = 3`。

**权限**: role = 2

---

## 9. 认证配置指引

> 本节适配 AI 接口测试平台的认证配置。

### Token 模式 (推荐)

适合已有 token 的场景，直接粘贴即可。

| 配置项 | 值 |
|--------|-----|
| 认证方式 | `Token` |
| Token | `<管理员登录后获取的 token>` |
| Header 名称 | `Authorization` |
| 前缀 | `Bearer ` |

**最终注入**: `Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...`

### 账号登录模式 (自动获取 Token)

平台自动调登录接口获取 token，适合 token 过期频繁的场景。

| 配置项 | 值 |
|--------|-----|
| 认证方式 | `账号登录` |
| 登录 URL | `http://localhost:8080/us/user/admin/login` |
| 账号 | `admin` |
| 密码 | `123456` |
| 请求体 | `{"account":"username","password":"password"}` |
| Token 路径 | `data.token` |
| Header 名称 | `Authorization` |
| 前缀 | `Bearer ` |

### 验证方法

1. 完成认证配置后，执行一个需鉴权的接口（如 `GET /us/user/admin/overview`）
2. 若返回 `code: 200` 且 `data` 有内容 → 配置正确
3. 若返回 `401` → Token 过期或 Header 不对
4. 若返回 `500: 仅管理员可访问此接口` → 账号 role ≠ 2

---

## 附录: 接口路由总表

| 方法 | 网关路径 | 服务 | 说明 |
|------|----------|------|------|
| **登录** ||||
| POST | `/us/user/admin/login` | ym-user | 管理员登录 |
| **仪表盘** ||||
| GET | `/us/user/admin/overview` | ym-user | 仪表盘概览 |
| GET | `/us/user/admin/revenue` | ym-user | 收入趋势 |
| **用户管理** ||||
| GET | `/us/user/admin/page` | ym-user | 用户分页列表 |
| GET | `/us/user/admin/{id}` | ym-user | 用户详情 |
| PUT | `/us/user/admin/{id}/status` | ym-user | 启用/禁用用户 |
| PUT | `/us/user/admin/{id}/role` | ym-user | 修改用户角色 |
| **系统通知** ||||
| POST | `/us/user/admin/notice` | ym-user | 发送系统通知 |
| **商品审核** ||||
| GET | `/it/shop/admin/item/page` | ym-item | 商品分页列表 |
| GET | `/it/shop/admin/item/{id}` | ym-item | 商品详情 |
| PUT | `/it/shop/admin/item/{id}/approve` | ym-item | 审核通过 |
| PUT | `/it/shop/admin/item/{id}/reject` | ym-item | 审核驳回 |
| PUT | `/it/shop/admin/item/toggle-status/{id}` | ym-item | 上下架 |
| **商家管理** ||||
| GET | `/it/shop/admin/page` | ym-item | 商家列表 |
| GET | `/it/shop/admin/{id}` | ym-item | 商家详情 |
| PUT | `/it/shop/admin/{id}` | ym-item | 编辑商家 |
| DELETE | `/it/shop/admin/{id}` | ym-item | 删除商家 |
| PUT | `/it/shop/admin/toggle-status/{id}` | ym-item | 开关营业状态 |
| **分类管理** ||||
| GET | `/it/categories/admin/all` | ym-item | 全部分类 |
| POST | `/it/categories/admin` | ym-item | 新增分类 |
| PUT | `/it/categories/admin/{id}` | ym-item | 编辑分类 |
| DELETE | `/it/categories/admin/{id}` | ym-item | 删除分类 |
| **秒杀管理** ||||
| POST | `/pm/admin/flash-sales` | ym-promotion | 创建秒杀活动 |
| PUT | `/pm/admin/flash-sales/{id}/start` | ym-promotion | 开始秒杀 |
| PUT | `/pm/admin/flash-sales/{id}/end` | ym-promotion | 结束秒杀 |

---

## 附录: 网关路由映射

| 网关前缀 | 目标服务 | StripPrefix | 示例 |
|----------|---------|-------------|------|
| `/us/**` | `lb://ym-user` | 1 | `/us/user/admin/page` → ym-user `/user/admin/page` |
| `/it/**` | `lb://ym-item` | 1 | `/it/shop/admin/item/page` → ym-item `/shop/admin/item/page` |
| `/pm/**` | `lb://ym-promotion` | 1 | `/pm/admin/flash-sales` → ym-promotion `/admin/flash-sales` |
