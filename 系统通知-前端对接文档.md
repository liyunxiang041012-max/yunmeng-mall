# 系统通知 - 前端对接文档

---

## 表设计（放在 `ym_user` 库）

| 表 | 说明 |
|----|------|
| `system_notice` | 通知内容（标题+详情+发送者） |
| `user_notice` | 用户已读记录 |

---

## 接口清单

| 角色 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 管理员 | POST | `/us/user/admin/notice` | 发送通知 |
| 用户 | GET | `/us/user/notice/page` | 通知列表（含已读状态） |
| 用户 | PUT | `/us/user/notice/{id}/read` | 标记已读 |
| 用户 | GET | `/us/user/notice/unread` | 未读数量 |

---

## 1. 管理员发送通知

```
POST /us/user/admin/notice
Content-Type: application/json
```

**请求体：**

```json
{
  "title": "系统维护通知",
  "content": "今晚22:00-24:00系统维护，届时无法访问",
  "targetRole": -1
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `title` | String | 是 | 标题，≤100字 |
| `content` | String | 是 | 通知详情 |
| `targetRole` | int | 否 | -1=全部(默认), 0=普通用户, 1=商家, 2=管理员 |

**成功响应：**

```json
{ "code": 200, "message": "操作成功", "data": null }
```

---

## 2. 用户通知列表

```
GET /us/user/notice/page?page=1&size=10
```

**响应：**

```json
{
  "code": 200,
  "data": {
    "total": 5,
    "size": 10,
    "current": 1,
    "records": [
      {
        "id": 3,
        "title": "系统维护通知",
        "content": "今晚22:00-24:00系统维护，届时无法访问",
        "createTime": "2026-06-24T12:00:00",
        "isRead": false
      }
    ]
  }
}
```

| 字段 | 说明 |
|------|------|
| `isRead` | `true`=已读, `false`=未读 |
| `content` | 通知详情文本 |

---

## 3. 用户标记已读

```
PUT /us/user/notice/{noticeId}/read
```

```json
{ "code": 200, "message": "操作成功", "data": null }
```

---

## 4. 用户未读数量

```
GET /us/user/notice/unread
```

```json
{ "code": 200, "data": 3 }
```

---

## 前端 `admin.js` 路径

```javascript
// 系统通知 - 管理员
POST   /us/user/admin/notice          // 发送通知 {title, content, targetRole}

// 系统通知 - 用户
GET    /us/user/notice/page           // 通知列表 ?page=1&size=10
PUT    /us/user/notice/{noticeId}/read // 标记已读
GET    /us/user/notice/unread         // 未读数量
```

---

## 数据库初始化

执行 `ym-user/src/main/resources/sql/notice.sql` 建表。

---

## 后端文件清单

| 文件 | 说明 |
|------|------|
| `sql/notice.sql` | 建表 SQL |
| `domain/pojo/SystemNotice.java` | 通知实体 |
| `domain/pojo/UserNotice.java` | 已读记录实体 |
| `domain/dto/NoticeSaveDTO.java` | 请求 DTO |
| `mapper/SystemNoticeMapper.java` | Mapper |
| `mapper/UserNoticeMapper.java` | Mapper |
| `service/ISystemNoticeService.java` | 服务接口 |
| `service/impl/SystemNoticeServiceImpl.java` | 服务实现 |
| `controller/SystemNoticeController.java` | 控制器 |
