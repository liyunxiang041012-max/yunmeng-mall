# 商家端 - 登录 & 商品管理 接口文档

> 网关: `http://localhost:8080` | 更新时间: 2026-06-18

---

## 0. 商家登录（获取令牌）

所有商品管理接口都需要登录，先调登录拿 token。

```
POST /us/user/shop/login
Content-Type: application/json

{
  "account": "13800138000",
  "password": "123456"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| account | String | 是 | 手机号或用户名 |
| password | String | 是 | 密码 |

**响应**:
```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "role": 1,
    "needSetup": true
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| token | String | JWT令牌，后续所有请求都要带 |
| role | Integer | 1=商家 |
| needSetup | Boolean | 是否需要设置店铺信息 |

**后续请求携带方式**：
```
Authorization: Bearer <token>
```

**完整登录流程**：
```
1. POST /us/user/shop/login  →  拿到 token + needSetup
2. 如果 needSetup=true  →  跳转入驻页，调 POST /it/shop/entry 设置店铺
3. 之后所有请求带 Authorization: Bearer <token>
```

> **注意**：登录必须用 `/us/user/shop/login`（商家专用），不能用 `/us/user/login`（普通用户），否则 role 不对会被拦截。

---

## 商品管理接口

> 路径前缀: `/it/shop/item` | 需商家登录 (role=1) | 只能操作自己店铺的商品

---

## 接口一览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/shop/item/page` | 分页列表（搜索/状态筛选） |
| GET | `/it/shop/item/{itemId}` | 商品详情 |
| POST | `/it/shop/item` | 新增商品 |
| PUT | `/it/shop/item/{itemId}` | 编辑商品 |
| DELETE | `/it/shop/item/{itemId}` | 删除商品（软删除） |
| PUT | `/it/shop/item/toggle-status/{itemId}` | 上架 / 下架 |

---

## 1. 分页查询我的商品

```
GET /it/shop/item/page?page=1&size=10&status=1&keyword=手机
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | int | 否 | 页码，默认 1 |
| size | int | 否 | 每页条数，默认 10 |
| status | int | 否 | 1=上架 0=下架，不传=全部 |
| keyword | String | 否 | 按商品名称模糊搜索 |

**响应**:
```json
{
  "code": 200,
  "data": {
    "total": 15,
    "pages": 2,
    "list": [{
      "id": 101,
      "shopId": 1,
      "categoryId": 5,
      "brandId": 3,
      "name": "无线蓝牙耳机",
      "image": "https://oss.xxx/item/xxx.jpg",
      "price": 19900,
      "stock": 50,
      "sold": 12,
      "status": 1,
      "deleted": 0,
      "createTime": "2026-06-18 10:00:00",
      "updateTime": "2026-06-18 15:00:00"
    }]
  }
}
```

**price 单位是「分」**，前端展示时除以 100。

---

## 2. 商品详情

```
GET /it/shop/item/101
```

响应同上 `list` 中的单个对象。

---

## 3. 新增商品

```
POST /it/shop/item
Content-Type: application/json

{
  "name": "无线蓝牙耳机",
  "image": "https://oss.xxx/item/xxx.jpg",
  "categoryId": 5,
  "brandId": 3,
  "price": 19900,
  "stock": 100
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | String | 是 | 商品名称 |
| image | String | 否 | 商品主图URL |
| categoryId | Long | 否 | 分类ID |
| brandId | Long | 否 | 品牌ID |
| price | Long | 是 | 价格（分） |
| stock | Integer | 是 | 库存数量 |

**注意**：新增后默认**下架**状态（status=0），需要手动点击"上架"。

**响应**:
```json
{ "code": 200, "data": { "id": 101, "status": 0, ... } }
```

---

## 4. 编辑商品

```
PUT /it/shop/item/101
Content-Type: application/json

{
  "name": "新款蓝牙耳机",
  "image": "https://oss.xxx/new.jpg",
  "price": 15900,
  "stock": 200
}
```

字段都是**可选的**，传哪个改哪个。没传的字段保持原值。

**响应**: `{ "code": 200, "data": null }`

---

## 5. 删除商品（软删除）

```
DELETE /it/shop/item/101
```

只标记 `deleted=1`，数据库里还在。列表查询自动过滤掉。

**响应**: `{ "code": 200, "data": null }`

---

## 6. 上架 / 下架

```
PUT /it/shop/item/toggle-status/101
```

每次调用切换一次：上架→下架，下架→上架。

**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 101,
    "status": 1,
    ...其他字段
  }
}
```

---

## 前端页面建议

### 列表页
- 状态筛选 Tab：全部 / 上架中 / 已下架 → 传 `status` 参数
- 搜索框 → 传 `keyword` 参数
- 表格列：图片缩略图、商品名称、价格（÷100 显示元）、库存、状态标签、操作
- 操作：编辑、上架/下架、删除

### 新增/编辑弹窗（共用一个表单）
- 商品名称（必填）
- 图片上传（调已有上传接口拿URL）
- 价格输入（前端*100转分发给后端）
- 库存输入
- 分类选择
- 品牌选择

### 状态切换
```js
// status=0(下架) 显示灰色 按钮文字"上架"
// status=1(上架) 显示绿色 按钮文字"下架"
await axios.put(`/it/shop/item/toggle-status/${itemId}`);
```
