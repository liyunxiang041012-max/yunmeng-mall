# 优惠券支付 + 秒杀服务 - 前端对接文档

> 最后更新：2026-06-25

---

## 一、优惠券支付流程

### 1.1 用户下单使用优惠券

```
POST /api/orders  (创建订单)
```

**请求体：**
```json
{
  "items": [
    { "skuId": 1001, "quantity": 2 },
    { "skuId": 1002, "quantity": 1 }
  ],
  "couponId": 8880001
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| items | Array | 是 | 商品列表 |
| items[].skuId | Long | 是 | SKU ID |
| items[].quantity | Integer | 是 | 数量 |
| couponId | Long | 否 | 用户券ID（user_coupon表的id），不传则不用券 |

**响应：**
```json
{
  "code": 200,
  "data": {
    "orderId": "1719301234567123456",
    "expireTime": "2026-06-25 12:00:00"
  }
}
```

**折扣计算规则（后端自动）：**

| 类型 | discountType | 说明 | 示例 |
|------|-------------|------|------|
| 每满减 | 1 | 每满X减Y，上不封顶 | 每满200减30，订单600 → 减90 |
| 折扣 | 2 | 折扣率(discountValue=80即8折) | 8折，最高减50 → 实减50 |
| 无门槛 | 3 | 直接减X | 无门槛减10 |
| 满减 | 4 | 满X减Y | 满100减20 |

> `payAmount = totalAmount - discountAmount`，订单查询时前端展示 `payAmount` 为实付金额

---

## 二、优惠券完整 API

### 2.1 平台管理员 - 优惠券管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/coupons` | 新增优惠券 |
| GET | `/coupons/page` | 分页查询优惠券 |
| GET | `/coupons/{id}` | 查询优惠券详情 |
| PUT | `/coupons/{id}` | 更新优惠券 |
| DELETE | `/coupons/{id}` | 删除优惠券 |
| PUT | `/coupons/{id}/issue` | 开始发放 |
| PUT | `/coupons/{id}/pause` | 暂停发放 |

**新增优惠券请求体：**
```json
{
  "name": "满100减20",
  "discountType": 4,
  "specific": false,
  "discountValue": 2000,
  "thresholdAmount": 10000,
  "maxDiscountAmount": 0,
  "obtainWay": 1,
  "issueBeginTime": "2026-06-01 00:00:00",
  "issueEndTime": "2026-12-31 23:59:59",
  "termDays": 30,
  "totalNum": 5000,
  "userLimit": 1
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| name | String | 优惠券名称 |
| discountType | Integer | 1=每满减, 2=折扣, 3=无门槛, 4=满减 |
| specific | Boolean | 是否限定使用范围 |
| discountValue | Integer | 折扣值（分为单位，如2000=20元；折扣类型时80=8折） |
| thresholdAmount | Integer | 门槛金额（分），0=无门槛 |
| maxDiscountAmount | Integer | 最高优惠金额（分），0=不限制 |
| obtainWay | Integer | 获取方式：1=手动领取, 2=兑换码 |
| issueBeginTime | DateTime | 发放开始时间 |
| issueEndTime | DateTime | 发放结束时间 |
| termDays | Integer | 有效期天数（0=用termBeginTime/termEndTime指定） |
| totalNum | Integer | 发行总量 |
| userLimit | Integer | 每人限领数量 |

### 2.2 用户端 - 领券/查券/用券

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/coupons/list` | 查询发放中的优惠券列表（可领） |
| POST | `/user-coupons/{couponId}/receive` | 领取优惠券 |
| POST | `/user-coupons/{code}/exchange` | 兑换码兑换 |
| GET | `/user-coupons/page?status=1` | 查询我的优惠券（status: 1=未使用,2=已使用,3=已过期） |
| POST | `/user-coupons/{id}/use?orderAmount=15000` | 使用优惠券（支付时调用） |

**查询我的优惠券响应：**
```json
{
  "code": 200,
  "data": {
    "total": 5,
    "pages": 1,
    "list": [
      {
        "id": 8880001,
        "name": "满100减20",
        "discountType": 4,
        "discountValue": 2000,
        "thresholdAmount": 10000,
        "maxDiscountAmount": 0,
        "specific": false,
        "status": 1,
        "termEndTime": "2026-07-25 12:00:00"
      }
    ]
  }
}
```

---

## 三、秒杀服务 API

### 3.1 管理员 - 秒杀管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/admin/flash-sales` | 创建秒杀活动 |
| PUT | `/admin/flash-sales/{id}/start` | 开始活动 |
| PUT | `/admin/flash-sales/{id}/end` | 结束活动 |

**创建秒杀活动请求体：**
```json
{
  "flashSale": {
    "name": "618秒杀专场",
    "startTime": "2026-06-18 10:00:00",
    "endTime": "2026-06-18 12:00:00"
  },
  "items": [
    {
      "skuId": 5001,
      "spuId": 2001,
      "flashPrice": 9900,
      "stock": 100,
      "limitPerUser": 1,
      "sort": 0
    }
  ]
}
```

### 3.2 用户端 - 秒杀

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/flash-sales` | 查询当前进行中的秒杀活动及商品 |
| GET | `/flash-sales/items/{itemId}` | 查询秒杀商品详情 |
| POST | `/flash-sales/items/{itemId}/order` | 秒杀下单 |

**秒杀活动列表响应：**
```json
{
  "code": 200,
  "data": [
    {
      "id": 100,
      "name": "618秒杀专场",
      "startTime": "2026-06-18 10:00:00",
      "endTime": "2026-06-18 12:00:00",
      "items": [
        {
          "id": 1,
          "skuId": 5001,
          "spuId": 2001,
          "flashPrice": 9900,
          "stock": 95,
          "sold": 5,
          "limitPerUser": 1
        }
      ]
    }
  ]
}
```

**秒杀下单流程：**
1. 前端调用 `POST /flash-sales/items/{itemId}/order`
2. 后端 Redis 分布式锁扣库存
3. 成功返回 `{"code": 200}` 
4. 失败（库存不足）返回 `{"code": 500, "msg": "秒杀库存不足"}`

---

## 四、数据库表

### 秒杀活动表 (flash_sale)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 活动ID |
| name | VARCHAR(100) | 活动名称 |
| start_time | DATETIME | 开始时间 |
| end_time | DATETIME | 结束时间 |
| status | INT | 1=未开始, 2=进行中, 3=已结束 |

### 秒杀商品表 (flash_sale_item)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| flash_sale_id | BIGINT | 活动ID |
| sku_id | BIGINT | SKU ID |
| spu_id | BIGINT | SPU ID |
| flash_price | BIGINT | 秒杀价格（分） |
| stock | INT | 秒杀库存 |
| sold | INT | 已售数量 |
| limit_per_user | INT | 每人限购 |

---

## 五、接口汇总

| 模块 | 路径前缀 | 说明 |
|------|---------|------|
| 优惠券管理 | `/coupons` | 管理员 CRUD |
| 用户优惠券 | `/user-coupons` | 领取/兑换/查询/使用 |
| 秒杀管理 | `/admin/flash-sales` | 管理员创建/启停 |
| 用户秒杀 | `/flash-sales` | 用户浏览/下单 |

> 所有金额单位统一为**分**（前端展示时 ÷ 100）
