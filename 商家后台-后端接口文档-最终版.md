# 商家后台 - 后端接口文档（最终版）

> 更新时间: 2026-06-18 | 网关: `http://localhost:8080` | 需商家登录 token

---

## 接口一览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/shop/status` | 检查入驻状态 |
| POST | `/it/shop/entry` | 入驻/更新店铺 |
| POST | `/it/shop/upload/avatar` | 上传店铺头像 |
| GET | `/it/shop/{shopId}` | 店铺详情 |
| GET | `/it/shop/stats` | 仪表盘数据 |
| GET | `/it/shop/stats/revenue` | 收入趋势 |
| GET | `/it/shop/orders` | 订单列表（分页+筛选） |
| GET | `/it/shop/orders/{orderId}` | 订单详情 |
| PUT | `/it/shop/orders/{orderId}/ship` | 发货 |
| GET | `/it/shop/item/page` | 商品分页 |
| POST | `/it/shop/item` | 新增商品 |
| PUT | `/it/shop/item/{itemId}` | 编辑商品 |
| DELETE | `/it/shop/item/{itemId}` | 删除商品 |
| PUT | `/it/shop/item/toggle-status/{itemId}` | 上架/下架切换 |

---

## 1. GET /it/shop/stats — 仪表盘数据

```
GET /it/shop/stats
Authorization: Bearer <token>
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "shopId": 4,
    "shopName": "云梦优选",
    "logo": "https://oss.xxx/item/xxx.jpg",
    "status": 1,

    "totalProducts": 15,
    "activeProducts": 10,

    "todaySales": 1285000,
    "todayOrders": 86,
    "totalRevenue": 52345600,
    "totalOrders": 320,
    "views": 0
  }
}
```

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| totalProducts | Long | — | 全部商品数 |
| activeProducts | Long | — | 上架中商品数 |
| todaySales | Long | 分 | 今日销售额（已完成订单） |
| todayOrders | Integer | — | 今日订单数 |
| totalRevenue | Long | 分 | 累计收入 |
| totalOrders | Integer | — | 累计订单数 |
| views | Integer | — | 店铺浏览量（暂为0） |

---

## 2. GET /it/shop/orders — 订单列表

```
GET /it/shop/orders?page=1&size=10&status=PAID&keyword=1718726400
Authorization: Bearer <token>
```

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | int | 否 | 页码，默认1 |
| size | int | 否 | 每页条数，默认10 |
| status | String | 否 | 状态筛选：PENDING_PAYMENT / PAID / SHIPPED / COMPLETED / CANCELLED |
| keyword | String | 否 | 关键词搜索（匹配订单号） |

**响应**:
```json
{
  "code": 200,
  "data": {
    "page": 1,
    "size": 10,
    "total": 25,
    "pages": 3,
    "records": [
      {
        "id": "1718726400000123456",
        "shopId": 4,
        "userId": 100,
        "totalAmount": 19900,
        "payAmount": 0,
        "discountAmount": 0,
        "status": "PENDING_PAYMENT",
        "createTime": "2026-06-18T10:00:00",
        "updateTime": "2026-06-18T10:00:00",
        "payTime": null,
        "expireTime": "2026-06-18T10:30:00",
        "address": "北京市朝阳区xxx路xxx号",
        "phone": "13800138000",
        "trackingNo": null,
        "remark": "请尽快发货",
        "productName": "手工牛皮托特包",
        "productImage": "https://oss.xxx/item/xxx.jpg",
        "buyerName": "张先生"
      }
    ]
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| productName | String | 订单首个商品名 |
| productImage | String | 订单首个商品图片 |
| buyerName | String | 买家昵称（无昵称则为手机号） |
| address | String | 收货地址 |
| phone | String | 收货人电话 |
| trackingNo | String | 物流单号（发货后填写） |
| remark | String | 买家备注 |

---

## 3. GET /it/shop/orders/{orderId} — 订单详情

```
GET /it/shop/orders/1718726400000123456
Authorization: Bearer <token>
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "id": "1718726400000123456",
    "orderNo": "1718726400000123456",
    "shopId": 4,
    "userId": 100,
    "totalAmount": 19900,
    "payAmount": 0,
    "discountAmount": 0,
    "status": "PENDING_PAYMENT",
    "createTime": "2026-06-18T10:00:00",
    "updateTime": "2026-06-18T10:00:00",
    "payTime": null,
    "expireTime": "2026-06-18T10:30:00",
    "address": "北京市朝阳区xxx路xxx号",
    "phone": "13800138000",
    "trackingNo": null,
    "remark": "请尽快发货",
    "buyerName": "张先生",
    "buyerPhone": "13800138000",
    "items": [
      {
        "name": "手工牛皮托特包",
        "image": "https://oss.xxx/item/xxx.jpg",
        "price": 19900,
        "quantity": 1
      }
    ]
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| orderNo | String | 订单号（同 id） |
| address | String | 收货地址 |
| phone | String | 收货人电话 |
| trackingNo | String | 物流单号 |
| remark | String | 买家备注 |
| buyerName | String | 买家昵称 |
| buyerPhone | String | 买家手机号 |
| items | Array | 订单商品明细列表 |
| items[].name | String | 商品名 |
| items[].image | String | 商品图片 |
| items[].price | Long | 单价（分） |
| items[].quantity | Integer | 数量 |

---

## 4. PUT /it/shop/orders/{orderId}/ship — 发货

```
PUT /it/shop/orders/1718726400000123456/ship?trackingNo=SF1234567890
Authorization: Bearer <token>
```

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| trackingNo | String | 是 | 物流单号 |

**响应**: `{ "code": 200, "data": null }`

---

## 5. GET /it/shop/stats/revenue — 收入趋势

```
GET /it/shop/stats/revenue?period=7d
Authorization: Bearer <token>
```

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| period | String | 否 | `7d` / `30d` / `90d`，默认7d |

**响应**:
```json
{
  "code": 200,
  "data": [
    { "date": "2026-06-11", "amount": 420000 },
    { "date": "2026-06-12", "amount": 380000 },
    { "date": "2026-06-13", "amount": 510000 },
    { "date": "2026-06-14", "amount": 460000 },
    { "date": "2026-06-15", "amount": 680000 },
    { "date": "2026-06-16", "amount": 720000 },
    { "date": "2026-06-17", "amount": 1285000 }
  ]
}
```

| 字段 | 类型 | 单位 |
|------|------|------|
| date | String | 日期 yyyy-MM-dd |
| amount | Long | 分 |

---

## 统一规范

### 响应格式
所有接口统一返回 `{ "code": 200, "data": ... }`。  
Axios 响应拦截器: `res => res.data`，解包后取 `.data` 获取业务数据。

### 金额单位
所有金额字段统一使用 **分（Long）**，前端展示时除以 100 并 `.toFixed(2)`。

### 订单状态枚举

| 值 | 含义 |
|---|------|
| PENDING_PAYMENT | 待付款 |
| PAID | 已付款/待发货 |
| SHIPPED | 已发货 |
| COMPLETED | 已完成 |
| CANCELLED | 已取消 |

### 时间格式
后端返回 `yyyy-MM-ddTHH:mm:ss`（ISO 格式），前端可用 `new Date()` 解析。

---

## 额外说明

### Order 表新增字段
已为 `order` 实体添加以下字段，后续创建订单时需传入：

| 字段 | 列名 | 类型 | 说明 |
|------|------|------|------|
| address | address | VARCHAR | 收货地址 |
| phone | phone | VARCHAR | 收货人电话 |
| trackingNo | tracking_no | VARCHAR | 物流单号 |
| remark | remark | VARCHAR | 买家备注 |

如果数据库表缺少这些列，需执行：
```sql
ALTER TABLE `order` ADD COLUMN `address` VARCHAR(500) NULL COMMENT '收货地址' AFTER `expire_time`;
ALTER TABLE `order` ADD COLUMN `phone` VARCHAR(20) NULL COMMENT '收货人电话' AFTER `address`;
ALTER TABLE `order` ADD COLUMN `tracking_no` VARCHAR(64) NULL COMMENT '物流单号' AFTER `phone`;
ALTER TABLE `order` ADD COLUMN `remark` VARCHAR(500) NULL COMMENT '买家备注' AFTER `tracking_no`;
```

### 改动文件清单

| 模块 | 文件 | 操作 |
|------|------|------|
| ym-pay | `Order.java` | +address/phone/trackingNo/remark |
| ym-pay | `IOrderService.java` | +shopOrderList(增强)/shopStats/revenue/detail/ship |
| ym-pay | `OrderServiceImpl.java` | 全部实现 |
| ym-pay | `OrderController.java` | +5个端点 |
| ym-user | `IUserService.java` | +getUserById |
| ym-user | `UserServiceImpl.java` | +getUserById实现 |
| ym-user | `UserController.java` | +GET /user/info/{id} |
| ym-api | `UserFeign.java` | +getUserById |
| ym-api | `OrderFeign.java` | +getShopStats/shopRevenue/detail/ship，getShopOrders加参数 |
| ym-item | `ShopServiceImpl.java` | getShopStats调用OrderFeign |
| ym-item | `ShopController.java` | +orders/{id}详情/+ship/+stats/revenue，orders加筛选参数 |
