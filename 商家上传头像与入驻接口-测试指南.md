# 商家上传头像与入驻接口 - 快速测试指南

## 使用 Postman/Apifox 测试

### 1. 上传店铺头像

**请求配置:**
- **方法**: POST
- **URL**: `http://localhost:8082/it/shop/upload/avatar`
- **Headers**: 
  - `Authorization`: `Bearer <your_token>`
- **Body** (form-data):
  - Key: `file` (类型选择 File)
  - Value: 选择一个图片文件

**预期响应:**
```json
{
  "code": 200,
  "message": "success",
  "data": "/api/uploads/shop/1718726400000/a1b2c3d4e5f6.jpg"
}
```

---

### 2. 商家入驻

**请求配置:**
- **方法**: POST
- **URL**: `http://localhost:8082/it/shop/entry`
- **Headers**: 
  - `Authorization`: `Bearer <your_token>`
  - `Content-Type`: `application/json`
- **Body** (raw JSON):
```json
{
  "shopName": "测试店铺",
  "logo": "/api/uploads/shop/1718726400000/a1b2c3d4e5f6.jpg",
  "description": "这是一个测试店铺"
}
```

**预期响应:**
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

---

## 使用 cURL 测试

### 1. 上传头像

```bash
curl -X POST http://localhost:8082/it/shop/upload/avatar \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@/path/to/your/image.jpg"
```

### 2. 商家入驻

```bash
curl -X POST http://localhost:8082/it/shop/entry \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "shopName": "测试店铺",
    "logo": "/api/uploads/shop/1718726400000/a1b2c3d4e5f6.jpg",
    "description": "这是一个测试店铺"
  }'
```

---

## 注意事项

1. **获取 Token**: 先调用商家登录接口 `/us/user/shop/login` 获取 token
2. **角色验证**: 确保登录的用户 role=1（商家）
3. **文件大小**: 上传的图片不能超过 5MB
4. **文件格式**: 只支持 jpg、png、gif、webp 格式

---

## 常见错误

| 错误信息 | 原因 | 解决方案 |
|---------|------|----------|
| 仅商家可访问此接口 | 用户不是商家角色 | 使用商家账号登录 |
| 上传文件不能为空 | 没有选择文件 | 在 form-data 中选择文件 |
| 文件大小不能超过5MB | 文件太大 | 压缩图片后重新上传 |
| 只支持jpg、png、gif、webp格式的图片 | 文件格式不支持 | 转换图片格式 |
| 店铺名称不能为空 | shopName 为空 | 填写店铺名称 |
