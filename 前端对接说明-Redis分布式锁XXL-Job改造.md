# 前端对接说明 — Redis / 分布式锁 / XXL-Job 改造

> 本次后端改造主要集中在并发安全、缓存优化和定时任务，前端大部分改动为透明升级，仅少量状态展示和错误处理需配合。

---

## 一、无需改动（后端透明）

| 改造项 | 说明 |
|--------|------|
| SKU 信息增加 Redis 缓存 | 接口响应不变，纯后端优化 |
| 发放中优惠券列表加缓存 | 接口响应不变，纯后端优化 |
| 优惠券发放/结束 清除缓存 | 接口响应不变 |
| 用户券过期扫描 XXL-Job | 状态值已存在于枚举，见下方说明 |
| 点赞同步改为 XXL-Job | 接口响应不变 |

---

## 二、需前端配合的改动

### 1. 订单超时自动取消 ⚠️ 重要

**改动内容：** 下单后 **30 分钟** 未支付，后端定时任务会自动将订单状态改为「已取消」。

**影响接口：**
- 订单列表 `/order/list`
- 订单详情 `/order/detail/{orderId}`

**前端需配合：**

- 订单 `status` 字段值为 `4`（CANCELLED）时，展示「已取消」状态
- **建议**：待付款订单展示倒计时提示（如「剩余 29:30 自动取消」），提升用户体验
- 若用户正在支付页面倒计时结束，需轮询订单状态或 WebSocket 通知刷新

```json
// 订单状态枚举
{
  "0": "待付款",
  "1": "已支付",
  "2": "已发货",
  "3": "已完成",
  "4": "已取消",
  "5": "已退款"
}
```

---

### 2. 用户券过期状态展示

**改动内容：** 用户券新增自动过期标记（每日凌晨扫描），`termEndTime` 超期的券会变为 `status=3`（EXPIRED）。

**影响接口：**
- 用户券列表 `/user-coupon/page`

**前端需配合：**

- `status` 字段值为 `3` 时，展示「已过期」标签/样式（置灰不可用）

```json
// 用户券状态枚举（已有，无需新增）
{
  "1": "未使用",
  "2": "已使用",
  "3": "已过期"
}
```

---

### 3. 库存扣减并发提示

**改动内容：** 高并发下单时，若库存锁获取失败，会返回错误。

**影响接口：**
- 创建订单 `/order/create`

**前端需配合：**

- 当返回 `"系统繁忙，请稍后再试"` 时，前端提示用户稍后重试，不要静默失败

```json
// 错误响应示例
{
  "code": 500,
  "msg": "系统繁忙，请稍后再试"
}
```

---

### 4. 支付回调幂等处理

**改动内容：** 支付回调增加分布式锁防重，重复回调直接 return（无异常）。

**影响：** 前端无感知，回调处理更安全。

---

## 三、总结

| 优先级 | 改动点 | 前端动作 |
|--------|--------|----------|
| 🔴 高 | 订单 30 分钟超时自动取消 | 展示倒计时 / 处理已取消状态 |
| 🟡 中 | 用户券过期状态 | 展示「已过期」标签 |
| 🟢 低 | 库存扣减并发提示 | 优雅展示"系统繁忙"提示 |

> 以上枚举值均为后端已有定义，前端只需补充对应的 UI 展示逻辑，无需新增字段。
