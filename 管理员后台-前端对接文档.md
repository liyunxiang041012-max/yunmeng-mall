# 管理员后台 — 前端对接文档

> 网关: `http://localhost:8080` | 需管理员登录 token

---

## 接口一览

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/us/user/admin/login` | 管理员登录 |
| GET | `/us/user/admin/page` | 用户分页列表 |
| GET | `/us/user/admin/{userId}` | 用户详情 |
| PUT | `/us/user/admin/{userId}/status` | 启用/禁用用户 |
| PUT | `/us/user/admin/{userId}/role` | 修改用户角色 |

> 商家管理 CRUD 已存在：`/it/shop/admin/*`（分页/详情/编辑/删除/开关状态）

---

## 1. 管理员登录

```
POST /us/user/admin/login
Content-Type: application/json

{
  "account": "admin",
  "password": "123456"
}
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGci...",
    "role": 2,
    "needSetup": false
  }
}
```

**错误**:
| code | message | 触发条件 |
|------|---------|---------|
| 1002 | 用户不存在 | 账号不存在 |
| 500 | 密码错误 | 密码不对 |
| 500 | 该账号不是管理员 | role≠2 |
| 500 | 该账号已被禁用 | status=0 |

---

## 2. 用户分页列表

```
GET /us/user/admin/page?page=1&size=10&keyword=138&role=1&status=1
Authorization: Bearer <admin_token>
```

**请求参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | int | 否 | 页码，默认1 |
| size | int | 否 | 每页条数，默认10 |
| keyword | String | 否 | 搜索（用户名/手机号/昵称） |
| role | int | 否 | 角色筛选 0用户/1商家/2管理员 |
| status | int | 否 | 状态筛选 0禁用/1启用 |

**响应**:
```json
{
  "code": 200,
  "data": {
    "total": 56,
    "pages": 6,
    "list": [
      {
        "id": 10001,
        "username": "ym12345678",
        "phone": "13800138000",
        "nickname": "张三",
        "role": 0,
        "status": 1,
        "createTime": "2026-05-07T12:00:00",
        "updateTime": "2026-06-01T08:30:00",
        "avatar": "https://oss.xxx/user-header/xxx.jpg"
      }
    ]
  }
}
```

---

## 3. 用户详情

```
GET /us/user/admin/10001
Authorization: Bearer <admin_token>
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 10001,
    "username": "ym12345678",
    "phone": "13800138000",
    "nickname": "张三",
    "role": 0,
    "status": 1,
    "createTime": "2026-05-07T12:00:00",
    "updateTime": "2026-06-01T08:30:00",
    "avatar": "https://oss.xxx/user-header/xxx.jpg",
    "experience": 120,
    "region": "北京 北京市"
  }
}
```

---

## 4. 启用/禁用用户

```
PUT /us/user/admin/10001/status
Authorization: Bearer <admin_token>
Content-Type: application/json

{ "status": 0 }
```

| body字段 | 类型 | 说明 |
|----------|------|------|
| status | int | 0=禁用 / 1=启用 |

**响应**: `{ "code": 200, "data": null }`

---

## 5. 修改用户角色

```
PUT /us/user/admin/10001/role
Authorization: Bearer <admin_token>
Content-Type: application/json

{ "role": 1 }
```

| body字段 | 类型 | 说明 |
|----------|------|------|
| role | int | 0=用户 / 1=商家 / 2=管理员 |

**响应**: `{ "code": 200, "data": null }`

---

## 角色枚举

| 值 | 含义 |
|---|------|
| 0 | 普通用户 |
| 1 | 商家 |
| 2 | 管理员 |

## 用户状态

| 值 | 含义 |
|---|------|
| 0 | 已禁用 |
| 1 | 正常 |

---

## 改动文件清单

| 模块 | 文件 | 操作 |
|------|------|------|
| ym-user | `IUserService.java` | +adminLogin |
| ym-user | `UserServiceImpl.java` | +adminLogin 实现（校验role=2） |
| ym-user | `UserController.java` | +POST /user/admin/login |
| ym-user | `AdminController.java` | 🆕 用户管理CRUD（分页/详情/启用禁用/改角色） |

> 网关 `/us/**` → ym-user 已存在，无需额外配置。重启 ym-user 即可。
