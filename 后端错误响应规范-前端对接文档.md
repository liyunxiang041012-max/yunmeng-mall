# 后端错误响应规范 — 前端对接文档

> 发送给前端，按此文档处理接口错误

---

## 统一错误响应格式

后端所有接口（含异常）统一返回 JSON：

```json
{
  "code": 500,
  "message": "错误描述"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 状态码：200=成功，非200=失败 |
| message | String | 可展示给用户的错误提示 |

**不再有** raw 堆栈文本或 HTML 错误页。

---

## 各接口可能返回的错误

### 1. 发送验证码 `POST /us/user/sendCode`

| code | message | 触发条件 | 前端处理 |
|------|---------|---------|---------|
| 500 | 该手机号已注册为商家，请直接登录 | 手机号已是商家 | 引导用户去商家登录页 |
| 200 | — | 成功发送 | 进入验证码输入步骤 |

### 2. 商家登录 `POST /us/user/shop/login`

| code | message | 触发条件 | 前端处理 |
|------|---------|---------|---------|
| 1003 | 密码错误 | 密码不匹配 | toast 提示 |
| 500 | 该账号不是商家，请使用商家账号登录 | role≠1 | 引导去普通用户登录 |

### 3. 商家入驻 `POST /it/shop/entry`

| code | message | 触发条件 | 前端处理 |
|------|---------|---------|---------|
| 500 | 仅商家可访问此接口 | token 角色不对 | 引导重新登录 |
| 500 | 您还未开设店铺 | 无店铺 | 引导入驻流程 |

### 4. 订单列表 `GET /it/shop/orders`

| code | message | 触发条件 | 前端处理 |
|------|---------|---------|---------|
| 500 | 仅商家可访问此接口 | token 角色不对 | 跳转登录 |
| 500 | 您还未开设店铺 | 未入驻 | 跳转入驻页 |

### 5. 订单详情 `GET /it/shop/orders/{orderId}`

| code | message | 触发条件 | 前端处理 |
|------|---------|---------|---------|
| 500 | 订单不存在 | ID 无效 | toast 提示后返回列表 |

### 6. 发货 `PUT /it/shop/orders/{orderId}/ship`

| code | message | 触发条件 | 前端处理 |
|------|---------|---------|---------|
| 500 | 订单不存在 | ID 无效 | toast |
| 500 | 只能对已付款的订单发货 | 订单状态≠PAID | toast |

### 7. 商品管理 `/it/shop/item/**`

| code | message | 触发条件 | 前端处理 |
|------|---------|---------|---------|
| 500 | 仅商家可访问此接口 | 角色不对 | 跳转登录 |
| 500 | 商品不存在 | itemId 无效 | toast |

---

## Axios 拦截器建议配置

```js
// 响应拦截器
axios.interceptors.response.use(
  res => {
    const body = res.data;  // { code, data, message }
    if (body.code !== 200) {
      // 统一错误提示
      ElMessage.error(body.message || '操作失败');
      return Promise.reject(new Error(body.message));
    }
    return body.data;  // 直接返回业务数据
  },
  err => {
    // 网络异常/500 等
    const body = err.response?.data;
    if (body && body.message) {
      ElMessage.error(body.message);
    } else {
      ElMessage.error('网络异常，请稍后重试');
    }
    return Promise.reject(err);
  }
);
```

---

## 本次修复内容

| 修复 | 文件 | 说明 |
|------|------|------|
| RuntimeException → JSON | `GlobalExceptionHandler.java` | 新增 `RuntimeException` 处理器，所有 `throw new RuntimeException("xxx")` 现在返回 `{code:500, message:"xxx"}` |
| sendSms 放宽检查 | `UserServiceImpl.java` | 普通用户（非商家）获取验证码不再被拦截，只拦截已是商家的手机号 |

**影响**：之前 `sendSms` 对已有手机号返回 500 HTML/堆栈，现在返回 `{"code":500,"message":"该手机号已注册为商家，请直接登录"}`（仅商家被拦截），普通用户能正常获取验证码以升级为商家。
