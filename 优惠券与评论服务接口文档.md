# 优惠券 & 评论服务 - 前端接口文档

---

## 通用说明

| 项目 | 说明 |
|------|------|
| 基础路径 | `http://localhost:8080`，所有接口前加网关前缀：优惠券 `/pm`，评论 `/rm`。例 `/coupons/list` → `/pm/coupons/list` |
| 认证方式 | 用户端接口需携带 JWT Token → Header: `Authorization: Bearer <token>` |
| 金额单位 | **所有金额均为「分」**，前端显示需除以 100 |
| 时间格式 | `yyyy-MM-dd HH:mm:ss`，如 `2026-05-28 23:59:59` |
| 分页参数 | `pageNo` (默认1), `pageSize` (默认20) |
| 分页响应结构 | `{ "total": 100, "pages": 5, "list": [...] }` |

---

# 一、优惠券管理接口（管理端）

## 1.1 新增优惠券

```
POST /coupons
```

**请求体 JSON：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | String | 是 | 优惠券名称，4~20字 |
| discountType | String | 是 | 折扣类型：`PER_PRICE_DISCOUNT`(每满减) / `RATE_DISCOUNT`(折扣) / `NO_THRESHOLD`(无门槛) / `PRICE_DISCOUNT`(满减) |
| thresholdAmount | Integer | 否 | 使用门槛(分)，0=无门槛 |
| discountValue | Integer | 是 | 折扣值：满减=抵扣金额(分)，折扣=80表示8折 |
| maxDiscountAmount | Integer | 否 | 最高优惠金额(分)，0=不限 |
| totalNum | Integer | 是 | 发行总量，1~5000 |
| userLimit | Integer | 是 | 每人限领数量，1~10 |
| obtainWay | String | 是 | 领取方式：`PUBLIC`(手动领取) / `ISSUE`(兑换码) |
| specific | Boolean | 否 | 是否限定使用范围 |
| scopes | Long[] | 否 | 限定范围的分类/商品ID列表 |

**请求示例：**
```json
{
  "name": "满100减20",
  "discountType": "PRICE_DISCOUNT",
  "thresholdAmount": 10000,
  "discountValue": 2000,
  "maxDiscountAmount": 0,
  "totalNum": 5000,
  "userLimit": 1,
  "obtainWay": "PUBLIC",
  "specific": false
}
```

**响应：** `void`

---

## 1.2 开始发放优惠券（设置发放时间+有效期后上架）

```
PUT /coupons/{id}/issue
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| issueBeginTime | String | 否 | 发放开始时间，必须晚于当前时间 |
| issueEndTime | String | 是 | 发放结束时间，必须晚于当前时间 |
| termDays | Integer | 否 | 有效天数，与termBeginTime/termEndTime二选一 |
| termBeginTime | String | 否 | 有效期开始时间 |
| termEndTime | String | 否 | 有效期结束时间 |

**请求示例：**
```json
{
  "issueEndTime": "2026-12-31 23:59:59",
  "termDays": 30
}
```

> 如果是兑换码类型的优惠券，开始发放后会自动异步生成兑换码。

**响应：** `void`

---

## 1.3 修改优惠券

```
PUT /coupons/{id}
```

参数同 [1.1 新增优惠券]，额外需要传 `id`。

**响应：** `void`

---

## 1.4 删除优惠券

```
DELETE /coupons/{id}
```

**响应：** `void`

---

## 1.5 暂停发放

```
PUT /coupons/{id}/pause
```

**响应：** `void`

---

## 1.6 查询优惠券详情

```
GET /coupons/{id}
```

**响应示例：**
```json
{
  "id": 1001,
  "name": "满100减20",
  "discountType": "PRICE_DISCOUNT",
  "thresholdAmount": 10000,
  "discountValue": 2000,
  "maxDiscountAmount": 0,
  "totalNum": 5000,
  "userLimit": 1,
  "obtainWay": "PUBLIC",
  "issueBeginTime": "2026-05-01 00:00:00",
  "issueEndTime": "2026-12-31 23:59:59",
  "termDays": 30,
  "termBeginTime": null,
  "termEndTime": null,
  "specific": false
}
```

---

## 1.7 分页查询优惠券（管理端）

```
GET /coupons/page?pageNo=1&pageSize=10&status=3&name=满减
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| pageNo | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页大小，默认20 |
| type | Integer | 否 | 折扣类型：1=每满减, 2=折扣, 3=无门槛, 4=满减 |
| status | Integer | 否 | 状态：1=待发放, 2=未开始, 3=发放中, 4=已结束, 5=暂停 |
| name | String | 否 | 优惠券名称(模糊搜索) |

**响应示例：**
```json
{
  "total": 5,
  "pages": 1,
  "list": [
    {
      "id": 1001,
      "name": "满100减20",
      "discountType": "PRICE_DISCOUNT",
      "thresholdAmount": 10000,
      "discountValue": 2000,
      "maxDiscountAmount": 0,
      "obtainWay": "PUBLIC",
      "totalNum": 5000,
      "issueNum": 128,
      "usedNum": 0,
      "issueBeginTime": "2026-05-01 00:00:00",
      "issueEndTime": "2026-12-31 23:59:59",
      "termDays": 30,
      "status": "ISSUING",
      "specific": false
    }
  ]
}
```

---

# 二、用户端优惠券接口

## 2.1 查询可领取优惠券列表

```
GET /coupons/list
```

不需要传参，返回所有状态为「发放中」的优惠券。

**响应示例：**
```json
[
  {
    "id": 1001,
    "name": "满100减20",
    "discountType": "PRICE_DISCOUNT",
    "thresholdAmount": 10000,
    "discountValue": 2000,
    "maxDiscountAmount": 0,
    "termDays": 30,
    "termEndTime": null,
    "specific": false,
    "available": true,
    "received": false
  }
]
```

| 字段 | 说明 |
|------|------|
| available | 是否可领取（库存未耗尽且限领未达上限） |
| received | 当前用户是否已领取过 |

---

## 2.2 领取优惠券

```
POST /user-coupons/{couponId}/receive
```

**路径参数：** `couponId` — 优惠券ID

**响应：** `void`

> 错误情况：库存不足返回400(BizIllegalException)，已达限领上限返回400(BadRequestException)，高并发请求会使用分布式锁排队。

---

## 2.3 兑换优惠券（兑换码）

```
POST /user-coupons/{code}/exchange
```

**路径参数：** `code` — 10位Base32兑换码字符串

**响应：** `void`

---

## 2.4 查询我的优惠券

```
GET /user-coupons/page?pageNo=1&pageSize=10&status=1
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| pageNo | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页大小，默认20 |
| status | Integer | 否 | 状态：1=未使用, 2=已使用, 3=已过期 |

**响应示例：**
```json
{
  "total": 3,
  "pages": 1,
  "list": [
    {
      "id": 101,
      "name": "满100减20",
      "discountType": "PRICE_DISCOUNT",
      "thresholdAmount": 10000,
      "discountValue": 2000,
      "maxDiscountAmount": 0,
      "termDays": 30,
      "termEndTime": "2026-06-28 10:00:00",
      "specific": false
    }
  ]
}
```

---

# 三、兑换码管理接口

## 3.1 分页查询兑换码

```
GET /codes/page?couponId=1005&status=1&pageNo=1&pageSize=20
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| couponId | Long | 是 | 优惠券ID |
| status | Integer | 否 | 状态：1=待兑换, 2=已兑换 |
| pageNo | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页大小，默认20 |

**响应示例：**
```json
{
  "total": 100,
  "pages": 5,
  "list": [
    { "id": 12345, "code": "ABCDEFGHJK" },
    { "id": 12346, "code": "ABCDEFGHJL" }
  ]
}
```

---

# 四、点赞接口

## 4.1 点赞 / 取消点赞

```
POST /likes
```

**请求体 JSON：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| bizId | Long | 是 | 点赞目标业务ID（如商品ID、评论ID） |
| bizType | String | 是 | 业务类型，如 `product`、`comment` |
| liked | Boolean | 是 | true=点赞，false=取消点赞 |

**请求示例：**
```json
{
  "bizId": 1001,
  "bizType": "comment",
  "liked": true
}
```

**响应：** `void`

---

## 4.2 批量查询点赞状态

```
GET /likes/list?bizIds=1001&bizIds=1002&bizIds=1003
```

**说明：** 传入多个业务ID，返回当前用户已点赞的ID集合。

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| bizIds | Long[] | 是 | 业务ID列表，逗号分隔或重复参数 |

**响应示例：**
```json
[1001, 1003]
```

> 返回数组中包含的是当前用户已点赞的业务ID。前端可根据此集合判断高亮状态。

---

# 五、评论接口

## 5.1 发表评论 / 回复

```
POST /comments
```

**请求体 JSON：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| bizId | Long | 是 | 评论目标业务ID（如商品ID） |
| bizType | String | 是 | 业务类型，如 `product` |
| parentId | Long | 否 | 父评论ID，0或不传=一级评论，传值=回复 |
| content | String | 是 | 评论内容 |

**请求示例：**
```json
{
  "bizId": 100,
  "bizType": "product",
  "content": "质量很好，推荐购买！"
}
```

**回复示例：**
```json
{
  "bizId": 100,
  "bizType": "product",
  "parentId": 5001,
  "content": "谢谢支持！"
}
```

**响应：** `void`

---

## 5.2 分页查询评论

```
GET /comments/page?bizId=100&bizType=product&pageNo=1&pageSize=20
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| bizId | Long | 是 | 业务ID（如商品ID） |
| bizType | String | 是 | 业务类型，如 `product` |
| pageNo | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页大小，默认20 |

**响应示例：**
```json
{
  "total": 25,
  "pages": 2,
  "list": [
    {
      "id": 5001,
      "userId": 18,
      "bizId": 100,
      "parentId": 0,
      "content": "质量很好，推荐购买！",
      "likedTimes": 12,
      "replyCount": 3,
      "createTime": "2026-05-28 14:30:00"
    }
  ]
}
```

---

# 六、枚举字典

## 6.1 优惠券折扣类型（discountType）

| 枚举值 | 说明 |
|--------|------|
| `PER_PRICE_DISCOUNT` | 每满减（如每满200减30） |
| `RATE_DISCOUNT` | 折扣（如8折） |
| `NO_THRESHOLD` | 无门槛（直接减） |
| `PRICE_DISCOUNT` | 满减（满X减Y） |

## 6.2 优惠券状态（status）

| 值 | 说明 |
|----|------|
| 1 | 待发放 |
| 2 | 未开始 |
| 3 | 发放中 |
| 4 | 已结束 |
| 5 | 暂停 |

## 6.3 领取方式（obtainWay）

| 枚举值 | 说明 |
|--------|------|
| `PUBLIC` | 手动领取 |
| `ISSUE` | 兑换码兑换 |

## 6.4 用户券状态

| 值 | 说明 |
|----|------|
| 1 | 未使用 |
| 2 | 已使用 |
| 3 | 已过期 |

## 6.5 兑换码状态

| 值 | 说明 |
|----|------|
| 1 | 待兑换 |
| 2 | 已兑换 |
| 3 | 已过期 |

---

# 七、数据库速查

## 优惠券表 (coupon)

| 字段 | 说明 |
|------|------|
| id | 雪花ID |
| discount_type | 折扣类型(1~4) |
| threshold_amount | 门槛金额(分) |
| discount_value | 折扣值(分或百分比) |
| max_discount_amount | 最高优惠(分) |
| obtain_way | 领取方式(1/2) |
| total_num | 发行总量 |
| issue_num | 已领取数 |
| user_limit | 每人限领数 |
| term_days | 有效天数 |
| status | 状态(1~5) |

## 用户优惠券表 (user_coupon)

| 字段 | 说明 |
|------|------|
| id | 雪花ID |
| user_id | 用户ID |
| coupon_id | 优惠券模板ID |
| term_begin_time | 有效期开始 |
| term_end_time | 有效期结束 |
| status | 1=未使用, 2=已使用, 3=已过期 |

## 评论表 (comment)

| 字段 | 说明 |
|------|------|
| id | 雪花ID |
| user_id | 评论用户ID |
| biz_id | 业务ID(商品ID等) |
| biz_type | 业务类型 |
| parent_id | 父评论ID，0=一级评论 |
| liked_times | 点赞数 |
| reply_count | 回复数 |

## 点赞表 (liked_record)

| 字段 | 说明 |
|------|------|
| user_id | 点赞用户ID |
| biz_id | 业务ID |
| biz_type | 业务类型 |
