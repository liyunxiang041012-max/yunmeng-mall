# 商家上传头像与入驻接口 - 前端对接文档

## 接口概述

本文档提供两个核心接口的详细说明：
1. **上传店铺头像接口** - 用于上传店铺logo/头像图片
2. **商家入驻接口** - 用于填写店铺名称、头像URL和店铺描述完成入驻

---

## 前置条件

- 用户必须已通过商家登录接口 (`POST /us/user/shop/login`) 登录
- 登录用户的 `role` 必须为 `1`（商家角色）
- 请求头需携带有效的 JWT Token

---

## 接口详情

### 1. 上传店铺头像

#### 基本信息
- **接口路径**: `POST /it/shop/upload/avatar`
- **Content-Type**: `multipart/form-data`
- **权限要求**: 仅商家可访问 (role=1)

#### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | File | 是 | 图片文件 |

#### 文件限制
- **支持格式**: jpg, png, gif, webp
- **文件大小**: 最大 5MB
- **推荐尺寸**: 建议正方形图片，如 200x200px 或 400x400px

#### 响应示例

**成功响应:**
```json
{
  "code": 200,
  "message": "success",
  "data": "/api/uploads/shop/1718726400000/a1b2c3d4e5f6.jpg"
}
```

**失败响应:**
```json
{
  "code": 500,
  "message": "只支持jpg、png、gif、webp格式的图片",
  "data": null
}
```

#### 前端调用示例

```javascript
// Vue3 + Axios 示例
const uploadAvatar = async (file) => {
  const formData = new FormData();
  formData.append('file', file);
  
  try {
    const response = await axios.post('/it/shop/upload/avatar', formData, {
      headers: {
        'Content-Type': 'multipart/form-data',
        'Authorization': `Bearer ${token}` // 从localStorage获取token
      }
    });
    
    if (response.data.code === 200) {
      const avatarUrl = response.data.data;
      console.log('上传成功，头像URL:', avatarUrl);
      return avatarUrl;
    }
  } catch (error) {
    console.error('上传失败:', error.response?.data?.message);
  }
};
```

```javascript
// React + Fetch 示例
const uploadAvatar = async (file) => {
  const formData = new FormData();
  formData.append('file', file);
  
  try {
    const response = await fetch('/it/shop/upload/avatar', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${token}`
      },
      body: formData
    });
    
    const result = await response.json();
    if (result.code === 200) {
      return result.data;
    }
  } catch (error) {
    console.error('上传失败:', error);
  }
};
```

---

### 2. 商家入驻（设置店铺信息）

#### 基本信息
- **接口路径**: `POST /it/shop/entry`
- **Content-Type**: `application/json`
- **权限要求**: 仅商家可访问 (role=1)

#### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| shopName | String | 是 | 店铺名称，不能为空 |
| logo | String | 否 | 店铺头像URL（通过上传接口获得） |
| description | String | 否 | 店铺描述，最多512字 |

#### 请求体示例

```json
{
  "shopName": "云梦优选小店",
  "logo": "/api/uploads/shop/1718726400000/a1b2c3d4e5f6.jpg",
  "description": "专注优质商品，为您提供最好的购物体验"
}
```

#### 响应示例

**成功响应:**
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

**失败响应:**
```json
{
  "code": 500,
  "message": "店铺名称不能为空",
  "data": null
}
```

#### 前端调用示例

```javascript
// Vue3 + Axios 示例
const submitShopEntry = async (shopData) => {
  try {
    const response = await axios.post('/it/shop/entry', shopData, {
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      }
    });
    
    if (response.data.code === 200) {
      ElMessage.success('入驻成功！');
      // 跳转到商家后台首页
      router.push('/merchant/dashboard');
    }
  } catch (error) {
    ElMessage.error(error.response?.data?.message || '入驻失败');
  }
};

// 使用示例
const handleSubmit = async () => {
  const shopData = {
    shopName: form.shopName,
    logo: uploadedAvatarUrl, // 从上传接口返回的URL
    description: form.description
  };
  
  await submitShopEntry(shopData);
};
```

```javascript
// React + Fetch 示例
const submitShopEntry = async (shopData) => {
  try {
    const response = await fetch('/it/shop/entry', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify(shopData)
    });
    
    const result = await response.json();
    if (result.code === 200) {
      message.success('入驻成功！');
      navigate('/merchant/dashboard');
    } else {
      message.error(result.message);
    }
  } catch (error) {
    message.error('入驻失败');
  }
};
```

---

## 完整流程示例

### 场景：商家首次入驻

```javascript
// 完整的入驻流程
const handleShopEntry = async () => {
  // 步骤1: 验证表单
  if (!form.shopName) {
    ElMessage.warning('请输入店铺名称');
    return;
  }
  
  // 步骤2: 如果有上传头像，先上传头像
  let logoUrl = '';
  if (avatarFile) {
    try {
      logoUrl = await uploadAvatar(avatarFile);
    } catch (error) {
      ElMessage.error('头像上传失败');
      return;
    }
  }
  
  // 步骤3: 提交入驻信息
  const shopData = {
    shopName: form.shopName,
    logo: logoUrl,
    description: form.description
  };
  
  try {
    await submitShopEntry(shopData);
    // 入驻成功，跳转页面
  } catch (error) {
    // 错误处理已在submitShopEntry中完成
  }
};
```

---

## 错误码说明

| 错误码 | 说明 | 解决方案 |
|--------|------|----------|
| 200 | 成功 | - |
| 401 | 未授权 | 检查Token是否有效，重新登录 |
| 403 | 无权限 | 当前用户不是商家角色，请使用商家账号登录 |
| 500 | 服务器错误 | 查看具体错误消息，联系后端开发 |

---

## 注意事项

### 1. 文件上传
- 上传接口返回的是相对路径，前端需要拼接完整的域名或CDN地址
- 建议在上传前进行图片压缩，提升用户体验
- 可以添加图片预览功能，让用户确认后再上传

### 2. 入驻流程
- 如果商家已经入驻过，再次调用会更新店铺信息
- 店铺名称不能为空，后端会进行校验
- 店铺描述可选，但建议不超过512字

### 3. Token管理
- 所有接口都需要在请求头中携带 Token
- Token 格式: `Authorization: Bearer <token>`
- Token 过期时需要引导用户重新登录

### 4. 错误处理
- 建议统一封装错误处理逻辑
- 对于网络错误，可以提供重试机制
- 对于业务错误，直接展示后端返回的错误消息

---

## 常见问题

### Q1: 上传头像后如何预览？
```javascript
// 上传成功后，可以直接使用返回的URL
const previewUrl = baseUrl + avatarUrl; // baseUrl为后端服务地址
<img :src="previewUrl" alt="店铺头像" />
```

### Q2: 如何判断用户是否需要入驻？
```javascript
// 登录后检查 needSetup 字段
const loginResponse = await loginApi.shopLogin(credentials);
if (loginResponse.data.needSetup) {
  // 需要入驻，跳转到入驻页面
  router.push('/merchant/entry');
} else {
  // 已入驻，跳转到商家后台
  router.push('/merchant/dashboard');
}
```

### Q3: 入驻后可以修改店铺信息吗？
可以。再次调用 `/it/shop/entry` 接口即可更新店铺信息。

---

## 技术支持

如有问题，请联系后端开发团队或查看 API 接口文档。

**文档版本**: v1.0  
**更新时间**: 2026-06-18
