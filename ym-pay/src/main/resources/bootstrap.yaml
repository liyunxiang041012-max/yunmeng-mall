spring:
  application:
    name: ym-cart
  cloud:
    nacos:
      server-addr: 192.168.150.101:8848
      discovery:
        group: DEFAULT_GROUP
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-redis.yaml
            refresh: false
          - data-id: shared-logs.yaml
            refresh: false
          - data-id: ym-cart.yaml
            refresh: false
          - data-id: shared-mq.yaml
            group: DEFAULT_GROUP
            refresh: true

server:
  port: 8083