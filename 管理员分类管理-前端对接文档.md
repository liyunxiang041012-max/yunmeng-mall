# 管理员分类管理 - 前端对接文档

---

## 接口路径（网关前缀 `/it/`）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/categories/admin/all` | 获取全部分类（平铺列表） |
| POST | `/it/categories/admin` | 新增分类 |
| PUT | `/it/categories/admin/{id}` | 编辑分类 |
| DELETE | `/it/categories/admin/{id}` | 删除分类（级联软删除子分类） |

---

## 响应格式

成功 `code = 200`，失败 `code = 500` 或 `400`。

```json
// 成功
{ "code": 200, "message": "操作成功", "data": {...}, "timestamp": 1719200000000 }

// 失败
{ "code": 500, "message": "该分类名称已存在", "data": null, "timestamp": 1719200000000 }
```

---

## 1. 获取全部分类

```
GET /it/categories/admin/all
```

**响应：**

```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    { "id": 1, "name": "图书", "parentId": 0, "level": 1, "sort": 0 },
    { "id": 2, "name": "小说", "parentId": 1, "level": 2, "sort": 0 },
    { "id": 3, "name": "科技", "parentId": 1, "level": 2, "sort": 0 }
  ]
}
```

- `data` 是**平铺列表**（非树形），前端自行构建父子关系
- `parentId = 0` 即一级分类
- `deleted = 1` 的数据不会返回

---

## 2. 新增分类

```
POST /it/categories/admin
Content-Type: application/json
```

**请求体：**

```json
{
  "name": "服装",
  "parentId": 0
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | String | 是 | 2-50 字符 |
| `parentId` | Long | 否 | 默认 0（一级分类） |

**成功响应：**

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { "id": 5, "name": "服装", "parentId": 0, "level": 1, "sort": 0 }
}
```

**错误响应：**

| 错误信息 |
|----------|
| 分类名称不能为空 |
| 分类名称长度需在2-50字符之间 |
| 父分类不存在 |
| 该分类名称已存在 |

---

## 3. 编辑分类

```
PUT /it/categories/admin/{id}
Content-Type: application/json
```

**请求体：** 同新增

```json
{
  "name": "新名称",
  "parentId": 0
}
```

**成功响应：**

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { "id": 5, "name": "新名称", "parentId": 0, "level": 1, "sort": 0 }
}
```

**额外校验：**
- 不能将分类的 `parentId` 设为自己
- 不能将分类移到其子孙分类下（防止循环引用）

---

## 4. 删除分类

```
DELETE /it/categories/admin/{id}
```

**成功响应：**

```json
{
  "code": 200,
  "message": "操作成功",
  "data": null
}
```

- 软删除（`deleted = 1`）
- **自动级联**软删除所有子孙分类
- 不拦截已绑定商品的分类

---

## 前端 `admin.js` 需更新的路径

```javascript
// 分类管理
GET    /it/categories/admin/all      // 全部分类
POST   /it/categories/admin          // 新增
PUT    /it/categories/admin/{id}     // 编辑
DELETE /it/categories/admin/{id}     // 删除
```

---

## 后端实现文件

| 文件 | 说明 |
|------|------|
| `ym-item/.../controller/CategoryAdminController.java` | 控制器 |
| `ym-item/.../service/ICategoryService.java` | 接口 |
| `ym-item/.../service/impl/CategoryServiceImpl.java` | 实现（含校验） |
| `ym-item/.../domain/dto/CategorySaveDTO.java` | 请求 DTO |
| `ym-item/.../domain/pojo/Category.java` | 实体（已存在） |
