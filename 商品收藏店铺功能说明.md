# 商品收藏 & 店铺功能 — 后端实现说明

## 一、数据库变更

### 需要执行的 SQL 脚本

文件位置：`ym-item/src/main/resources/sql/favorite_shop_follow.sql`

包含两张新表：
1. **favorite** - 商品收藏表
2. **shop_follow** - 店铺关注表

**执行步骤：**
```bash
# 连接到 MySQL 数据库
mysql -u root -p

# 执行 SQL 脚本
source /path/to/ym-item/src/main/resources/sql/favorite_shop_follow.sql
```

---

## 二、新增文件清单

### 2.1 实体类（Entity）
- `Favorite.java` - 收藏实体
- `ShopFollow.java` - 店铺关注实体

### 2.2 Mapper 接口
- `FavoriteMapper.java` - 收藏 Mapper
- `ShopFollowMapper.java` - 店铺关注 Mapper

### 2.3 Service 接口及实现
- `IFavoriteService.java` / `FavoriteServiceImpl.java` - 收藏服务
- `IShopFollowService.java` / `ShopFollowServiceImpl.java` - 店铺关注服务
- `IShopService.java` / `ShopServiceImpl.java` - 店铺服务（已扩展）

### 2.4 Controller
- `FavoriteController.java` - 收藏控制器
- `ShopController.java` - 店铺控制器（已扩展）

### 2.5 VO 类
- `ShopDetailVO.java` - 店铺详情 VO
- `ItemDetailVO.java` - 商品详情 VO（已修改，添加 shopId 字段）

---

## 三、接口清单

### 3.1 商品收藏接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/it/favorites/toggle/{itemId}` | 切换收藏状态 |
| GET | `/it/favorites/check/{itemId}` | 检查收藏状态 |

**示例：**

```bash
# 切换收藏状态
curl -X POST http://localhost:8082/it/favorites/toggle/1 \
  -H "Authorization: Bearer <token>"

# 检查收藏状态
curl http://localhost:8082/it/favorites/check/1 \
  -H "Authorization: Bearer <token>"
```

---

### 3.2 店铺详情接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/shop/{shopId}` | 获取店铺详情（含统计） |

**返回示例：**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "shopName": "云梦精选旗舰店",
    "logo": "/uploads/shop/logo_1.png",
    "description": "专注高品质数码产品，正品保障",
    "score": "4.9",
    "goodRate": "98.6%",
    "avgShipTime": "24h",
    "totalSales": 158000,
    "itemCount": 86,
    "fansCount": 32600
  }
}
```

---

### 3.3 店铺商品列表接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/shop/{shopId}/items` | 获取店铺商品列表（分页） |

**查询参数：**
- `sort` - 排序方式：`default`(综合) / `sales`(销量) / `price`(价格) / `newest`(最新)
- `page` - 页码（默认 1）
- `size` - 每页条数（默认 20）

**示例：**
```bash
curl "http://localhost:8082/it/shop/1/items?sort=sales&page=1&size=20"
```

**返回示例：**
```json
{
  "code": 200,
  "data": {
    "records": [
      {
        "id": 1,
        "name": "极简降噪耳机",
        "image": "/uploads/item/1.jpg",
        "price": 8900,
        "sold": 3200,
        "shopName": "云梦精选旗舰店",
        "status": 1
      }
    ],
    "total": 86,
    "size": 20,
    "current": 1,
    "pages": 5
  }
}
```

---

### 3.4 店铺关注接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/it/shop/follow/toggle/{shopId}` | 切换关注状态 |
| GET | `/it/shop/follow/check/{shopId}` | 检查关注状态 |

**示例：**

```bash
# 切换关注状态
curl -X POST http://localhost:8082/it/shop/follow/toggle/1 \
  -H "Authorization: Bearer <token>"

# 检查关注状态
curl http://localhost:8082/it/shop/follow/check/1 \
  -H "Authorization: Bearer <token>"
```

---

### 3.5 商品详情接口（已修改）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/items/{id}` | 获取商品详情 |

**新增字段：**
- `shopId` - 商品所属店铺ID

**返回示例：**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "shopId": 1,
    "name": "极简降噪耳机",
    "price": 8900,
    "mainImage": "/uploads/item/1.jpg",
    "sold": 3200,
    "skus": [...],
    "specs": [...]
  }
}
```

---

## 四、业务逻辑说明

### 4.1 收藏功能（Toggle 模式）

**逻辑流程：**
```
用户点击收藏按钮
  ↓
查询是否已收藏
  ↓
已收藏 → 删除收藏记录 → 返回 false
未收藏 → 添加收藏记录 → 返回 true
```

**数据库约束：**
- 唯一索引 `uk_user_item(user_id, item_id)` 防止重复收藏

---

### 4.2 店铺关注功能（Toggle 模式）

**逻辑流程：**
```
用户点击关注按钮
  ↓
查询是否已关注
  ↓
已关注 → 删除关注记录 → 返回 false
未关注 → 添加关注记录 → 返回 true
```

**数据库约束：**
- 唯一索引 `uk_user_shop(user_id, shop_id)` 防止重复关注

---

### 4.3 店铺详情统计

**统计项：**
1. **综合评分（score）** - 从 `shop_rating` 表的 `average_score` 字段获取
2. **好评率（goodRate）** - 从 `shop_rating` 表的 `good_rate` 字段获取
3. **平均发货时间（avgShipTime）** - 暂时固定为 "24h"，后续可从订单表计算
4. **总销量（totalSales）** - 统计店铺所有上架商品的 `sold` 字段之和
5. **在售商品数（itemCount）** - 统计店铺所有上架商品数量
6. **粉丝数（fansCount）** - 统计 `shop_follow` 表中该店铺的关注数

---

### 4.4 店铺商品列表排序

**支持的排序方式：**
- `default` - 综合排序（按创建时间倒序）
- `sales` - 按销量倒序
- `price` - 按价格升序
- `newest` - 按创建时间倒序（最新上架）

**过滤条件：**
- 只查询上架商品（`status = 1`）
- 只查询未删除商品（`deleted = 0`）

---

## 五、部署步骤

### 5.1 执行数据库脚本

```bash
mysql -u root -p ym_item < ym-item/src/main/resources/sql/favorite_shop_follow.sql
```

**注意：**
- 脚本包含两张新表：`favorite` 和 `shop_follow`
- 包含测试数据的 INSERT 语句（已注释），可根据需要取消注释执行
- 执行测试数据前，请确认用户ID、商品ID、店铺ID是否存在

### 5.2 重新编译项目

```bash
cd /path/to/yunmeng-mall-back
mvn clean package -DskipTests
```

### 5.3 重启 ym-item 服务

```bash
# 停止旧服务
kill -9 <pid>

# 启动新服务
java -jar ym-item/target/ym-item-1.0.0.jar
```

### 5.4 验证接口

使用 Postman 或 curl 测试上述接口，确保返回正常。

---

## 六、注意事项

1. **用户认证**：所有接口都需要用户登录（通过 `UserContext.getUserId()` 获取当前用户ID）
2. **数据库表**：必须先执行 SQL 脚本创建 `favorite` 和 `shop_follow` 表
3. **索引优化**：已为 `user_id` 和 `item_id` / `shop_id` 添加索引，提升查询性能
4. **事务处理**：收藏和关注操作使用数据库事务保证数据一致性
5. **错误处理**：店铺不存在时会抛出异常，前端需处理 404 情况

---

## 七、后续优化建议

1. **平均发货时间** - 从订单表计算真实数据
2. **缓存优化** - 店铺详情数据可使用 Redis 缓存
3. **批量操作** - 支持批量收藏/取消收藏
4. **收藏列表** - 添加获取用户收藏列表接口
5. **关注列表** - 添加获取用户关注店铺列表接口
6. **消息通知** - 收藏/关注成功后发送通知

---

**文档生成时间：2026-05-28**
**后端开发者：AI Assistant**
