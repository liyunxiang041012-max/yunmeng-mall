# 支付接口文档 — 前端对接

**网关地址**: `http://localhost:8080` | **服务**: ym-pay | **网关前缀**: `/py/**` (StripPrefix=1, 去掉 `/py`)

---

## 状态码对照

| status | 含义 | 前端展示 |
|--------|------|---------|
| 0 | 待付款 | 支付页面，显示倒计时 |
| 1 | 已支付 | "已支付" |
| 2 | 已发货 | 物流信息 |
| 3 | 已完成 | "已完成" |
| 4 | 已取消 | "已取消" |
| 5 | 已退款 | "已退款" |

> 金额单位均为**分**，展示时 ÷100。

---

## 1. 创建订单

```
POST /py/order/create
```

**Header**: `Authorization: Bearer <token>` | `Content-Type: application/json`

**请求体**:
```json
{
  "items": [
    { "skuId": 1001, "quantity": 2 },
    { "skuId": 1002, "quantity": 1 }
  ],
  "userCouponId": "123456"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| items | Array | 是 | 购物车商品 |
| items[].skuId | Long | 是 | SKU ID |
| items[].quantity | Integer | 是 | 数量 |
| userCouponId | String | 否 | 优惠券ID，不用传 `null` 或不传 |

**成功响应**:
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "orderId": "1782528622610464430",
    "expireTime": "2026-06-27 11:20:23",
    "totalAmount": 500,
    "payAmount": 250,
    "discountAmount": 250
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| orderId | String | 订单号，后续全部接口用这个 |
| expireTime | String | 过期时间 `yyyy-MM-dd HH:mm:ss`，前端倒计时用 |
| totalAmount | Long | 原价（分） |
| payAmount | Long | 实付（分）→ 展示 ÷100 |
| discountAmount | Long | 优惠券抵扣（分），0 表示未使用 |

---

## 2. 获取订单详情

```
GET /py/order/detail/{orderId}
```

**Header**: `Authorization: Bearer <token>`

**成功响应**:
```json
{
  "code": 200,
  "data": {
    "id": "1782528622610464430",
    "userId": 1,
    "shopId": 10,
    "totalAmount": 500,
    "payAmount": 250,
    "discountAmount": 250,
    "couponId": null,
    "status": 0,
    "createTime": "2026-06-27T10:50:23",
    "updateTime": "2026-06-27T10:50:23",
    "payTime": null,
    "expireTime": "2026-06-27T11:20:23"
  }
}
```

---

## 3. 获取订单商品项

```
GET /py/order-item/list/{orderId}
```

**Header**: `Authorization: Bearer <token>`

**成功响应**:
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "orderId": "1782528622610464430",
      "skuId": 1001,
      "spuId": 5001,
      "shopId": 10,
      "name": "可乐2.0 250ml",
      "image": "https://oss.xxx.com/item/1.jpg",
      "price": 250,
      "quantity": 1
    }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| name | String | 商品名 |
| image | String | 商品图 |
| price | Long | 单价（分） |
| quantity | Integer | 购买数量 |

---

## 4. 支付 ⚠️ 改变订单状态

```
PUT /py/order/status
```

**Header**: `Authorization: Bearer <token>` | `Content-Type: application/json`

**请求体**:
```json
{
  "orderId": "1782528622610464430",
  "status": 1
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| orderId | String | 订单号 |
| status | Integer | 前端支付固定传 `1`（已支付） |

**成功响应**:
```json
{ "code": 200, "message": "操作成功" }
```

> ⚠️ **必须等此接口返回 200 才能展示"支付成功"，不能提前改 UI。**

---

## 5. 查询用户订单列表

```
GET /py/order/list
```

**Header**: `Authorization: Bearer <token>`

**成功响应**:
```json
{
  "code": 200,
  "data": [
    {
      "id": "1782528622610464430",
      "userId": 1,
      "shopId": 10,
      "totalAmount": 500,
      "payAmount": 250,
      "discountAmount": 250,
      "status": 1,
      "createTime": "2026-06-27T10:50:23",
      "payTime": "2026-06-27T11:00:00",
      "expireTime": "2026-06-27T11:20:23"
    }
  ]
}
```

---

## 前端标准流程

```javascript
// ① 创建订单
const { data: createRes } = await axios.post('/py/order/create', {
  items: cartItems.map(i => ({ skuId: i.skuId, quantity: i.qty })),
  userCouponId: couponId || null
})
const { orderId, payAmount, expireTime } = createRes.data

// ② 并行获取订单详情 + 商品项（展示支付确认页）
const [detailRes, itemsRes] = await Promise.all([
  axios.get(`/py/order/detail/${orderId}`),
  axios.get(`/py/order-item/list/${orderId}`)
])

// ③ 用户点击"确认支付"
// ⚠️ 必须等返回成功再跳转！
const payRes = await axios.put('/py/order/status', { orderId, status: 1 })

if (payRes.data.code === 200) {
  // 支付成功，跳转结果页
}
```
