# 云梦商城 - 管理员后台完整接口文档

---

## 一、商品审核

### 状态定义

| status | 含义 |
|--------|------|
| `2` | 审核中（新增默认） |
| `1` | 已上架（用户可见） |
| `0` | 已下架 |

### 接口

```
管理员 role=2  /it/shop/admin/item/...
```

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/shop/admin/item/page?status=2` | 待审核商品列表 |
| GET | `/it/shop/admin/item/page?status=1` | 已上架商品 |
| GET | `/it/shop/admin/item/page` | 全部商品 |
| PUT | `/it/shop/admin/item/{id}/approve` | 审核通过 → status=1 |
| PUT | `/it/shop/admin/item/{id}/reject` | 审核驳回 → status=0 |

**查待审核：**

```
GET /it/shop/admin/item/page?page=1&size=10&status=2
```

**响应：**

```json
{
  "code": 200,
  "data": {
    "total": 5,
    "list": [{ "id": 1, "name": "xx", "status": 2, "shopId": 10, "image": "...", "price": 9900 }]
  }
}
```

**审核：**

```
PUT /it/shop/admin/item/{id}/approve   → {"code":200}
PUT /it/shop/admin/item/{id}/reject    → {"code":200}
```

---

## 二、用户管理

```
管理员 role=2  /us/user/admin/...
```

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/us/user/admin/page` | 用户列表 |
| GET | `/us/user/admin/{id}` | 用户详情 |
| PUT | `/us/user/admin/{id}/status` | 启用/禁用 `{status:0/1}` |
| PUT | `/us/user/admin/{id}/role` | 改角色 `{role:0/1/2}` |

---

## 三、商家管理

```
管理员 role=2  /it/shop/admin/...
```

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/shop/admin/page` | 商家列表 |
| GET | `/it/shop/admin/{id}` | 商家详情 |
| PUT | `/it/shop/admin/{id}` | 编辑商家 |
| PUT | `/it/shop/admin/toggle-status/{id}` | 开关营业状态 |

---

## 四、分类管理

```
管理员 role=2  /it/categories/admin/...
```

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/categories/admin/all` | 全部分类（平铺） |
| POST | `/it/categories/admin` | 新增 `{name, parentId}` |
| PUT | `/it/categories/admin/{id}` | 编辑 |
| DELETE | `/it/categories/admin/{id}` | 删除（级联软删） |

---

## 五、系统通知

```
管理员 role=2  /us/user/admin/notice
```

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/us/user/admin/notice` | 发送通知 `{title, content, targetRole}` |

**targetRole：** `-1`=全部, `0`=普通用户, `1`=商家, `2`=管理员

---

## 六、仪表盘

```
管理员 role=2  /us/user/admin/...
```

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/us/user/admin/overview` | 仪表盘概览 |
| GET | `/us/user/admin/revenue?period=7` | 收入趋势（天） |

---

## 七、admin.js 路由汇总

```javascript
// 商品审核
GET    /it/shop/admin/item/page?status=2    // 待审核
GET    /it/shop/admin/item/page             // 全部
PUT    /it/shop/admin/item/{id}/approve     // 通过
PUT    /it/shop/admin/item/{id}/reject      // 驳回

// 用户管理
GET    /us/user/admin/page
GET    /us/user/admin/{id}
PUT    /us/user/admin/{id}/status
PUT    /us/user/admin/{id}/role

// 商家管理
GET    /it/shop/admin/page
GET    /it/shop/admin/{id}
PUT    /it/shop/admin/{id}
PUT    /it/shop/admin/toggle-status/{id}

// 分类管理
GET    /it/categories/admin/all
POST   /it/categories/admin
PUT    /it/categories/admin/{id}
DELETE /it/categories/admin/{id}

// 系统通知
POST   /us/user/admin/notice

// 仪表盘
GET    /us/user/admin/overview
GET    /us/user/admin/revenue
```
