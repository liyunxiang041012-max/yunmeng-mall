# 商家管理系统 接口文档

> 网关: `http://localhost:8080` | 路径前缀: `/it/shop/admin` | 2026-06-18

---

## 接口一览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/it/shop/admin/page` | 分页列表（支持搜索/筛选） |
| GET | `/it/shop/admin/{shopId}` | 查看商家详情 |
| PUT | `/it/shop/admin/{shopId}` | 编辑商家信息 |
| DELETE | `/it/shop/admin/{shopId}` | 删除商家（软删除） |
| PUT | `/it/shop/admin/toggle-status/{shopId}` | 开关营业状态 |

统一响应: `{ "code": 200, "message": "success", "data": ... }`

---

## 1. 分页查询商家列表

```
GET /it/shop/admin/page?page=1&size=10&keyword=云梦&status=1
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | int | 否 | 页码，默认 1 |
| size | int | 否 | 每页条数，默认 10 |
| keyword | String | 否 | 按店铺名称模糊搜索 |
| status | int | 否 | 1=营业 0=关闭，不传=全部 |

**响应**:
```json
{
  "code": 200,
  "data": {
    "total": 25,
    "pages": 3,
    "list": [{
      "id": 1,
      "userId": 10,
      "shopName": "云梦优选",
      "logo": "https://oss.xxx/item/xxx.jpg",
      "description": "专注好物",
      "status": 1,
      "deleted": 0,
      "createTime": "2026-06-18 12:00:00",
      "updateTime": "2026-06-18 15:30:00"
    }]
  }
}
```

---

## 2. 查看商家详情

```
GET /it/shop/admin/1
```

**响应**: 同上 `list` 中单个对象。

---

## 3. 编辑商家信息

```
PUT /it/shop/admin/1
Content-Type: application/json

{
  "shopName": "新名称",
  "logo": "https://oss.xxx/new.jpg",
  "description": "新描述"
}
```

三个字段都是可选的，传哪个改哪个。

**响应**: `{ "code": 200, "data": null }`

---

## 4. 删除商家（软删除）

```
DELETE /it/shop/admin/1
```

删的是 `deleted` 标记，数据还在数据库。想真删需要再找我。

**响应**: `{ "code": 200, "data": null }`

---

## 5. 开关商家状态

```
PUT /it/shop/admin/toggle-status/1
```

营业 → 关闭，关闭 → 营业，来回切。

**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "status": 0,
    ...其他字段
  }
}
```

---

## 前端页面建议

### 列表页
- 搜索框 + 状态下拉（全部/营业/关闭）→ 触发 `page` 接口
- 表格列：ID、店铺名称、logo缩略图、状态、创建时间、操作
- 操作按钮：编辑、删除、开关
- 分页用 `pages` 判断是否显示"加载更多"

### 编辑弹窗
- 表单：店铺名称、logo上传、描述
- 提交调 `PUT /it/shop/admin/{id}`

### 删除
- 弹窗确认后调 `DELETE /it/shop/admin/{id}`
- 删完刷新列表

### 开关
- 直接调 `PUT /it/shop/admin/toggle-status/{id}`
- `status===1` 显示"营业中"绿色标签，`status===0` 显示"已关闭"灰色
